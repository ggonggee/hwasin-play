# 인수인계 — 화신(Hwasin)

이 문서는 **이 프로젝트를 처음 이어받는 사람(또는 AI 에이전트)** 을 위한 것이다.
`README.md` 가 "어떻게 실행하나"라면, 이 문서는 **"무엇을 알고 시작해야 하나"** 다.

---

## 0. 현재 상태 스냅샷 (2026-09-24 · v5.311 기준 · 상세는 아래 4장 로그)

- **다중 창 잠금(v5.311)**: 두 창이 세이브를 번갈아 덮어써 새 창 진행이 사라지던 결함 차단 — 최근에 연
  창만 저장, 나머지는 잠금 화면. **원칙: 세이브를 쓰는 새 경로를 만들면 save() 를 거쳐라(소유 확인이 거기 있다).**

- **라이브 결함 3건 수정(v5.310 · 2026-09-24)**: ① 장비 상세 [분해] 재클릭 **골드 무한 복제** —
  `subBody()` 하위 화면이 `openModal` 전환 뒤에도 남던 구조 결함(이제 openModal 이 조건 없이 닫음 +
  처분된 장비 재화 이동 차단) ② 튜토리얼 STEP 7 ✕ 를 STEP 박스가 덮어 **손가락대로 눌러도 진행 불가**
  (박스 터치 통과 + 겹치면 아래로 비킴) ③ 가져오기 세이브·길드명의 **HTML 태그가 innerHTML 로 실행**
  (입구 정리). **원칙: 하위 화면은 openModal 이 항상 닫는다 — 호출부에서 closeSub 를 챙길 필요 없다.**

- **즉시 저장 전수 시정 완료(v5.306~309 · 2026-09-19)**: '상태만 바꾸고 저장 안 함' 유형 결함 4건
  연속 수정 — 투기장 주간 롤오버(재접속 토스트 반복) · 오프라인 정산 [수령](재접속 중복 수령) ·
  소진/수령 15곳(일일 상한 우회·무한 수령 창) · 공용 관문/클로저 결제 13곳(소환·제작·강화·용광로
  등). **원칙: 재화·소진·진행도 확정 지점은 공용 관문(dailyUse·summonRun·matSummon·mkBuy) 경유
  또는 onclick 끝 save().** 밸런스 무변경(저장 시점만) — 곡선 수치 그대로.
- **롤오버 사전 실물 검증 완료(2026-09-19)**: 9/21 축제 교체(성장→채굴)·10/1 용광로 월 리셋(+주간
  W40 골드 이중 롤오버)을 Playwright clock 미래 시각 고정 부팅으로 사전 실증 — 결함 0(6시나리오).
  당일 목격은 선택. **도구: docs/qa/20260919-미래시각-롤오버-실물QA.cjs** — 날짜 의존 회귀에 재사용.
- **재화 사슬 감사·주사위 교환(v5.305 · 대표 요청)**: 재화 11종 사슬 총람·막힘 판정 완료(감사서는
  docs/design/20260918-재화사슬-감사.md, 내부). 나쁜 막힘 1건(주사위 소비 단선)을 골드상점
  '영웅 소환서 X3=주사위 90개' 교환으로 해소 — 600h CP +3.6%(539,359→558,649).
- **콘텐츠 확장 루프 4종 완결(2026-09-17~18 · 대표 승인 방향)**: 잔불의 미궁(v5.294 — 일 1회 위험
  선택 3문 던전·최고 문 기록 표시 v5.299) · 주간 축제(v5.295 — 골드/성장/채굴 +20% 3테마 주 번호
  %3 순환·전장 배지+D-N v5.299) · 축제 의뢰(v5.297 — 주간 5번째 슬롯, 테마별 축·강화석 50) ·
  용광로 시련(v5.300 — 월 1회 보스전 foe 30,000·hpMul 3 장기전 v5.301·D-N v5.302). **영웅 추가는
  에셋 확보 전 보류**(sheets/가 9영웅 1:1·여분 없음 — 요청 사양: 직업별 스프라이트시트 1세트[동작
  9~10]+초상 1장). 모달 47종·던전 7종.
- **세트 정보 공개(v5.298)**: 공략 카드 현재 배율(setDamageMul 정본 파생)·세트 도감 단계별 ×N —
  10시드 스윕에서 발견한 세트 빌드 격차(×2.29~×5.33)의 정보 비대칭 해소. 시뮬 세트 선호 정합까지
  완료(진행 중 세트 한정 배율 가산 — 시드 99 이상치 해소·표준 궤적 ×2.29~2.71, ×5.33은 빌드 연구자 전용).
- **곡선(시드 42·소각 게이트+세트 선호 정책 기준선 · v5.305 반영)**: N 0.5h → R 10h → E 48h →
  L 96h → **600h CP 558,649(주사위 교환 +3.6%)** · **12800h CP 4,211,670 · 결정 ✦31** ·
  **25600h CP 6,228,929 · ✦48 · 세이브 16.1KB · 벽 없음** — v5.305 주사위 교환 정책 반영
  재측정(2026-09-19)에서도 CP·✦·세이브 완전 동일(초반 가속이 장기에서 수렴 — 말기엔 영웅
  9/9·조각 과잉이라 소환 축 한계 도달). 최장 공백 27.0h(@5.66M, 말기 1회 — 시뮬-일 경계
  구조적 공백, 종전 18.0h 관측과 동일 유형).
- **시뮬 도구 정확화(2026-09-17)**: 저확률 합성 소각 게이트(N→R 상시+포화 시 상위 — '눈물 0' 병목
  해소·기준선 리셋) · 10시드 재실증(N 0.5h 전 시드 동일·공백 ≤22h) · 최장 공백 18.5h의 정체=
  시뮬-일 경계 구조적 공백(결함 아님).
- **캐주얼 2400h 기준선(v5.299 시점)**: R 24.5h·E 97h·L 169h·CP 773,443·각성 +47 — 콘텐츠 4종
  반영 후에도 ±3h 유지(세그먼트 무관성). '시련' 종류 분리 후 **행동 8/8종 커버 실측**(시련 6% —
  풀 대비 접속 시간당 비율 우위: 일/주/월 리듬이 접속 무관).
- **접속 리듬 3층위**: 일일(미션·출석·탑·던전·잔불) → 주간(의뢰 5종+축제, D-N) → 월간(의뢰 4종+
  용광로 시련, D-N). 리셋 카운트다운·스냅샷 즉시 확정·공략 카드 리듬 라인.
- **엔드게임**: 각성 50 완주 후 결정(✦) 무한 축 · 극한 강화 +25(12800h 10부위 도달) · 레벨 상한 9,999 · 전종 완성 보상.
- **배지 전역 커버**: 퀘스트·출석·공지·주간/월간 의뢰·오프라인 정산·칭호 개선 — refreshClaimBadges 5초 주기.
- **시뮬**: 재현성(기본 42·seed=N) · 재미 3축(밀도·공백·행동 8종['시련' 분리] — 풀/캐주얼 8/8) · 세이브 용량 감시.
- **온보딩**: 튜토리얼 9단계 실물 완주 · 재접속 3경로.
- **유료 정합**: 프리미엄/가호 키 분리(v5.253) · give 전수 회귀 0결함(v5.254).
- **게이트**: 버프 키 이중 소모유 · give 상태 키 존재 · 모달 수·README 정합(47종).
- **실물 QA(최신)**: 전 모달 47종 스윕 결함 0건(2026-09-17, 팹인·줄높이 오탐 11건 무해 판정) ·
  용광로 전투 완주 3회 실증(승리 보상 정확·hpMul 3 접전 9.8초@CP 3.1k) · 축제/잔불 실측 ·
  **첫 로드 성능 무결(game.js gzip 238KB·DCL≤534ms)·에셋 404 0건** · 롤오버 clock QA(위).
- **환경 주의**: IAB 탭 누적 → 호스트 불능. **실물 QA 우회로 확립**: 전역 playwright 헤드리스 +
  세이브 주입(언로드 부활 회피) / 배치 결함 판정은 rect 실측 먼저·스크린샷은 보조(온보딩 오버레이 주의) /
  **미래 시각 재현은 page.clock.install(+addInitScript 세이브, 클릭 시퀀스 #btnStart→#lmGuest→
  #srvEnter→#scYes — docs/qa 스크립트가 정본)**.
- **전 모달 렌더 스윕 오탐 패턴 2종**: ① openModal 직후 rect=팝인 중간값(350ms 후 정상) ② 리프
  sh>ch 3~6px=줄높이 무해(수평 sw==cw).

---

## 1. 이 프로젝트가 뭔가

제작·수집 방치형 RPG의 **플레이 가능한 프로토타입**. Unity 이식 전에 재미와 구조를 검증하는 단계다.

핵심 루프:

```
방치 자동전투 → 몬스터 처치 → 재료 드랍
   → 대장간에서 제작·강화 (플레이어의 선택)
   → 전투력 상승 → 더 강한 사냥터 해금 → (반복)
```

방치형이지만 **"뭘 만들지 고르는 것"** 이 재미의 중심이다. 사냥터마다 나오는 재료가 다르고, 지금 필요한 재료를 주는 몬스터를 골라 사냥한다.

---

## 2. 아키텍처 — 소스 3개

```
index.html · style.css · game.js  +  assets/ (아이콘·몬스터·이펙트 PNG)
```

> ⚠ v5.82 에서 단일 파일 미러(`화신.html`)와 `build-single.mjs` 파이프라인을 폐기했다.
> 아이콘이 외부 PNG 에셋으로 바뀌어 한 파일로 묶는 의미가 사라졌기 때문이다.
> `npm run build` / `build:check` 는 지금 실패하는 게 정상이고, 게이트는 `npm run check` 하나다.

- **`game.js` 가 사실상 전부다.** 약 5,300줄 한 파일. 모듈 시스템·번들러 없음. 브라우저가 그냥 로드한다.
- 구조는 크게 **① 상수 테이블 → ② 헬퍼 함수 → ③ `MODALS` 객체(화면 47종) → ④ `Battle` 모듈(캔버스 전투) → ⑤ 메인 루프·초기화** 순.
- 화면 하나 = `MODALS` 의 키 하나. `MODALS.shop.render(container)` 식으로 그린다.
- 상태는 전역 `S` 객체 하나. `localStorage` 에 통째로 직렬화한다.

### 화면을 여는 두 가지 방식

| 함수 | 용도 | 부모 화면 |
|---|---|---|
| `openModal(key, arg)` | 최상위 화면 전환 | 교체됨 |
| `openSub(key, arg)` | **하위 화면을 부모 위에 얹음** | 뒤에 남음 |
| `subBody(title)` | MODALS 가 아닌 함수형 하위 화면용 | 뒤에 남음 |

원작은 재료합성·장비강화·영웅상세 같은 하위 화면을 **부모 위 오버레이**로 띄운다. 대장간 탭이나 장비 슬롯이 팝업 가장자리로 계속 비친다. 그래서 이 3종을 구분해서 쓴다.

`openModal` 은 **같은 키로 다시 호출되면 부모를 갈아엎지 않고 그 자리에서 갱신**한다(스크롤 위치 유지). 하위 화면들이 자기 자신을 `openModal` 로 새로고침하는 기존 패턴을 살리기 위한 장치다.

---

## 3. ⚠ 반드시 알아야 할 함정

이어받는 사람이 **가장 시간을 많이 날릴 지점들**이다. 전부 실제로 겪은 사고다.

### 3-1. ~~`화신.html` 을 직접 고치면 안 된다~~ — 폐기됨(v5.82)

단일 파일 미러는 삭제됐다. 소스 3개만 고치면 되고 별도 빌드 단계는 없다. 2장 참조.

### 3-2. `S.mats` 에 **등급 키**를 쓰면 안 된다 — 5번 물린 함정

재료는 **실제 재료명**이 키다 (`S.mats['흑염석']`). 과거에 등급 공용풀(`S.mats['N']`, `S.mats['R']`…)이 있었는데 v4.3 에서 폐지했다.

```js
S.mats[grade] += n     // ❌ undefined + n = NaN. 조회도 소비도 안 됨.
matGain('흑염석', n)    // ✅ 실제 재료
matGainGrade('R', n)   // ✅ 등급만 정해졌을 때 (그 등급 재료 중 하나로 실체화)
```

폐지 이후 **사냥 드랍 → 요일던전 → 시련의 탑 → 인벤토리 표시 → 재료 소환** 순으로 **다섯 번** 잔재가 발견됐다. 지금은 0건이지만, 새 코드를 쓸 때 이 패턴이 손에 익어 다시 나올 수 있다.

> 왜 폐지했나: 공용풀 60을 N재료 6칸이 **각자 자기 것처럼 중복 표시**해서, 화면 합계가 432인데 실제 총량은 132였다. 흑염석 30개를 쓰면 손도 안 댄 녹옥정이 72→54로 같이 줄었다.

### 3-3. 세이브 마이그레이션 플래그를 `freshState()` 에 넣으면 안 된다

`mergeDefaults()` 가 `freshState()` 의 값으로 빈 필드를 먼저 채우기 때문에, 마이그레이션 판정용 플래그를 `freshState` 에 두면 **이관이 통째로 스킵된다.**

몬스터 8종 → 120종 확장 때 `_huntV` 를 `freshState` 에 넣었다가 이관이 아예 안 돌아간 적이 있다. 판정 플래그는 `freshState` 에 두지 말 것.

### 3-4. `.modal` 의 `transform` 을 덮어쓰면 안 된다

`.modal` 은 `transform: translate(-50%,-50%)` 로 중앙 정렬되고 `animation: pop` 팝인이 이미 붙어 있다(`style.css` 상단). 여기에 트랜지션을 넣겠다고 `transform` 을 건드리면 **모달이 화면 밖으로 밀린다.**

### 3-5. CSS 그리드는 `minmax(0,1fr)` 를 써야 한다

`repeat(N, 1fr)` 의 자동 최소값은 `min-content` 라, 셀 내용(이모지 등)이 크면 **트랙이 컨테이너보다 넓어져 레이아웃이 깨진다.** 실제로 대장간 5열 그리드가 1열 가로스크롤로 붕괴했었다.

### 3-6. 세트효과는 **착용** 기준이다

`setPieceCount()` 는 `e.equipped` 인 장비만 센다. '보유' 기준으로 세면 9개 세트가 전부 동시에 켜져 배율이 무한정 곱해진다.

> **2026-09-25 보강(검증 워크플로 #19)**: 세트 집계는 **영웅 구분 없이 계정 전역**이다. 그래서 작열의 무기 3종(같은 슬롯)을 영웅 3명에게
> 나눠 착용하면 8조각에 도달한다 — '작열 8세트 도달 불가' 가 아니다. 이것이 칭호 '용암'(lava)의 **유일한 획득 경로**라 의도적으로 유지한다.
> 영웅별 판정으로 바꾸면 lava 가 획득 불가가 된다(바꾸려면 칭호 조건부터 재설계).

### 3-6a. `slotSchema`(스탯 표시)와 `slotKeyOf`(착용 부위)를 섞어 쓰면 안 된다 — v5.228

`slotSchema` 는 아이템 상세의 스탯 **표시**용 그룹(투구·상의·하의·견갑 → 전부 '방어구', 신발·목걸이 → '보조')이다.
v5.81이 장착 교체 판정에 이 `part` 를 재사용하면서 **방어구는 한 영웅이 한 벌만 입을 수 있게 됐다.**
페이퍼돌은 10슬롯을 표시하고, 스모크 이관 테스트(v5.108)는 투구+상의+하의 6부위 동시 착용=주술 6세트를
검사하는데, 정작 실제 장착 경로만 어긋나 있었다 — 그 사이 **세트 대부분이 6세트 도달 불가**였고
(600h 시뮬 setm ×1.000 고정), 그늘칼 부위(면갑·흉갑 등)는 페이퍼돌에 표시조차 안 됐다.

v5.228 해결: 착용 부위 정본은 `slotKeyOf`(`SLOT_KEYS`, 페이퍼돌 10슬롯: 투구/목걸이/상의/하의/신발/벨트/
무기/방패/반지/정수). 장착 교체·페이퍼돌·시뮬 전부 이것으로 판정하고, 스탯 표시만 `slotSchema` 를 쓴다.
장착 본체는 `equipItem(e, heroId)` 전역 함수로 추출(스모크가 실경로 검증). 회귀 테스트가
'투구·상의·하의 동시 착용 + 무기 교체 파괴 + 세트 6도달 + 세트별 슬롯 분산 invariant'를 지킨다.

> 작열 8세트만 예외로 남아 있다 — 단검·대검·도끼 3조각이 같은 무기 슬롯이라 최대 6세트.
> 세트 정의(임계값·수치)는 실측 기반이라 함부로 못 바꾼다. 8단계 효과(쿨타임 35% 감소)도
> 미구현 상태라 '죽은 단계'다 — 추후 쿨타임 축 구현 시 함께 정리할 것.

### 3-7. 재료는 등급당 **6종**, 몬스터는 등급당 **5종** — 커버리지가 자동으로 안 맞는다

`HUNT_TIERS` 의 고정 드랍은 `mat = pool[n % 6]` (n=0..4)이라, **각 등급 6번째 재료는 어떤 몬스터의
고정 드랍도 아니게 된다** — 잿가루(N)·서리결정(R)·천공수정(E)·금강석(L). 레시피는 이 4종을 66곳에서
요구하는데(L 금강석만 24곳), 획득 경로가 `10% 랜덤 등급 드랍 × 1/6 ≈ 킬당 1.7%` 뿐이라 대표 재료의
**35% 대비 약 21배 느렸다.** 게다가 대장간 재료 칩 → 파밍 팝업이 '같은 등급 전체'를 보여줘서,
안내대로 사냥해도 그 재료가 안 나왔다.

v5.111 에서 등급 내 중간 난도 몬스터(`n===2`)에 **보조 고정 드랍 `mat2`** 를 붙여 24종 전부를 커버했고,
팝업은 실제 드랍 몬스터를 위로 올리고 나머지엔 '랜덤' 라벨을 달게 했다.

> **몬스터나 재료 수를 바꾸면 이 커버리지를 다시 확인하라.** 검사법: `HUNT_TIERS` 의 `mat`+`mat2`
> 집합이 `MATS` 전체를 덮는지 대조. 지금은 24/24.

---

### 3-8. 전투가 늘어지면 서든데스로 끊는다 (v5.117)

방어력이 높으면 아무도 안 죽고 제한시간만 흐르는 판이 나온다. 시간만 줄이면
'못 끝내고 패배'가 늘 뿐이라, **늦어질수록 피해가 커지는 가중**을 넣어 결판을 강제한다.

```js
const OVERTIME = { at:30, stepSec:5, stepMul:0.6 };   // 30초 이후 5초마다 +60%p
```

- `at` 초까지는 각자의 기본 공격·스킬 그대로. 그 뒤부터 **양측 피해가 함께** 커진다.
  (30초 x1.0 → 40초 x2.2 → 50초 x3.4 → 60초 x4.6)
- 켜는 법: `Battle.startDungeon({ ..., overtime:true })`. 규칙을 바꾸려면 객체로 넘긴다
  (`overtime:{ at:20, stepSec:5, stepMul:1.0 }`). **지정하지 않은 전투는 배율 1 고정이라 영향이 없다.**
- 현재 켜져 있는 곳: 투기장(`arenaFight`). 투기장 계열 콘텐츠를 새로 만들면 이 플래그를 붙여라.
- 표시: 투기장 헤더의 `서든데스 xN.N` 배지. 헤더는 벽시계가 아니라 `Battle.timeLeft()`·`Battle.otMul()`
  즉 **전투가 실제로 쓰는 값**을 읽는다 — 표시와 판정이 어긋나지 않게 하려는 것이니 되돌리지 마라.

---

## 4. 검증 도구 — 이게 이 프로젝트의 안전벨트

테스트 프레임워크 없이 Node 스크립트 3개로 굴린다. **도구에 종속되지 않으니 그대로 쓰면 된다.**

| 명령 | 하는 일 |
|---|---|
| `npm run verify` | 정적 무결성 — `MODALS` 키 ↔ `data-modal` ↔ `openModal` 인자 정합, `freshState` 필드 대조, 중복 선언, DOM id 존재 여부, **버전 일치** |
| `npm run smoke` | 런타임 — 최소 DOM 스텁 위에서 `game.js` 를 실제 실행. 모달 47종 전수 render/open, **구세이브·빈세이브·손상세이브 마이그레이션**, 클릭 핸들러 400여 건 전수 실행, 재화 음수/NaN 검사 |
| ~~`npm run build:check`~~ | ~~미러 동기화~~ — **폐기(v5.82). 지금은 실패하는 게 정상** |
| **`npm run check`** | **문법 검사 + verify + smoke. 커밋 전 이것만 돌리면 된다.** |
| `npm run setup` | push 전 검사 훅 설치(`core.hooksPath=.githooks`). **클론 후 1회.** |
| `npm run bump 5.131` | 버전 정본(package.json)을 올리고 `index.html` 캐시버스터·화면 표기를 자동으로 맞춘다 |

### 검사가 실제로 막는다 (2026-09-10 개편)

종전에는 `verify.mjs` 가 **문제를 찾아도 종료코드 0** 이라, `npm run check` 에 물려 있으면서도
정적 무결성 위반을 하나도 막지 못했다(출력만 했다). 지금은 아래 항목에서 실패하면 종료코드 1 이다.

| 코드 | 막는 것 |
|---|---|
| A · B | `data-modal` / `openModal` 대상이 `MODALS` 에 없음 → 눌러도 안 열리는 화면 |
| C | `freshState` 에 없는 `S.필드` 참조 → 구세이브에서 `undefined` → NaN. 의도한 예외는 `FS_EXEMPT` 에 이유와 함께 적는다 |
| D · E · F | 최상위 중복 선언 · 없는 DOM id 조회 · `index.html` 중복 id |
| J | `package.json` 버전 ↔ `index.html` 캐시버스터·화면 표기 불일치 |
| K | **CSS 계약** — 없어지면 기능이 조용히 죽는 규칙(튜토리얼 손가락 z-index, 서든데스 배지, 오류 띠 z-index 등)과 정의 없는 `@keyframes` |

`[B2]` 고아 모달과 `[G]` 미정의 심볼 후보는 **경고로만** 둔다 — 정규식 오탐과 의도된 예외가 섞여 있어
실패로 올리면 게이트가 늑대소년이 된다.

### 자동 실행 지점

| 언제 | 무엇 | 한계 |
|---|---|---|
| `git push` 직전 | `.githooks/pre-push` 가 `npm run check` 를 돌리고, 실패하면 push 를 막는다 | 훅을 설치한 PC 에서만 (`npm run setup`) |
| GitHub 에 올라간 뒤 | `.github/workflows/check.yml` 이 같은 검사를 돌린다 | `ip-banlist.json` 이 공개 저장소에 없어 **[9] 금칙 스캔은 건너뛴다**. 저장소 secret `IP_BANLIST_JSON` 을 등록하면 그것까지 돈다 |

`[K]` 는 2026-09-07 사고의 직접 대응이다. 스모크는 DOM 스텁 위에서 돌아 CSS 가 아예 없고,
verify 는 CSS 를 줄 수만 셌기 때문에 그 회귀를 아무도 잡지 못했다. 디자인 변경까지 막으면 게이트가
방해물이 되므로, **'이 규칙이 없거나 값이 틀리면 기능이 죽는 곳'** 만 목록으로 못박고 각 항목에 이유를
적어 뒀다(`verify.mjs` 의 `CONTRACTS`). 새 항목을 넣는 기준은 "화면이 조금 달라 보인다" 가 아니라
"눌러도 안 보인다 · 마크업은 나오는데 스타일이 없다 · 가려서 조작이 안 된다" 다.

> 왜 두 겹인가: 2026-09-07 에 검사를 거치지 않은 옛 `style.css` 사본이 그대로 배포돼
> v5.115~v5.127 의 CSS 수정 6건이 라이브에서 되돌아갔고, **3일 동안 아무도 몰랐다.**
> 훅은 사고를 막고, CI 는 (막지는 못해도) 즉시 알린다.

`smoke-test.mjs` 는 특히 값지다. 실제로 여러 번 커밋 직전에 회귀를 잡아냈다(문자열 일괄 치환이 의도 밖 모달을 깨뜨린 사고 등). **새 기능을 넣으면 여기에 검사도 같이 추가하는 걸 권한다.**

### 브라우저에서 직접 검증하는 법

DOM 스텁으로는 CSS 레이아웃·실제 렌더를 못 본다. 브라우저 콘솔에서 이렇게 격리 실행하면 최신 소스로 실측할 수 있다.

```js
// 개발 서버(npm run dev)를 띄운 상태에서, 콘솔에 붙여넣기
const [h,c,j] = await Promise.all(['index.html','style.css','game.js']
  .map(f => fetch(f+'?ns='+performance.now(), {cache:'no-store'}).then(r=>r.text())));
const doc = h.replace('<link rel="stylesheet" href="style.css">','<style>'+c+'</style>')
             .replace('<script src="game.js"></script>','<script>'+j+'<\/script>');
const ifr = document.createElement('iframe');
ifr.style.cssText = 'width:375px;height:812px;border:0;position:fixed;right:0;top:0;z-index:9999';
document.body.appendChild(ifr);
ifr.contentDocument.write(doc); ifr.contentDocument.close();
// 이후 ifr.contentWindow 로 게임 내부 함수 호출 가능
// 주의: const 로 선언된 최상위 심볼은 window 에 안 붙는다 → ifr.contentWindow.eval('MODALS') 로 접근
```

### 2026-09-12 실브라우저 전체 검증 (v5.173 기준) — 결과와 노하우

ZCode 내장 브라우저로 `npm run dev`(localhost:8765) 을 띄워 신규 진입(START→게스트→서버)부터
직접 플레이하며 v5.145~173 의 신규 화면을 전수 확인했다. **버그 0건.** 검증된 것:
몬스터 도감(실물 스프라이트·조우 1/20·실루엣+??? 19종·등급 프레임색), 영웅 도감(초상화 9종·
미보유 실루엣), 대장간 '지금 제작 가능'(탭 개수 일반 17·희귀 28 + 셀 ✓), 아이템 제작 팝업(90% 환급
문구), 파밍 팝업(고정 드랍/랜덤 라벨·사냥 버튼), 소환 피티(1.0%·70회 카운트다운), 영웅 XP 바(보유만,
Lv28 73.8%), 배지 점 4개(☰·퀘스트·출석·공지 — DOM으로 확인), N등급 초록 틴트, 도감 신규 기록
시스템 로그, 튜토리얼 손가락(팝업 주 버튼 짚기·목표 미리 선택), 재료 상한 비파괴(구세이브 2048 유지).

### 2026-09-13 실브라우저 QA (v5.228~230 변경분) — 튜토리얼 완주 + 봉쇄 버그 2건 수정

v5.228~230 의 변경(장착 슬롯·페이퍼돌·세트·확인창·시뮬 축)을 신규 세이브로 처음부터 플레이해
검증했다. **통과**: 방패 제작(30초 타이머·재료 차감·결과 팝업) → 장착(확인창) → **페이퍼돌 10슬롯에
방패 +0 표시 + 세트 칩 '잔불 1/6'** → 강화(+1 성공·v5.222 구간 표시) → 영웅 소환(자동 해금 '도르카
등장') → 재료 소환(수량 확인창) → 합성 → 진영 편성 저장 → 투기장 입장·매칭 → 미션 보상 → 클래스
선택까지 **STEP 1~9 완주**.

**봉쇄 버그 2건(둘 다 신규 유저 온보딩을 영구 막음 — 실물 재현으로 발견, v5.231 수정):**
1. **STEP 7(합성) 구조적 불가** — N등급은 합성 불가(v5.86), 합성 대상은 R(조각 80)뿐인데 초기
   얼음 조각이 20이라 영원히 못 채움. 게다가 불 60은 첫 소환에서 자동 해금(25)이 도르카를 미리
   지급해 합성 수업 대상을 소멸시킴. 수정: 초기 얼음 80(리엔 보유 위에 비케 합성 수업 완결) +
   구세이브 구제(튜토리얼 미완료·합성 0회면 얼음 80 보충) + 스모크 회귀(초기 킵으로 R 합성 가능).
2. **STEP 9(미션 보상) 봉쇄** — 투기장 '자동입장' 연전이 입장권 자동 충전(40초/1개·상한 30) 때문에
   사실상 무한히 이어지고, tutPoll 이 전투 중엔 보상 팝업을 안 열어 영영 대기. 수정: 튜토리얼
   완료 전 자동입장 금지(토글 거부 + 결과창 연전 차단).

노하우 추가:
- **캐시버스터 미변경 상태에서 game.js 를 고치고 reload 하면 캐시에서 옛 파일을 읽는다** —
  브라우저 검증 전 반드시 `npm run bump` 로 캐시버스터부터 올려라(이번에 실제로 걸림).
- **폴링 조건에 트래커 상시 문구를 쓰면 오탐** — "미션 완료 보상"은 STEP 9 트래커에 항상 떠
  있어 팝업 오픈 판정이 아니었다. 팝업 고유 텍스트('전투형' 등)로 걸러야 한다.
- 투기장 결과창은 3초 뒤 자동 진행 — 검증 시 결과 확인은 3초 안에.
- **실브라우저 검증의 최강 도구는 게임 자체의 `#errbar`** — 스모크 DOM 스텝은 예외를
  삼키는 지점이 있어(MODALS 클릭 커버리지의 catch(e){}) 실물에서만 보이는 결함이 있다.
  무반응 버튼을 만나면 domSnapshot 전에 `#errbar` 텍스트부터 읽어라.
- **MODALS 키 순회 커버리지의 사각: 전역 팝업** — openMatMonsterPopup 처럼 currentModal 을
  직접 바꾸는 팝업은 재료 칩 onclick 안에서 열려 커버리지 수집 이후에 생긴 버튼은 안 눌린다.
  이런 팝업은 별도 step 에서 렌더+버튼 onclick 직접 호출로 검사한다(v5.232 회귀 참고).

### 2026-09-13 실브라우저 QA 2차 (중반 콘텐츠) — 핵심 파밍 루프 사망 버그 수정 (v5.232)

인벤토리 재료 탭(24종·등급풀 가용 표기)·재료 칩→파밍 팝업·방치 정산(오프라인 골드 50%·8h
표시)을 실물 검증하다 **라이브 중증 결함 1건 발견·수정**:

- **파밍 팝업 [사냥] 전멸(실브라우저)** — v5.225 가 확인창 코드를 넣으며 MODALS.monster 의
  `danger`/`leadCP` 변수만 인용하고 선언을 가져오지 않아 `danger is not defined` 로
  **재료→몬스터 선택이라는 코어 루프의 버튼이 실물에서 전부 죽어 있었다**(v5.225 배포~v5.231,
  약 8버전). 스모크가 못 잡은 이유: ① MODALS 키 순회 커버리지는 이 전역 팝업의 버튼을
  수집하지 않음 ② 커버리지가 예외를 삼킴. 발견 경로: 버튼 무반응 → 게임 자체 `#errbar` 에
  "danger is not defined ×3". 수정: 팝업에 leadCP/danger 정의 추가 + ⚠ 표기도 총전투력→
  리더 기준으로 통일(v5.185 정신) + smoke 회귀(24재료 × 전 [사냥] onclick 직접 호출).
  실물 재검증: 안전 몬스터 즉시 사냥 전환 ✓ / 위험 몬스터 "⚠ 전멸 위험 — 권장 3,600 ·
  내 출격 영웅 1,672" 확인창 ✓ (v5.225 의도 동작이 처음으로 실물 가동).

### 2026-09-13 실브라우저 QA 3차 (상점·각성·시련의 탑) — 결함 0건, 정본 공식 수치 일치 확인

v5.232 로 상점·각성·탑 축을 실물 검증했다. **신규 결함 없음** — 특히 탑은 보상 수식이
정본 공식과 소수점까지 일치하는지 실측했다:

- **상점**: ☰ 드로어 10+항목 정상 렌더 · 9탭 · 광고 제단(15/15) · 골드 상품 '망치 X10'
  구매 실행 — 골드 6,713.8만 → 5,214.4만(1,500만 차감 정확) + 토스트.
- **각성**: +0 화면(조각 387·심화 안내 문구) → 1회 실행 → +1(+1.5% 3스탯) 표시 · 다음
  비용 270으로 상승 · 조각 부족 시 버튼 disabled — 경제 정상.
- **시련의 탑 전 사이클(★핵심)**: 입장 확인창 → 실전 웨이브 전투 → **"12 Wave · 골드
  +480만 · 강화석 +36 · 웨이브 상자 +6"** — 12×40만·12×3·floor(12/2) 로 정본 공식과
  전부 일치 → 최고기록 12 Wave 갱신 표시 → 소탕(기록 50%: 골드 240만·상자 +3) → 교환
  팝업(기록서 8상자·소환권+ 4·재료권+ 3·재료 4종) → **기록서 1권 교환 성공**.
  심화 각성 재화 경제(탑→상자→기록서)가 실물에서 완결 가동.
- **실측 참고**: 리더 CP 1,672로 12 Wave 도달 — 시뮬의 탑 근사(1:1 전투력 기준, 같은
  CP면 약 5~6웨이브 예상)는 보수적이다. 실전투는 회복·스킬로 CP 대비 더 버틴다 →
  시뮬의 기록서 경제 속도는 실제보다 느리게 측정된다(안전 방향 오차).
- **못 연 것**: 골드던전·길드 — ☰ 드로어/하단 레일 자동화 클릭이 타이밍 문제로 계속
  실패(시련의탑·같은 그리드 패턴은 성공했으므로 게임 결함이라는 증거 없음). 다음 검증
  과제로 남긴다. QA 세이브(localhost localStorage)는 각성·탑 일일·골드 소모 오염됨 —
  실이용자 영향 없음.

### 2026-09-13 실브라우저 QA 4차 — 콘텐츠 레일 z-index 결함 수정 (v5.233)

골드던전·길드 검증을 이어가다 원인을 특정했다:

- **결함(수정)**: `#chat`(z-index 6, 하단 220px 고정)이 `#content-rail`(z-index 5) 위에
  있어, ☰ 메뉴의 던전·길드 항목 11종이 채팅 박스 **아래 층**에 깔린다. 렌더는 반투명
  그라데이션이라 비쳐 보이지만 클릭 가능성이 구조적으로 깨진다. **z-index 5→7** 로 수정
  (채팅 6 위 · 하단 nav 7 동급 · 사이드메뉴 8·모달 20 아래 — 층서 자연스러움).
- **자동화 클릭 불가의 정체**: 이 레일은 Playwright locator 클릭·CUA 좌표 클릭 모두
  일관 실패(hit-test가 다른 층 요소를 보고). 스테이지가 453px 기준 스케일이라 375px
  IAB 뷰포트와 좌표계가 어긋나는 것으로 추정 — 비전 판독(2회 교차)으로는 **렌더 정상,
  라벨 겹침/잘림 없음** 확인. 즉 "이미지가 깨진다"가 아니라 "클릭 계층" 문제였고,
  z-index 수정이 그 해결책이다.
- **교훈(레일류 UI 검증법)**: absolute + 스케일 스테이지의 UI는 좌표 클릭을 믿지 말
  것 — ① 렌더는 스크린샷+비전 교차 판독 ② 클릭 계층은 CSS z-index 정적 대조 ③ 동작은
  스모크의 data-modal 커버리지에 의존. 스크린샷이 타일 노이즈를 낼 때 좌표 산출은
  비신뢰다.
- 골드던전·길드 모달의 실물 렌더·동작 검증은 여전히 미실시(다음 검증자는 모달 z20 위
  요소에서 진입할 것 — 예: 스모크 커버리지 경유).

### 2026-09-13 실브라우저 QA 5차 — ☰ 드로어가 레일 상단을 덮던 결함 수정 (v5.235) + 골드던전·길드 실물 검증 완료

4차의 z-index 수정(v5.233)에도 클릭이 계속 실패하는 이유를 `getBoundingClientRect()`
실측으로 밝혀냈다 — **z-index와 무관한 두 번째 겹침**이 있었다:

- **결함(수정)**: ☰ 토글은 sidemenu와 content-rail을 **한 덩어리로 같이 열고**(v4.9),
  그 sidemenu가 행 93/75/75/93px(총 369px, top 86 → bottom 455, x 237~447)이라
  **레일 첫 행(top 353)과 353~455 세로 겹침**. 요일던전·보스·골드던전·레이드 아이콘이
  "도움말·공략·7일출석" 패널 아래에 깔려 보이지도 눌리지도 않았다(모든 이용자).
  행이 커진 범인 2개 — ① `.gi`의 line-height(28px 폰트×1.5=42px)가 아이콘 34px보다
  커 행을 +8px ② 4글자 라벨("세트효과" 등)이 셀 폭(약 47px)보다 넓어 2줄 wrap로 +18px.
  **수정**: `.gi`를 flex로(라인박스 무시) + 라벨 11px nowrap + 여백 축소 → 행 59px 균일,
  패널 bottom **343 < 레일 353**(10px 여유). 로컬·라이브 양쪽 실측으로 확인.
- **골드던전·길드 실물 검증 완료(3루프 숙제 해소)**: 겹침 해소 뒤 드로어 연 상태에서
  아이콘 클릭 → 골드던전 "황금 용광로"(5단계·재료조건·일 3회·자동입장 안내) · 길드
  미가입 화면(생성 3억/1억·가입 검색) 정상 렌더, #errbar 오류 0건. 라이브(신규 세이브)에
  서는 튜토리얼 `.ob-prog` 차단으로 모달 진입 불가 — **설계된 동작**(아래 노하우 참고).
- **교훈(레이아웃 분쟁의 최종 판정법)**: 비전 판독 2회가 서로 모순(패널이 65~70%/100%
  까지 vs 4열 정상)났을 때 정답은 **비전이 아니라 `getBoundingClientRect()` 실측**이었다
  (실제는 43% 지점까지 — 두 판독 모두 절반쯤 틀림). 겹침 의심은 rect 좌표 4개(top/
  bottom/left/right)끼리 직접 대조하면 한 번에 확정된다. 스크린샷·비전은 렌더 확인용.
- **노하우(좌표 변환)**: IAB 새 탭 뷰포트(1280×720)에선 #device가 449,0에서 383×720
  (스케일 0.845)로 렌더된다. 게임 내부 좌표(453×852) → 뷰포트: `x=449+gx×0.845,
  y=gy×0.845`. CUA 클릭은 이 변환을 거쳐야 하고, Playwright locator는 absolute UI의
  hit-test 엄격성에서 자주 타임아웃 나니 cua 좌표 클릭이 낫다. 오래된 탭은 클릭 불응이
  재현(새 탭 열면 해결) — 4차에서도 같은 패턴.

### 2026-09-13 실브라우저 QA 6차 — v5.236 엔드게임 연장 2축 실물 검증 + 강화 버튼 sticky (v5.237)

- **심화 각성 50**: 세이브 각성 31·기록서 15로 실물 확인 — "+31·스탯 46.5%" 표기,
  "영웅 기록서 10권 소모(보유 15)" → 클릭 → **+32·기록서 5권 차감·다음 비용 11권·
  남은 총 270권** — 전부 공식과 정합(280권 수기 검산 일치). errbar 0건.
- **극한의 벼림 +21~25**: 레전더리 잿불 단검 +20 — "성공 30% · 골드 2000만 · 강화석 5"·
  ⚗ 문구·'극한 (성공 30% · 실패해도 유지)' 구간 표기. 실행: 실패 시 **"극한의 벼림
  실패 — 단계 유지 (재화만 소모)" 토스트 + enh 20 유지 + 장비 생존**, 성공 시
  "강화 성공 +21". 재화 정밀 대조: 골드 6천만 차감(3회×2천만)·강화석 500→485(3회×5)
  — 소수점까지 일치.
- **결함(수정 v5.237)**: 위험 안내(v5.222)+극한 안내(v5.236)가 쌓이며 강화 오버레이가
  887px로 길어져 .sub-body(632px) 초과 — **'강화' 실행 버튼이 첫 화면에 안 보이고
  255px 스크롤해야 누를 수 있었다**(실측 rect y 772 > 뷰포트 720). 실행 버튼을
  `position:sticky; bottom:0`으로 고정 — scrollTop 0에서 vpY 596 첫 화면 노출 실증.
- **노하우(QA 세이브 주입은 '가져오기' 경로로)**: localStorage를 직접 고쳐 reload하면
  **게임이 unload 시점에 메모리 S로 세이브를 다시 써서 주입이 항상 무효화된다**
  (부팅 직후 이미 옛 값). 정석은 게임 내 설정→진행도 가져오기 — 파싱·핵심 필드 검증·
  백업·_saveSealed(unload save 차단)를 게임이 알아서 처리한다. 붙여넣기는 textarea에
  value 주입으로 대체하고, 오버레이 버튼은 #modal-root가 'on'일 때만 화면 안에
  렌더됨에 주의(openModal('settings') 후 saveImport()).
- **다음 과제 메모**: 강화석 과잉 — 1600h 시뮬 보유 5,310개로 유입(탑·일일)이 소모
  (극한 5/시도 포함)보다 여전히 많다. 싱크 확대(극한 강화석 비용 상향 등) 후보.

### 2026-09-13 v5.238 — 강화석 과잉 해소 (망치 강화석 교환 + 극한 20개/시도)

- **교훈(싱크 설계)**: 극한 강화석 단가만 올리는(5→20) 첫 시도는 **보유가 오히려
  5,310→5,402로 늘었다** — 시도 횟수가 골드·망치 비용에 제한돼 있어 단가 인상의
  총소모 증가가 미미했기 때문. 재화 과잉은 '소모처 부족'이지 '단가 낮음'이 아닐 수
  있다 — 시뮬로 먼저 총소모 반응을 본 뒤 접근을 고를 것.
- **채택안**: 골드상점에 강화석→망치 교환(일반 150/전설 500 — 골드 정본가 환율).
  유입 실측(탑 소탕 도달×3)은 못 건드리므로 소모처를 새로 만들었다. 과잉 싱크 +
  망치 골드 병목 완화 + 극한 진행 가속이 한 축으로 해결.
- **시뮬 전후(1600h)**: 보유 5,402→665(4,737개 소화) · 평균 강화 19.1→24.2 ·
  +25 완주 0→2부위 · CP 811,700→901,953(+11%). 망치 구매가 골드 3,960M →
  골드 1,760M+강화석 2,500개로 분산.
- **상점 결제 헬퍼**: CURN/have/payCur에 stones 통과 추가 — 다른 통화 상품도
  같은 패턴으로 3곳만 고치면 된다.
- **실물**: 교환 상품 렌더·구매(480→330·망치+10)·강화석 부족 시 disabled 확인.

노하우(다음 검증자가 같은 데 시간 쓰지 않게):
- **튜토리얼 STEP 2~9 중엔 `.ob-prog` 박스가 목표 외 클릭을 가로막는다.** 도감 탭 전환·
  인벤토리 칩 클릭 등은 STEP 완료 후에 하거나, 튜토리얼 목표 화면(대장간 등) 안에서 검증하라.
- **비전 모델은 7px 배지 점을 놓친다** — 작은 UI는 스크린샷 판독 말고 DOM
  (`element.querySelector('.rdot')`) 으로 확인한다. 반대로 그리드·색상·실루엣 같은 큰 덩어리는
  비전 판독이 잘 맞는다.
- IAB 탭이 수 분 뒤 타이틀 화면으로 리로드된 사례가 있었다(원인 미상) — 세이브는 localStorage
  에 살아있어 재진입(START→게스트→서버→예)하면 이어진다.
- **튜토리얼 중 화면 접근법(2026-09-12 실측)**: 인트로 대사(npc-layer ▶)·미션 보상 모달·
  .ob-prog 박스가 클릭을 번갈아 가로막는다 — 'npc-layer ▶ 진행'과 '모달 확인 버튼'을
  둘 다 꺼질 때까지 번갈아 처리하는 정리 루프를 먼저 돌리고, 그다음 목적 화면을 연다.

---

## 5. 벤치마크 대조 — 사실 판정의 규칙

`docs/reference/기능별/` 의 **스크린샷 390장이 1차 근거**다. 기획서보다 우선한다.

지금까지 세 차례 전수 대조를 했다:

| 라운드 | 기준 | 결과 |
|---|---|---|
| 1차 | 화면에 **적힌 개수** | 6건 수정 (몬스터 8→120종, 랭킹 30→40행 등) |
| 2차 | UI **배치·골격** | 구조 5건 완료, 배치·문구 약 33건 미착수 |
| 3차 | **기능·시스템 유무** | 화면 누락 없음 확인. 대신 **연결이 끊긴 축** 다수 발견·수정 |

**교훈 하나**: "우리가 스펙에 적은 값"과 "우리 코드"를 대조하는 건 자기 답안지끼리 채점하는 것이다. 반드시 **원작 스크린샷에 적힌 숫자**와 대조해야 한다. 실제로 "탭 4개"라는 구조만 기록하고 헤더에 `30마리`라고 쓰여 있는 걸 안 세서 몬스터가 등급당 2마리로 남아 있었다.

**교훈 둘**: 근거가 없는 것을 "없다"의 근거로 삼지 말 것. 월드보스 랭킹 보상표를 "원작에 없음 확정"이라고 결론냈는데, 알고 보니 그 화면의 `[i]` 버튼을 누른 캡처가 한 장도 없었을 뿐이었다. 나중에 촬영해보니 9행짜리 표가 그대로 있었다.

**교훈 셋 (법무)**: 원작 캐릭터명을 1:1 기계 치환해 새 이름을 만들었더니, 원작 IP는 피했지만 **다른 회사 게임 캐릭터명과 충돌하는 이름**이 나왔다(Dota·LoL·캐슬바니아 등 12건). 치환명은 반드시 타사 IP 대조를 거쳐야 한다.

---

## 6. 남은 작업

우선순위 순. **2026-08-07 전수 재점검 결과를 반영했다** — 아래 두 항목은 과거 버전에서 "남은 작업"으로
적혀 있었으나 실제로는 이미 완료됐거나 전제가 틀렸음이 확인됐다.

### 2026-09-19 갱신 — 대기 1·2번 사전 실물 검증 완료 (게임 무변경 · 결함 0)

**방법**: Playwright `page.clock.install` 로 Date를 미래에 고정한 뒤 실제 부팅(클릭 시퀀스
#btnStart→#lmGuest→#srvEnter→#scYes 로 홈 진입)해, 롤오버 토스트·배지·세이브 반영·모달
문구를 실물 DOM에서 검증 — 세이브는 game.js 의 freshState/freshMats/MATS 를 vm 으로 추출
합성(weekly 키 W38·claimed.w1, monthly '2026-9'·claimed.forgeTrial=true 심어 리셋 판정).
스크립트는 `docs/qa/20260919-미래시각-롤오버-실물QA.cjs`(내부 보관, .gitignore).

- **9/21(월) 축제 교체 ✓** — 성장→채굴. 롤오버 토스트 1회('⛏️ 이번 주는 채굴 축제입니다'),
  배지 '채굴 축제 D-7', weekly 키 W39 갱신·claimed 리셋·base 현재 stats 재스냅샷,
  축제 의뢰 '[채굴 축제] 장비 25회 제작' 전환 — 7검사 전부 통과.
- **10/1(목) 이중 롤오버 ✓** — 주(W39→W40 골드 축제·배지 D-4·토스트) + 월(monthly 키
  '2026-10' 갱신·claimed.forgeTrial 소멸·용광로 '도전' 버튼 부활·'다음 달 1일 초기화'
  문구) — 8검사 전부 통과. 현재 주(W38) 유지 시나리오도 통과(배지 성장 D-2·용광로
  소진 'D-12'·리셋 없음). 페이지 에러 3부팅 모두 0건.
- **당일 실측 완료(2026-09-21 00:02)**: 실제 자정 경과 시각에 W38 세이브 주입 부팅(로컬 동일 커밋+
  라이브 Pages 양쪽) — 채굴 축제 롤오버 토스트 1회 · 배지 D-7 · weekly W39 · 페이지 에러 0건.
  스크립트 docs/qa/20260921-축제교체-당일실측.cjs. 완전 종결.

1. ~~9/21(월) 축제 교체 실물 목격~~ — **위 사전 검증으로 완료(2026-09-19)**.
2. ~~10/1(화) 용광로 입장권 월 리셋 실물 목격~~ — **위 사전 검증으로 완료(2026-09-19)**.
3. **영웅 추가 재개 (에셋 확보 후)** — sheets/가 9영웅 1:1이라 신규 영웅 불가. 대표에게
   요청할 사양: 직업별 스프라이트시트 1세트(동작 9~10)+초상 1장. 에셋 도착 시 침습 범위
   (곡선·상점·합성·투기장 매칭·시뮬 소환 정책 전수 재측정)를 감안해 별도 설계 루프로.
4. (보류 확정) 강화석→기록서 교환소 — 기록서 병목은 '탑을 돌라'는 유도 장치, 잔여는
   종반 자연 상태. 근거는 2026-09-18 루프 기록(4장).

### 2026-09-11 추가 — 도감 수집 축·제작 UX 정비 (v5.145~v5.153)

같은 날 자율주행 루프 9커밋. 다음 이어받는 사람이 알아야 할 구조 변화만 적는다(상세는 각 커밋 메시지).

- **도감(코덱스)이 '수집 기록' 화면이 됐다.** 몬스터 탭은 `S.codexKills`/`S.codexBossKills`
  (freshState 소유, 홈 사냥 onKill 집계, 보상 없음 — 경제 영향 0)로 처치 수를, 영웅 탭은 9영웅
  초상화 로스터로, 세트 탭은 착용 진행도+구성품 아이콘으로. 카드를 탭하면 각 행동 화면으로 간다.
  도감용 `MONSTERS` 이름 배열은 제거됐다(도감이 HUNT_TIERS 를 직접 읽음).
- **제작 시작 본체가 대장간 모달 클로저 밖의 전역 `craftStart(grade, cat, item)` 로 이동**했다.
  결과 팝업 [다시 제작]·모달 [제작] 이 같은 경로를 쓴다. 대장간을 고칠 때 이 함수가 정본이다.
- 인벤토리 미장착 장비 목록: 등급→강화→최신순 정렬, 표시 상한 60. (종전 무정렬 16개 — 신규
  상위 장비가 목록에 안 나오던 결함)
- 도감 집계 회귀 검사가 smoke 에 있다(`몬스터 도감 집계` — Battle.stepFrame 으로 홈 전투 64초
  실측). **idleTick 은 골드 루프일 뿐 전투를 돌리지 않는다** — 킬 관련 검사를 만들 때 이 사실을
  기억하라(2026-09-11 실측, 첫 버전 테스트가 이 전제로 실패했다).

### 2026-09-12 추가 — 연속 체인 루프 (v5.162~v5.181)

작업 종료 후 1분 대기를 두고 이어진 자율 체인 20커밋. **스모크가 크게 강화됐다 — 게이트가
지키는 것이 무엇인지 아는 것이 이 저장소를 다루는 데 중요해졌다:**

- **데이터 정합 불변식(신규)**: 세트 구성품 전량 제작 가능(획득 불가 세트 차단) · 길잡이 목표
  제작 가능 · 사냥터 CP 곡선 단조성 · 재료 보유 상한(2000/900 비파괴 클램프) · 소환 피티
  확률식 경계값 · 제작시간 배율(버프 0.5배) · 장비 정렬 정본(sortEquipList) · 배지 판정 전이.
- **표시↔판정 불일치 4건 수정** (이 유형의 스캔법: 화면 문구·바를 코드 정본과 대조한다):
  일일 미션 '매일 리셋'(실제 누적 스탯이었다) · 그래픽 품질(위약) · 보유 상한(위약) ·
  제작시간 버프 미반영. 가짜 바 2종도 실제 값으로(길드레이드 62% 하드코딩, 정산 kills%100).
- **알아둘 것**: `matGain` 은 재료명이 틀리면 조용히 no-op 이다 — 리터럴 전수 감사는 smoke 의
  세트/길잡이 불변식과 함께 유지된다. 게임 밸런스 수치를 바꿀 때 CP 단조성·드랍 커버리지
  게이트가 같이 돈다.
- 남은 큰 과제는 여전히 **설계 결제 필요**(세트 밸런스 시뮬 통합)와 **원작 캡처 필요**
  (비전미확인)뿐 — 자율 안전 범위의 개선은 이 시점에서 소진됐다.

### 2026-09-12 추가② — 밸런스 시뮬레이터와 곡선 확정 (v5.182~v5.189)

대표 방침("캡처 없는 항목은 자체 설계로 해결, 크리티컬만 결재") 아래 돈 밸런스 축을
데이터로 다룬 8커밋. **다음 인계자는 이것부터 알아야 한다:**

- **`balance-sim.mjs` (npm run sim [시간] [casual] [seed=N])** — 실제 game.js 를 vm 위에서 구동해 정본 함수
  (heroPower[세트 포함]·craftStart·onKill·heroFuse·summonRun)만으로 장기 곡선을 측정한다.
  인자는 위치 무관: `casual`=일 8h 세그먼트 · `seed=N`=시드 분산 측정(기본 42, 0 금지 — v5.282).
  별도 모델링 금지 — 공식을 다시 적으면 시뮬과 게임이 어긋난다(과거 재시뮬 사고 참조).
  가상 플레이어: 저축형 구매(부위×등급 최고 업그레이드, 못 사면 저축) · 수요 기반 사냥
  회전 · 일일 콘텐츠(골드던전 3·요일던전 5·탑 소탕) · 소환서 골드 구매(상점 15M/10장) ·
  안전 강화 +10. 시드 42 고정.
- **확정 곡선(강화 포함)**: N 0.5h → R 14h → E 66h(≈day3) → L 139h(≈day6) → 600h
  CP 102,011 · 벽 없음(정체 0회). 등급 지표는 **리더 전투력 기준**(v5.185 사냥 게이트와 정합).
  > ⚠ **v5.228 갱신 — 위 수치는 '세트 없는 베이스라인'이었다.** 당시 장착 교체가 스탯 표시용
  > part(투구·상의·하의 → 방어구 1부위) 기준이라 세트가 구조적으로 완성될 수 없었고(setm ×1.000
  > 600h 고정), 시뮬의 그리디 제작도 세트를 스스로 맞추지 않았다. 10슬롯 복원(v5.228) + 시뮬 세트
  > 지향 tie-break(같은 부위·등급이면 입은 세트 조각 수가 많은 쪽) 이후: **N 0.5h → R 12h →
  > E 62.5h → L 115h → 600h CP 309,810 · 잔불 6세트 @1h(×1.3) → 작열 6세트 @219.5h(×2.286)**.
  > 등급 게이트 속도는 거의 그대로(세트는 중후반 증폭 축), 최종 전투력은 2.4배. 제작 확률이
  > 비시드 RNG라 실행마다 ±10% 흔들린다 — 곡선 비교는 평균 감각으로.
  > ★ v5.229 추가: 위험 강화(+11~20) 축도 시뮬에 통합(상시 보호·L장비 한정 정책).
  > 개시 약 166h(L 10부위 +10 직후) · 600h 평균 강화 +15.9~17.0(+20 도달은 600h 넘어서) ·
  > 망치 구매 1.1~1.2B 골드 · 강화석 잉여 1,100 → 320개(엔드게임 소비처 실증).
  > ★ v5.230 추가: 심화 각성(13~30) 축도 시뮬에 통합 — 탑 도전·소탕·상자→기록서 교환을
  > 정본 공식으로(웨이브 ×1.18 지수 상승, base=600+최고×450). 600h 측정: 탑 최고 20~21Wave ·
  > 각성 +22(기록서 30권 소비, 30단계 완성은 600h 넘어 — 한 달+ 장기축 실증) ·
  > 최종 CP 415k~478k(v5.229 대비 +30%+, 각성 +15%p·탑 일일 골드·강화석 유입 포함).
- **곡선 수정 3건**: v5.184 영웅 합성 레벨 100% 승계 + 처치 XP 티어 비례(N1·R2·E4·L8) ·
  v5.185 전멸 트랩(위험 판정을 홈 출격 영웅 기준으로) · v5.186 벤치 영웅 XP 50% 분배.
- **종결된 것(재검토 금지)**: E/L 영웅은 게임에 없음(9종=N5+R4, HERO_SHARD_NEED.E/L은
  미래용) · +11~20 위험 강화는 시뮬 실측 기대값(부위당 약 3.2억 골드 — v5.229 시뮬 통합으로
  갱신, 구 479M은 '실패마다 망치 소모' 오산) 10부위 ≈ 32억 ≈ 한 달 그라인드로 적절한
  엔드게임 싱크 · 전체 곡선 속도 조정 불필요.
  ⚠ v5.373 정정: 3.2억은 +20 미도달 부위까지 평균에 넣은 표본 편향이었다. 해석해·몬테카를로 20만 회로 +10→+20 부위당 평균
  **약 4.76억**(시도 36.6회·전설 망치 81.5개, 중앙 4.0억·2억~9억) · 10부위 약 47억. 결론(적절한 엔드게임 싱크)은 유지.
- **분해 축**: v5.187 개별 분해(환급 = 제작가 50%+강화 5%p/단, **상한 90% — 상한 없으면
  +11+에서 골드 순환 이익 발생**, smoke 가 배포 전에 잡음) · v5.188 등급별 일괄분해 ·
  v5.189 벤치 10레벨 마일스톤 로그.
- **시뮬 함정 4개(실측)**: ① 정체 판정기는 '창 시작 시점 CP'와 비교해야 한다 — 이벤트
  push 후 비교하면 항상 무성장 오판(62b032c). ② 홈 전투는 리더 1명 — 총전투력으로 안전
  판정하면 전멸 트랩. ③ **게임의 `dailyUse`/`rollDaily`를 시뮬에서 쓰면 시뮬 전체에서
  1회만 실행된다** — rollDaily는 '실제 날짜' 변경 시 리셋되는데 시뮬은 몇 초 만에 여러
  시뮬-일을 지나므로(v5.230 실측: 탑 도전·소탕이 600h 내내 1회씩만 돌았다). 일일 횟수는
  dailyStep 로컬 카운터로 셀 것(골드던전 left=3 방식). ④ 소환서 구매 정책이 로스터
  9/9에서 멈추면 각성이 조각 기아에 걸린다 — 조각 소비처는 합성만이 아니라 기본 각성
  (12단계까지 약 4,700개)이므로 '각성<12'까지 계속 산다(v5.230 실측: 600h 각성 +0 사고).

### 2026-09-13 추가 — 재미·가이드·리텐션 축 (v5.190~v5.204)

밸런스 곡선 확정 후 이어진 15커밋. 인계자가 알아야 할 것만:

- **리텐션 축 완성**: 오프라인 수익을 인게임의 50%(정본 OFFLINE_GPM, 종전 5.3%)로(v5.196) ·
  연속 접속 보상 7일 주기(v5.201, LOGIN_REWARDS — 양수=주사위·음수=기타 기호 규약,
  강화석=-4) + 퀘스트 탭 미리보기(v5.202).
- **수집·세트 가시화**: 등급 도감 완성 1회성 보상(v5.195, S.codexReward 플래그 —
  1회성·기존 재화·소비처 확인 3원칙) · 착용창 활성 세트 칩(v5.203, 1칸 남으면 금색◀).
- **정합 게이트 2종 추가(smoke)**: 도움말 주장 ↔ 코드 정본(v5.199 — DEEP_CAP·PROTECT_COST·
  MAT_CAP·환급 0.9 자동 대조) · 이 외에 일일미션(v5.161)·보유상한(v5.171) 등 기존 게이트 유지.
- **가이드 실측화**: 도움말 허구 스탯 제거·각성 상한 정정(v5.197/198) · 공략을 시뮬 확정
  곡선 기준으로 재작성(v5.204 — 공략이 '시뮬 데이터의 이용자 표시'가 됨).
- **기타**: 분해 개별(v5.187·환급 90% 상한)·일괄(v5.188) · 레전더리 제작 성공 연출(v5.190) ·
  마릿수 프리셋 칩(v5.191) · 죽은 CSS 17종 제거(v5.192) · 벤치 10레벨 마일스톤 로그(v5.189) ·
  음량 슬라이더(v5.200) · 각성 심화 상한 30(v5.193) + 남은 기록서 총량 표시(v5.194, 13~30=99권).
- **환경**: 브라우저 웹뷰가 가끔 미부착("guest not attached")이 된다 — 패널을 닫았다 열면
  복구. 그동안은 정적 교차검증(배포 코드 grep)으로 QA 대체했다(실측 프로세스).

### 2026-09-13 추가② — 플레이버→실전 정합 시리즈 완결 (v5.205~v5.215)

아이템 안내 문구(itemFlavor)와 실제 전투 동작의 정합을 6축 전부 해소한 11커밋:

| 축 | 아이템 | 안내 | 구현 | 상한 |
|---|---|---|---|---|
| 계정 스탯 | 고서 | 보유 시 계정 스탯 상승 | v5.210 +3%/종 | 3종 최대 +9% |
| 칭호 조건 | 곡괭이 | 채굴 칭호 획득 조건 | v5.211 제작 경로 연결 | — |
| 회복(자연) | 물약 | 전투 중 자동 소모 회복 | v5.212 +100%/종 자연회복 | 2종 최대 3배 |
| 회복(주기) | 방패 | 17초마다 20% 회복 | v5.213 HP 20%p/17s | 착용자 |
| 이동 속도 | 신발·목걸이 | 공속·이속 상승 | v5.214 이속 배율 | 최대 +1.9배 |
| 치명타 | 반지류 | 치확·치공 상승 | v5.215 확률+배율 | +8%p / +0.8 |

**유보: 공격 속도(aspd)** — 공격 주기(0.7~1.1s 시드 난수)를 건드리면 결정론 해시가
전부 바뀌고 DPS 곡선이 흔들린다(v5.214 판단 근거). 무기·방어구의 matk/def/mdef는
CP 파이프라인(heroPower→partyCP→dmg/cp 정규화)으로 이미 간접 반영 — 별도 구현 불필요.

**아키텍처 원칙 확립**: 전투 스탯 가산은 전부 **순수 장비 상태 함수**(같은 장비=같은 값)
로 작성 → 결정론(D1~D5) 자동 유지. D1~D5는 자기일관성 검사(같은 시드 N회 동일 해시)
라 기준 해시 갱신이 필요 없다.

### 2026-09-13 추가③ — 가시성·연출·경제 게이트 축 (v5.216~v5.222)

스킬 해금 시도(v5.216)·데이터 무결성 감사를 거쳐 정보 가시화와 경제 보호에 집중한 7커밋:

- **결정론 우선 원칙 확립 (v5.216)**: 스킬 등급 해금이 '선언뿐'이었으나, 구현 시도 두 방식
  모두 D1 자기일관성을 깨뜨려(rngDrawCount 524↔522) 되돌림 — 표기를 실제 동작에 맞췄다.
  결정론(전투 재현성)이 표현적 정합보다 우선한다는 판단 기준을 주석에 영구 기록.
- **경제 정본 게이트 (v5.218)**: 합성 확률(N→R 50%·R→E 0.8%·E→L 0.08%)과 비용(30/500)을
  G-31 실측값으로 smoke 에 고정 — 미션 규칙('실측 수치 함부로 변경 금지')을 코드로 강제.
- **정보 가시화**: 버프 화면 15종(고서·물약·각성·세트 포함, v5.217) · 전투력 구성 팩터별
  기여도(v5.220) · 강화 위험 구간·기대 손실(v5.222) · 업적 10축(v5.219: 합성·각성·분해·레전더리).
- **연출**: 영웅 합성 성공 시 승계 레벨 즉시 표시 + legendary 음(v5.221).

### 2026-09-13 추가④ — 안전장치·신뢰·세트 해방·강화·심화 각성 축 (v5.223~v5.230)

플레이어의 실수·불신을 줄이고, 대표 위임 과제(세트 시뮬 통합)를 해결한 6커밋:

- **설정 마지막 저장 시각 표시 (v5.223)**: '내 진행도가 저장되고 있나?' 불안 해소.
- **오프라인 골드 배율 표시 (v5.224)**: 정산 화면에 '인게임의 50% · 8h 상한' 명시 — 숨은 규칙 공개.
- **전멸 위험 확인창 (v5.225·v5.227)**: 권장 전투력보다 약한 몬스터 선택 시 styledConfirm.
  파밍 팝업(v5.225)에 이어 몬스터 선택 화면(v5.227)까지 — 안전한 선택은 즉시 실행 유지.
- **미사용 칭호 효과 폐지 (v5.226)**: '채굴 피해 +100%' 선언만 있고 동작 없던 것 제거(플레이버→실전 정합의 마무리).
- **세트 해방 — 10슬롯 복원 + 시뮬 통합 (v5.228, ★대표 위임)**: 장착 교체가 스탯 표시용 part 를 써서
  세트 대부분이 6세트 불가였던 구조 결함을 `slotKeyOf`(페이퍼돌 10슬롯 정본)로 복원(상세 3-6a).
  장착 본체 `equipItem()` 전역 추출 + 스모크 회귀(동시 착용·무기 교체·세트 6도달·슬롯 분산 invariant).
  시뮬에 세트 계측·세트 지향 제작 tie-break 추가 — 곡선 갱신은 추가② 참고.
- **위험 강화(+11~20) 축 시뮬 통합 (v5.229, 남은 과제 ② 해소)**: 시뮬에 상시 보호·L장비 한정 정책
  추가(망치는 상점 정본가 구매). 발견 2건 — ① 구 몬테카를로 479M/부위는 '실패마다 망치 소모' 오산,
  실측 약 3.2억(파괴 분기 50%에서만 소모 — ⚠ v5.373: 표본 편향, 실제 약 4.76억) → 공략 문구 정정 ② 소환서 무한 구매 정책이 엔드게임
  골드를 조각(소비처 없음)으로 바꾸고 있었다 → 로스터 9/9 완성 후 구매 중단으로 수정.
- **심화 각성(13~30) 축 시뮬 통합 (v5.230)**: 탑 도전·소탕·상자→기록서 교환·심화 각성 비용을
  전부 정본 공식으로 시뮬에 반영. 이 과정에서 시뮬 결함 2건 발견(함정 ③④ — dailyUse 1회
  실행, 소환서 조각 기아). 게임 코드 변경 없음 — 측정 결과 600h 각성 +22·CP 415k~478k,
  엔드게임 3축(세트·위험 강화·심화 각성)이 병렬로 600h을 넘기는 장기 구조 실증.

### ~~곧바로 할 수 있는 것~~ (둘 다 종결)

- **~~밸런스 재시뮬~~** — ⚠ **전제 오류로 종결**. "세트효과 반영(v5.6~5.7)으로 후반 전투력이 높게 나온다"고
  적혀 있었으나, 시뮬 스크립트(`20260724-…-2라운드.py`)의 `hero_cp()`(전투력 공식)는 **세트효과를 전혀
  모델링하지 않는다**. `[J]`섹션은 세트효과를 독립 DPS 시뮬로만 다룬다. 따라서 코드의 세트효과 변경이
  시뮬 결과에 영향을 줄 리 없고 — 실제로 2026-08-07 재실행 결과 **이전과 완전히 동일**(시드 고정).
  *재실행 자체는 의미 없음.* 진짜 과제가 있다면 `hero_cp()`에 세트효과 기여를 추가하는 것이나,
  이는 밸런스 설계 결정이 필요하다(아래 "판단 필요"로 이동).
- **~~UI 배치·문구 약 33건~~** — ⚠ **구식 정보, 이미 완료**. v4.7~v5.3 커밋들이 갭스펙 P2(31건)+P3(21건)
  합 52건을 이미 전부 반영했다. 2026-08-07 전수 대조에서 미반영 0건 확인. 예시로 거론된
  `즉시완성→즉시완료` 문구도 사실이 아님(원작은 "즉시 완성" 띄어쓰기 — 가장 최근 커밋 `4232341`에서 정정 완료).

### 판단이 필요한 것

- **~~세트효과의 밸런스 시뮬 통합~~** — ✅ **2026-09-13 종결(v5.228, 대표 위임 과제).** 구 python 시뮬의
  `hero_cp()` 이야기는 낡았다 — 현행 `balance-sim.mjs` 는 정본 `heroPower`(setm 포함)을 쓰고 있었다.
  문제는 다른 곳에 있었다: ① 장착 교체가 스탯 표시용 part 기준이라(3-6a 함정) 세트가 구조적으로
  완성 불가 — 시뮬 600h 내내 setm ×1.000; ② 시뮬 가상 플레이어의 그리디 제작(가격순)이 세트를
  스스로 맞추지 않음. 해결: 게임은 10슬롯 복원(`slotKeyOf`), 시뮬은 세트 지향 tie-break 추가.
  새 확정 곡선은 추가② 섹션 참고(600h CP 309,810 · 작열 6세트 @219.5h). 환산 방식 논쟁(dmg·def
  가중 그대로 쓸지)도 무의미해졌다 — 정본 `setDamageMul` 그대로가 답이고, 그것이 이미 heroPower 에 곱해진다.
- **~~아트 방향~~** — ⚠ **해소됨(2026-08-14 확인)**. "에셋 0개, 아이콘 전부 이모지"는 낡은 정보다.
  지금 `assets/` 에 **928개 파일**(아이콘·몬스터·영웅 스프라이트·이펙트·배경)이 있고,
  v5.82 장비 아이콘 교체 → v5.109 아이콘 팩 교체·매핑 파이프라인(`npm run icons`)까지 끝났다.
  코드의 이모지는 `eImg()` 64곳이 PNG 로 치환해 그린다(이모지는 매핑 키로만 남아 있다).
- **세트 구성 아이템** — 9종 세트의 구성품은 **원작 대조가 아니라 자체 설계**다(원작 도감은 아이콘만 있고
  이름이 안 뜬다). `SET_PIECES` 표만 갈아끼우면 언제든 교체 가능.
- **~~대장간 재료 부족 → 몬스터 소환 연결~~** — ⚠ **이미 구현됨**. v5.59/v5.61 에서 재료 칩 클릭 →
  파밍 팝업 → [사냥]으로 붙었고, v5.111 에서 정확도를 고쳤다(아래).
- **~~몬스터 소환권(`tickMon`/`tickMonP`) 소비처 0~~** — ✅ **2026-09-10 종결(폐지 + 환불).**
  회색코인 80/300 을 받고 팔면서 코드 전체에 차감하는 곳이 0 이던 **함정 상품**이었다.
  회색코인은 전투로 벌리지 않고 길드 레이드·약탈·기여로만 들어오는 느린 재화라 피해가 컸다.

  종전 선택지 ①(소비처를 오리지널로 설계)은 **자리가 없다는 것이 수치로 확인돼** 접었다.

  | 넣으려던 자리 | 왜 안 되는가 |
  |---|---|
  | 보스 소환 재료 대체 | R보스 재료 42개 = 회색 약 840 상당인데 소환권 3장은 80. 20배 이상 싸서 재료 소모처가 무너진다 |
  | 재료 지급권 | 회색상점이 이미 재료를 직접 판다(R 20 · E 35 · L 90). 소환권 장당 원가 약 27 이라 'R재료 1개(20)' 보다 비싸 살 이유가 없다 |
  | 말 그대로 '몬스터 소환' | 이 게임에서 그 행동은 [몬스터] 화면의 사냥터 선택이고 **무료다**(코드 문구도 "몬스터 소환 →"). 코어 루프라 유료화 불가 |

  → 이름만 있고 설계가 없던 재화로 판정하고 **회수**했다. 회색상점 2줄·탑 상자 교환 1줄·소환 화면
  카운터 2칸을 내렸고, 보유분은 `migrateMonTickets()` 가 **구매가로 환불**한다(장당 26 / 300).
  탑 상자 교환은 빈자리에 '재료 소환권+'(실제 소비처가 있는 재화)를 넣어 3종을 유지했다.
  회귀 검사 2건을 `smoke-test.mjs` 에 추가했다 — 환불 이관 정확성 + **판매·지급 경로 부활 감시**.

  > 되살리려면 **소비처 설계가 먼저**다. 판매부터 붙이면 같은 함정이 다시 생긴다.

### 추가 촬영이 필요한 것 (근거 부재)

코드에 `⚠비전미확인` 주석(24곳)으로 표시해뒀다. 원작을 실행해 캡처해야 확정된다.
**전체 목록은 `docs/design/20260807-비전미확인-촬영대기목록.md` 에 체계화돼 있다.** 아래는 대표 항목.

- 영웅별 강화의 "4강화당 N회"에서 **N값**, 강화 상한, 단계별 요구량 스케일업
- 투기장 티어별 임계 점수, 개인랭크 버프 단계별 %
- 장비 옵션 재설정(주사위)의 옵션 스탯 풀·등급 추첨 확률표
- 절전 화면 카운트다운 0 도달 동작, 12칸 중 6칸 재화 정체
- 칭호 '초급자'/'절대자' 획득 조건 원문

---

## 7. 문서 지도

| 파일 | 내용 |
|---|---|
| `docs/design/20260727-화신-원작대조-갭스펙.md` | **가장 중요.** 원작 대조 갭 136건 + 구현 이력 + 미결 항목 |
| `docs/design/20260722-화신-통합기획서.md` | 시스템 전반 |
| `docs/design/20260725-화신-UI재현카탈로그.md` | 화면별 재현 스펙 (대용량) |
| `docs/design/20260725-화신-원작플로우-전량판독.md` | 원작 전량 판독 |
| `docs/design/20260722-화신-대표결정-변경기록.md` | 방향이 바뀐 결정들의 이력 |
| `docs/reference/화면지도.md` | 원작 화면 지도 |

**git 로그도 문서다.** 커밋 메시지에 "왜 그렇게 고쳤는지"와 근거 스크린샷 파일명을 남겨뒀다. `git log --oneline` 으로 65개 커밋의 흐름을 훑으면 프로젝트 맥락이 잡힌다.

```bash
git log --format='%h %ad %s' --date=short
```

---

## 8. 이 저장소의 git 상태

- 원본(`20260722_Hwasin/repo_Hwasin`)에서 **전체 히스토리를 그대로 복사**해 출발했다.
- **원격은 `origin` = https://github.com/ggonggee/hwasin-play.git 이다.**
  `main` 에 푸시하면 GitHub Pages 라이브에 그대로 나간다 — **푸시 = 배포**다.

  > ⚠ 2026-09-10 정정: 이 자리에는 오래도록 "원격(remote)은 제거했다" 라고 적혀 있었다.
  > v5.111 시점의 저장소 일원화(커밋 `c177207`)로 원격이 다시 붙었는데 문서만 옛 상태로 남았던 것이다.
  > README 는 "푸시 = 배포" 라고 적고 HANDOFF 는 "원격 없음" 이라고 적은 모순이 3주 넘게 있었다.

- 클론한 직후에는 **`npm run setup` 을 한 번 돌려라.** push 전 검사 훅(`.githooks/pre-push`)이 설치된다.
  `core.hooksPath` 는 저장소 로컬 설정이라 클론마다 새로 잡아야 한다.

### 2026-09-13 v5.242 — 밸런스 시뮬 재현성 수리 (3원인) — 같은 시드 3회 실행 완전 동일

시드 42를 고정했는데도 실행마다 궤적이 갈라졌다(400h CP 279,351 / 288,211 / 294,327 …).
**전후 비교의 신뢰 기반이 흔들리는 문제라 우선 수리했다.** 원인은 3개였다:

1. **요일 던전 인덱스가 실제 실행 요일**(`new Date().getDay()`) — 월요일에 돌면 월요일
   던전만 400h 내내 돈다. → 시뮬 시간 기반 `floor(simSec/86400)%7` (24h마다 순환 —
   게임 규칙도 더 충실).
2. **vm에 진짜 Date 주입** — 게임의 `Date.now()`/`new Date()`(오프라인 정산·제작 만료 등)이
   실행 시각을 봤다. → `FakeDate`(Date 상속, now/생성자만 시뮬 시계 SIM_NOW=창 시각) 주입.
3. **난수가 전부 결정적이지 않았다** — 전투는 `Battle.setSeed(42)`(전투 전용 _battleRng)인데
   ① vm 전역 `Math.random`(onKill 보상 드랍·제작 성공 p0·합성)과 ② 시뮬 정책 코드
   (위험강화·안전강화·합성 확률 4곳)이 진짜 난수였다. → vm에는 시드 xorshift 교체
   (Math는 `Object.create(Math)` 복사본을 주고 shadowing — Node 전역 무오염),
   시뮬에는 `srand()` 교체. 전투·전역·시뮬 세 스트림 모두 결정적.

**검증: 400h 3회 연속 실행 → CP 278,554·상위 재료 목록까지 완전 동일.**
시뮬로 전후 비교할 땐 이제 단일 실행으로 확정된다(다만 코드를 고치면 시드 시퀀스가
어긋나 궤적이 바뀐다 — 수치 비교는 같은 코드 버전 안에서 할 것).
동반: 시뮬 창 상한 12800(6400h 측정) — '각성의 결정'(v5.241)의 장기 적립 확인용.

### 2026-09-13 실브라우저 QA 7차 — v5.238~243 누적 기능 실물 검증 (결함 0건)

시뮬 중심 6루프(v5.238~243)가 쌓아둔 미확인 항목 4개를 세이브 주입(가져오기 경로,
각성 50·결정 0·기록서 20·강화석 500·+20 L단검)으로 전수 확인했다:

- **각성의 결정(✦)**: 50 화면 ✦0·복합 비용 문구 "기록서 20권 + 강화석 300개(보유 …)"
  → 실행 → **✦1·스탯 75.0→75.5%·기록서 20→0·강화석 500→200·다음 비용 25권+340개**
  (공식 20+5n·300+40n 정확)·토스트 정상.
- **기록서 골드 상품**: 골드 탭 "영웅 기록서 X1 · 골드 3000만" 렌더 → 구매 →
  골드 3천만 차감·기록서 +1·토스트 "영웅 기록서 X1 획득".
- **극한 강화석 20**: +20 강화 화면 "골드 2000만 · 강화석 20"·⚗ 문구·구간 표기.
- **전종 완성 보상**: codexAllReward 직접 호출 — 토스트 전문 렌더·첫 호출 true·
  재호출 false(1회성). 지급 수치 정밀 검증은 smoke가 담당.

**노하우(자동화 함정 2건 — 다음 검증자는 같은 데 시간 쓰지 말 것):**
1. **tab.evaluate는 단일 표현식만 소화한다.** `openModal('x'); 'ok'` 같은 세미콜론
   2문장은 evaluate 파이핑에서 "Unexpected token ';'"로 실패하고, 이 예외가 게임
   window.onerror → #errbar에까지 기록돼 '게임 결함'으로 오인한다. **다중 문장은
   반드시 (()=>{ … })() IIFE 한 덩어리로.** (errbar 에러 카운트가 늘어 있으면 먼저
   내 스크립트 실패 잔상인지 의심할 것)
2. **브라우저 탭이 여럿이면 tabs.at(-1)이 셀마다 다른 탭을 가리킬 수 있다** — 매 셀
   새 커널에서 url 필터 후 보증(개수·home 표시)하고 id를 재획득할 것.
3. (재확인) 상품 구매의 세이브 반영은 최대 수 초 지연 — 인메모리 판정은 클릭 직후
   토스트(1.9초 소멸)로, 세이브 검증은 3초 후 스냅샷으로.

### 2026-09-13 QA 8차 — 신규 유저 온보딩 실측 (인트로→튜토리얼 STEP3, 결함 0건)

엔드게임 경제 정비(7루프) 뒤 미션의 본질로 돌아와 **신규 유저 첫 플레이**를 실제로 밟았다:

- **인트로**: 대사 3장(리안, #npc-layer 클릭 진행) → 보상 팝업 3칸(투기장 랭크 골드 50만·
  출석 소환권·첫 방치 골드 72만) 수령 → 자원 사전 지급 정상.
- **튜토리얼 STEP2(제작)**: 대장간 진입 → [제작]이 아이템 상세 팝업(G-24)을 열고
  그 안의 [제작 시작]으로 시작 — **2단계 플로우는 실측 설계**. 30초 제작 → 자동 완료
  (7859행 gameLoop 폴링) → 아이템 지급 → STEP3(장착) 자동 전환.
- **STEP 차단**: STEP2~9 중 목표 외 클릭 차단이 인벤토리도 막는다 — 설계 동작
  (튜토리얼 박스가 목표 화면으로 유도). 유저는 혼란 없이 영웅 탭으로 안내받는다.
- **관찰(결함 아님)**: 제작 완료 후 대장간 모달 하단 '남은 시간 00:01' 텍스트가 재렌더
  전까지 잔존 — 결과 팝업이 뜨는 관문이라 실 UX 문제 아님.

**노하우(QA 세이브 초기화)**: localStorage.removeItem도 unload save로 부활한다 —
값 수정과 마찬가지로 삭제조차 무효화된다(실측: 신규 세이브로 시작했는데 kills 106,448
옛 세이브가 부활). **초기화는 반드시 `_saveSealed=true` 동반**(게임의 [데이터 초기화]
버튼 로직 7114행 참조) 또는 그 로직을 그대로 evaluate로. 인트로 팝업 확인 시 modalOn이
아니라 #npc-layer(대사)부터 봐야 한다(1차 관찰 오판 방지). cua 좌표 클릭이 탭 노화로
불응하면 버튼 DOM .click()으로 대체 검증.

### 2026-09-13 QA 8차 보강 — 튜토리얼 STEP3~7 실물 완주 (결함 0건)

8차 기록에 이어 같은 신규 세이브로 STEP7까지 실제 플레이로 밟았다:

- **STEP3(장착)**: 영웅 상세(heroDetail)엔 장비 버튼이 없다 — 착용창 진입은 로스터
  카드의 **초상(eq) 클릭**(game.js 5083) 또는 홈 '🛡️ 장비 착용창' 버튼. 착용창 하단
  보유 장비 카드 → 상세 → [장착] → 확인창 [장착](showConfirmDialog, 7655) → 완료.
- **STEP4(강화)**: 착용창 라운드 ⚒버튼 → 강화 1회 → 즉시 완료.
- **STEP5(영웅 소환)**: 소환 타일 → **소환도 확인창 2단계**(mkTile, 5484) → 조각 +20.
- **STEP6(재료 소환)**: '재료 소환|재료권 1' 타일 → 동일 2단계 → 완료.
- **STEP7(합성)**: 합성 버튼은 직업 공용 조각 기준 활성/비활성 — 불(flame 144≥80)
  계열 '불꽃검 라비스' 합성 성공("Lv3 승계" 토스트 — v5.184 승계 규칙 실물 확인).
- **관찰**: 핵심 액션(제작·소환·장착)이 전부 '상세 팝업/확인창 → 실행' 2단계로
  일관 — 실측 설계. STEP8(편성·투기장)·9(완주)는 v5.231 완주 검증에 위임.
- 자동화 노하우: 클릭 후 상태는 STEP 전환(.ob-prog 'STEP n/9')·토스트로 인메모리
  판정 — 세이브 반영은 최대 수 초 지연이라 즉시 세이브 읽기로 오판하지 말 것.

### 2026-09-13 v5.244 — 시뮬 '재미 지표' 첫 측정 — 후반 밀도 0.30/h·최장 공백 17.5h

'재미'를 정성 판단에서 수치로: 액션 이벤트(제작·합성·강화·세트·각성 창)를 방치
수익 창과 구분해 밀도·공백을 잰다(측정 방식은 v5.244 커밋 참조).

**첫 측정(1600h, 시드 42)**: 평균 0.48회/h · 구간 밀도 1.98→1.13→0.50→0.30/h
· 최장 공백 17.5h(@739.5h — 위험강화 망치 골드 저축 대기).

**다음 재미 개선의 데이터 표적**: ① 600h+ 밀도 0.30/h(초반의 15%) ② 700h대
17.5h 액션 구멍. 후보 설계: 자원 대기 중에도 누를 수 있는 소액션(저가 안전
강화 재료 전환·소액 레벨업 등) — 다만 '방치 게임의 대기' 자체를 없애면 안 되고
**대기 중 선택지**를 주는 방향. 시뮬에서 밀도 회복을 목표 수치(예: 600h+ 0.5/h)로
검증한 뒤 게임에 반영할 것.

**작업 노하우**: Git Bash heredoc→Python 한글 전달은 인코딩이 어긋나 매치가
조용히 실패한다(assertion으로 잡힘) — 한글이 든 패치는 Write 도구로 임시
스크립트(UTF-8 보장)를 만들어 node로 실행하는 편이 확실하다.

### 2026-09-13 v5.245 — 재미 지표 현실화 — '후반 썰렁' 오판 정정

v5.244의 표적('600h+ 밀도 0.30/h 개선')을 실행하려다 **측정 자체의 두 빈틈**을 먼저
발견해 정정했다(게임 변경보다 지표 신뢰가 먼저다):

1. 일일 콘텐츠(탑 도전/소탕·골드던전 3회·요일던전·기록서 교환)가 액션 집계에
   빠져 있었다 → dailyStep 수행 요약을 did에 연결.
2. 창(30분) 밀도 '/h'는 24h 연속 플레이 가정 — 방치 게임 유저는 하루 1~3회 접속에
   몰아한다. 세션 관점 '액션/일'을 병기.

**정정된 판정**: 600h+ = 7.4 액션/일(탑·던전·각성·강화) — 건강. 최장 공백 17.5h도
오프라인 정산·자연 접속 간격이면 정상. 재미 개선 표적 철회, 지표는 구간 추이
감시용으로 유지(후속 변경이 5 액션/일 미만으로 떨어뜨리면 그때가 진짜 경고).

**교훈(측정 설계)**: 대리 지표는 '측정 단위'(여기선 시간 단위)가 유저 행동 패턴과
어긋나면 정반대 결론을 낸다 — 임계값을 정하기 전에 단위부터 의심할 것.

### 2026-09-13 QA 9차 — 온보딩 9단계 완주 실물 + STEP9 팝업 경쟁 결함 수정 (v5.246)

신규 세이브로 **튜토리얼 전 스텝을 실물 완주**했다(온보딩 검증 종결):

- STEP8: 진영 편성(우측 목록에서 영웅 클릭→저장) + 투기장 입장(입장 버튼→확인창
  [입장] 2단계) → arenaEnters=1 → STEP9 전환.
- STEP9: 투기장 ✕ 후 미션 완료 보상 → 클래스 특성 선택 → 비교 화면 [확정] →
  seenTutorial=true · classTrait=mage · '마법형 확정' 토스트. errbar 0건.

**결함 2건 평가**:
1. '입장 제한 10:00~12:00에 막힌다'(게임 시계 22:37에서 입장 무반응으로 의심) —
   코드 확인 결과 **표시 문구일 뿐 게이팅 없음**(무반응의 정체는 내 클릭이 확인창
   2단계를 덜 누른 것). 의심 철회.
2. **실결함(수정 v5.246)**: 매치 결과 3초 콜백이 튜토리얼 중 투기장을 재오픈해
   '모달이 전부 닫혀야 여는' 미션 보상 팝업과 자리다툼 — 실물에서 ✕를 여러 번
   눌러야 했다. closeModal() 홈 복귀로 수정(v5.231 자동입장 차단 유지).

**6400h 재미 지표(시드 42)**: 600h+ 6.2 액션/일(감시 규칙 5/일 위 안정) ·
최장 공백 19.5h(@5879.5h) · 결정 ✦15 · CP 2,301,636.

### 2026-09-13 v5.247 — '결정 가호'(골드 버프) — 후반 소액션 + 골드 싱크

6400h 재미 지표의 마지막 매찰(공백 19.5h·골드 적체)에 대한 소액션 상품.
상세 근거·시뮬 전후는 v5.247 커밋 메시지 참조. 핵심:
- addGold 단일 관문(F2) 반영 — 골드던전·오프라인 등 addGold 경유 수입 전부에
  버프가 붙으므로 '던전 직전 켜기' 전략이 실사용 가치의 핵(사냥 유입만 보면
  손해라 시뮬 초반 정책 91M은 효과가 없었다 — 61M로 완화해 +16% 액션 실증).
- 재미 지표: 600h+ 8.6 액션/일(전 7.4). 감시 규칙(5/일)에서 여유 확대.
- 시뮬 buffTally 진단 추가(구매 횟수·골드).

**교훈(버프류 설계)**: 획득량 버프의 가치는 '관문 경유 수입 전체'에서 나온다 —
단일 수입원(사냥) 기준으로 가격을 잡으면 상품이 죽는다. 관문(addGold)에 붙인
지금 구조가 정확하고, 시뮬 정책도 그 수입 구성을 반영해야 측정이 유효하다.

### 2026-09-13 QA 10차 — 결정 가호(v5.247) 실물 검증 + 6400h 가호 포함 곡선

- **실물(신규 탭·골드 1억 주입)**: 골드 탭 상품 렌더("결정 가호 1시간 (+50% 골드) ·
  골드 500만") → 구매 → 골드 5,005만 차감·buffs.goldUntil 설정·토스트 정상·
  goldBuffMul()=1.5(관문 정합)·남은 시간 60분 정확·errbar 0건. 연장(max+1h)은
  smoke 회귀가 커버.
- **6400h 가호 포함 곡선(시드 42)**: 가호 4,499회(225억 골드 소모 — 시뮬 정책이
  소환서+망치 예산 위에서 상시 구매) · 600h+ 재미 지표 **21.9 액션/일**(가호 전
  6.2의 3.5배 — 상시 구매 포함 수치, 실사용 전략 구매면 중간쯤) · 결정 ✦11(전
  ✦15 — 가호가 기록서 골드와 경쟁) · CP 2,276,412(-1%, 인플레 없음) ·
  강화석 18,849(전 21,113 — 소모 증가).
- **판정**: 가호는 골드 싱크·액션 밀도 모두 유효하며 밸런스 파손 없음(CP -1%).
  결정(영구)과 가호(일시)의 골드 경쟁은 유저 선택지로 둘 다 열려 있는 설계.

### 2026-09-13 QA 11차 — 엔드게임 장기 플레이 안정성 사냥 (결함 0건)

완주급 세이브(각성 50·결정 2·기록서 100·강화석 2,000·골드 10억·전 장비 +24 L)로
최신 기능을 실제로 섞어 돌리며 잠복 결함을 사냥:

- **결정 각성 연속**: ✦2→4 진행 — 기록서 65권·강화석 800개 차감, 다음 비용
  "40권+460개"(공식 정합), 스탯 77.0%(50×1.5%+4×0.5%). ⚠ 세이브 반영이 수 초
  지연이라 직후 스냅샷으로 '무반응'으로 오판하기 쉽다(실측 교훈 재확인).
- **가호 구매 + 극한 강화 +24→25**: "강화 성공 +25" 1회 통과·err 0.
- **22개 모달 전수 순회**(forge~arena): 빈 바디 0·errbar 계속 빈.
- **3분 방치**: IAB 백그라운드 탭이 discard·리로드되어 전투 정지 — 개발 환경
  특성(게임 결함 아님). 재진입 정상.
- **오프라인 정산 경로**: 온보딩 미완료 세이브(seen=false)는 매 접속 인트로
  대사가 재생되며(v5.113 멱등 설계) settle 자동 팝업은 완료자 분기(8437)에만
  있다 — 미완료자는 **topbar timepod 수동 경로**(8493)로 정상 수령 확인
  (방치 정산 팝업·획득 재화 목록 렌더 정상). pending이 갇히지는 않는 설계.

온보딩 중 이탈·재접속 사용자는 인트로 대사 3장부터 다시 밟는다 — 멱등이라
결함은 아니지만, 신규 이탍 단면에서 매번 대사가 먼저 오는 건 UX 후보로 남김
(우선순위 낮음: 대사 스킵은 이미 클릭 몇 번으로 지나감).

### 2026-09-13 v5.248 — 인트로 재접속 대사 생략 (QA 11차 UX 후보 실행)

온보딩 중 이탈(introDone=false) 재접속 유저가 매번 리안 대사 3장부터 다시 밟던
것을 수정 — tut.introSeen 표식으로 대사를 생략하고 introRewards(멱등)로 직행.
보상 완료 케이스는 원래 깔끔했고, 이 수정은 미완료 케이스만 겨냥한다.
경로별 결과·회귀 내용은 v5.248 커밋 메시지 참조.

**남은 온보딩 후보(우선순위 낮음)**: 튜토리얼 시작 대사('먼저 몬스터…')도 재접속
때마다 재생된다 — introSeen 과 같은 패턴(tut.tutSeen)으로 줄일 수 있으나,
이 대사는 STEP 안내 성격이라 남겨두는 게 나을 수도. 실물로 재접속 흐름을
밟아본 뒤 판단할 것.

### 2026-09-13 QA 12차 — v5.248 인트로 재접속 실물 검증 (전 경로 통과 · 후보 소멸)

'대사 1장에서 이탈 → 재접속'을 실제로 밟아 v5.248을 검증했다:

1. **첫 접속**: 인트로 대사 3장 표시와 함께 tut.introSeen=true 세팅 ✓
2. **대사 중 이탈 → 재접속**: 첫 대사("안녕하세요…") 재생 없이 **보상 획득 팝업
   직행**(투기장 랭크 골드 50만 [받기]) — 멱등 직행 경로 정확 ✓
3. **보상 3칸 수령**: introDone=true → 튜토리얼 시작 대사 1회("먼저 몬스터…") →
   STEP1 진행(킬 카운트) → 골드 정합(보상 122만 반영) · errbar 0 ✓
4. **v5.248 기록의 남은 후보(튜토리얼 시작 대사 재생) 소멸**: 이 대사는 인트로
   완료 직후 1회만 재생된다 — introDone=true 후 재접속은 enterHome else 분기
   (renderTutorial만, 대사 없음). "재접속마다 반복"이 아니므로 고칠 것이 없다.

QA 11차가 남긴 UX 후보가 전부 닫혔다 — 온보딩 이탈·재접속 3경로(대사 중·보상
중·완료 후) 모두 매끄러움 실증.

### 2026-09-13 v5.249 — '주간 의뢰' (일일 루프에 주간 리듬)

퀘스트 [주간] 탭 — 월요일 ISO 주 리셋·stats 스냅샷 차이 진행·의뢰 3종(처치 5,000→
기록서 3·제작 15→전설망치 10·소환 30→골드 2,000만)·의뢰별 1회성 수령.
시뮬 전후·설계 노트는 커밋 참조(각성 +36·CP +3.8%·공백 16.5h로 소폭 개선).

**교훈 2건(재발 방지)**:
1. **렌더 템플릿에 지급 함수(give)를 넣지 마라** — 첫 패치가 렌더만으로 보상이
   지급되는 구조였다(내부적으로 발견·수정). 표시는 전용 상수(WEEKLY_REWARD_TXT),
   지급은 핸들러 안에서만. '렌더 무지급'을 smoke 회귀로 못 박아둠.
2. **임시 패치 스크립트는 git add -A 전에 삭제** — _patch_*.js가 커밋에 섞여
   들어가는 사고(553ee2b로 제거). 커밋 직전 `git status`로 미추적 파일 확인.

### 2026-09-13 QA 13차 — 주간 의뢰(v5.249) 실물 검증 (결함 0건)

- **주간 탭 렌더**: 퀘스트 [주간] 탭 — "주간 의뢰 · 2026-W37"(ISO 주 표기)·의뢰 3행
  ·진행 수치(5000/5000·0/15·0/30)·미달 의뢰 [받기] disabled까지 정확.
- **수령 실행**: w1 [받기] → 토스트 "주간 의뢰 완료 보상 — 영웅 기록서 X3" →
  행 "수령 완료 ✓"·버튼 '완료'(1회성) → weekly.claimed.w1 저장 → 기록서 +3
  반영(save 지연 후 records=3 확인). errbar 0건.
- 탭 클릭은 cua 좌표가 못 잡아 DOM click으로(탭 4개로 늘어 좌표 변동 — 다음
  검증자는 DOM click 우선). 진행 주입은 가져오기로 weekly.base를 역산해 넣었다.

### 2026-09-13 v5.250 — 12800h 초장기 시뮬 (첫 데이터) — 결정 병목이 기록서로 전환

시뮬 창 상한 25600(=12800h)·재미 지표 구간 세분화(600~3200/3200+). 첫 측정(시드 42):

- **무한 축 장기 작동**: 결정 ✦23(✦15@6400h → 지속 적립) · CP 3,122,372(+37%) ·
  재미 지표 600h+ 24.3 액션/일(감시 규칙 5/일 크게 위) · 최장 공백 17h(@1650h).
- **발견 ① — 결정의 병목이 기록서로 전환**: ✦24 비용 135권 > 보유 108권. 결정이
  기록서 대기로 멈추는 동안 골드가 캡(50억) 근접까지 쌓인다(40.8억). 주원인은
  시뮬 정책 — 결정 구매 조건이 '기록서 비용 전액'(✦24=40.5억)을 요구해 분할
  구매(권 단위 3천만)를 못 한다. 실사용자는 분할 구매로 캡 유실을 피할 것.
  → 다음 과제: 시뮬 결정 정책의 분할 구매화(권 단위).
- **발견 ② — 강화석 장기 적체**: 18,849(6400h) → 44,437(12800h). 결정의 강화석
  비용 기울기(✦23=1,220개/단계 ≈ 2주치 유입)가 유입을 못 따라간다. 무한 축의
  자연 감속이지만 강화석이 다시 고아화되는 신호 — 결정 비용 기울기 상향(40n→?)
  또는 유입 확대 후보. ✦23/12800h(실플 ~5개월) 페이스 자체는 적정이라
  급한 조정은 아님 — 다음 분할 구매 정책 적용 후 재측정으로 판단.

**판정**: 게임 밸런스 변경 없음(시뮬 인프라만). 초장기에서도 벽·인플레 붕괴 없음.

### 2026-09-13 v5.251 — 시뮬 분할 구매화 — 12800h 골드 적체 해소·✦27

v5.250 발견 ①의 정책 수정: 결정 기록서 구매를 '비용 전액'에서 **권 단위 분할**
(3천만×n, 망치+소환서 예산 위)로. 전후 수치·판정은 커밋 참조(골드 40.8억→5,767만,
✦23→27, 기록서 구매 165억→310억, 강화석 44,437→39,357).

**발견 ② 종결**: 강화석 잔여도 결정이 더 오르며 소모가 늘어 자연 완화 — 기울기
별도 조정 불필요로 판정. 초장기(12800h)에서 벽·캡 유실·인플레 모두 없음이
재실증됨. 시뮬 정책-실사용자 행동 정합(전액 요구는 합리적 플레이어가 안 한다)이
장기 곡선 신뢰의 조건이라는 교훈을 v5.250 기록에 이어 재확인.

### 2026-09-13 v5.252 — 레벨 상한 999→9,999 — 초장기 포화 해제

12800h 시뮬 진단의 'Lv999'가 실제 하드캡 포화(≈9,500h 도달)였다 — 캡처 근거 없는
임시 상한이라 9,999로 상향. 전후 수치·판정은 커밋 참조(Lv1,102·CP +7%·이상 없음).
레벨업 골드 버튼(heroDetail)의 비용식 lv×8만도 4자리에서 정상 동작(자리수 가정 무).

**교훈**: 시뮬 진단의 '깎은 숫자'(999·30만·x0 등)는 의심하고 출처를 뒤져라 —
이번 999는 우연히 진단 표기에서 발견됐지만, 상한·캡 계열 상수는 코드에 산재해
있고 하나씩 '실측인가 임시값인가' 구분이 남은 과제다(예: 골드 캡 50억·GOLD_CAP).

### 2026-09-13 v5.253 — 골드 버프 키 충돌·유료 약속 미구현 발견 수정

'상한·캡 상수 출처 의심' 원칙을 버프 키에 적용해 발견: v5.247 결정 가호가 프리미엄
구독 키(goldUntil)를 재사용 — 프리미엄 '골드 +100%(30일)'은 배율 구현이 아예
없었고(유료 약속 위반), 가호는 프리미엄 만료 시각에 +1h만 붙었다. 키 분리
(goldPactUntil)·약속 정합(+100%/+50% 합산)·명패 배지에 가호 잔여(💠G50) 상시
표시. 검증·수치는 커밋 참조(시뮬 곡선 불변이 정합).

**교훈**: 새 기능이 기존 상태 키를 재사용할 땐 그 키의 '원래 소유자'(여기선 유료
상품)부터 조사하라 — 우연히 배율이 붙는 순간 유료 약속 위반이 된다. 유료 상품
스캔(BUFFSHOP·RUBYPKG 등 give/효과 정합)은 아직 전수 검증된 적 없다 — 다음 QA
후보.

### 2026-09-13 v5.254 — 유료 상품 give 정합 전수 회귀 — 미구현 0건 실증

v5.253의 후보 실행: give 함수를 가진 유료 상품 전량(40여 개)의 지급을 스냅샷
비교로 자동 검증하는 smoke 회귀. 결과·설계는 커밋 참조.

**교훈(스냅샷 방어망)**: '변화 0 탐지' 회귀는 스냅샷 필드가 곧 검증 범위다 —
첫 실행의 12건 false positive가 누락 필드(즉시완성권·곡식·재료·장비·칭호)를
전부 가르쳐줬다. 신규 재화 추가 시 이 스냅샷에 필드를 넣어야 방어망이 따라온다.
BUFFSHOP(렌더러 인라인 구매)은 이 방식 밖 — 버프류 신설 시 소비처 관문 회귀를
별도로 둘 것(v5.253 결정 가호 회귀가 그 모델).

### 2026-09-13 v5.255 — 명패 배지 회귀 (QA 14차 갈음) + IAB 탭 누적 노하우

- **QA 14차(IAB) 시도 → 호스트 불능**: 루프들이 탭을 13개까지 누적시킨 끝에
  'guest not attached (webview not ready)' — 새 탭 생성·기존 탭 정리 모두 불능.
  **세션당 IAB 탭은 한두 개로 유지할 것.** 브라우저 세션을 못 믿을 땐 게임
  상태를 건드리지 않는 검증(배지·표기류)을 smoke 의 DOM 경로 회귀로 갈음한다.
- **명패 배지 회귀(v5.255)**: refreshHUD→#premiumBadge 텍스트 세팅 경로를
  그대로 검증(버프 0 → G50 없음 / 가호 → 💠G50 / 병립 → 💎G·📘E·🔨C·💠G50).
  v5.253의 마지막 미확인(배지 실물)이 게이트로 못 박힘.

### 2026-09-13 v5.256 — '월간 의뢰' — 일-주-월 리듬 완결

주간(v5.249) 패턴의 월간 복제: 매월 1일 리셋(getMonthKey), 3의뢰(처치 30,000→
기록서 10 · 제작 60→전설망치 30 · 소환 150→골드 1억 raw), 의뢰별 1회성.
전후 수치는 커밋 참조(각성 +38·CP ±0·공백 15h·가호 순환).

**남은 미확인**: 월간 탭 실물 렌더·수령(주간과 동일 경로라 위험 낮음 — 다음
브라우저 세션에서 확인). 리듬 3층위(일-주-월)의 실사용 체감은 운영 데이터 필요.

### 2026-09-13 v5.258 — 버프 키 이중 소유 정적 검사 + README 해상도 정정

- verify 신규: 상품 배열의 버프 키 소유 중복 → 게이트 실패(v5.253 사고 방어).
  자기 검증으로 현재 통과·v5.247 시뮬레이션 탐지 확인. 상품 배열에 신규 버프
  상품을 추가할 땐 이 검사가 키 충돌을 막아준다.
- README 권장 해상도 375×812 → 453×852(설계 정합).
- IAB 3루프째 불능 — 월간 실물 이월 유지.

### 2026-09-13 v5.259 — 레전더리 제작 성공 풀스크린 연출

v5.190 토스트에 시각 피크 추가(conic 방사 광원+아이콘 팝 2.2초, z30, DOM+CSS라
결정론 무관). **스텁 한계 발견**: documentStub.querySelector가 클래스
셀렉터('.lgd-flash')를 지원하지 않아 항상 더미 노드를 반환 — 게임의 중복 체크가
smoke 환경에서 첫 호출부터 차단됐다(실제 브라우저는 정상). 회귀는 함수 정합으로
강등하고 오버레이 실물은 브라우저 QA 이월. **향후 과제**: documentStub에 클래스
셀렉터 지원을 추가하면 이류 회귀들이 렌더 검증으로 복원 가능.

### 2026-09-13 v5.260 — 스텁 클래스 셀렉터 지원·플래시 회귀 복원

v5.259 과제 실행: documentStub._findByClass + Node2 노드 스코프 '.cls' 탐색.
과정에서 shop 4건 회귀 파탄으로 **shop이 card.querySelector('.sh-left')를
폴백 더미에 우연히 의존** 중임을 발견 — 폴백 보존으로 해소하고 원칙 확립:
**존재 탐색(요소 얻기)은 querySelector 근사, 실존 판정은 children 직접 스캔.**
legendaryFlash 중복 체크를 children 스캔으로 변경(실제 DOM 의미 동등)하고
회귀를 렌더 수준으로 복원. 임시 스크립트 커밋 사고 2회째(53f1247 제거) —
**패치 스크립트는 실행 즉시 rm 하기 전에 git status를 볼 것.**

### 2026-09-13 v5.261 — 첫 25600h 초초장기 시뮬 — 무한 축 4배 기간 무결점

✦45·CP 500만·12800h+ 17.1 액션/일(시간이 지날수록 상승)·골드 적체 없음·새 병목
없음. 강화석 8.6만 재적체는 결정 비용 기울기가 유입을 못 따라가는 것 — ✦45
완주 페이스(실플 ~12개월)가 적정 감속이라 기울기 조정 보류. 수치·판정은 커밋
참조. IAB 6루프째 불능 — 실물 이월(월간 탭·연출 픽셀).

### 2026-09-13 v5.262 — 주간 의뢰 4축 '탑 5회 도전' — 의뢰 다양성

stats.towerTries(탑 도전 확정 카운트) 신규 + 주간 w4(기록서 X2). 시뮬 전후는
커밋 참조(각성 +39·CP +2%·공백 14h·인플레 없음). 구세이브는 stats.towerTries가
undefined로 시작 — weeklyState가 0으로 스냅샷해 정상 동작(회귀가 커버).
미확인: w4 실물 렌더·수령(주간과 동일 경로) — IAB 8루프째 불능으로 이월.

### 2026-09-13 v5.263 — 월간 4축 '탑 20회 도전' — 주간과 대칭 완결

주간 w4·월간 m4로 양 리듬 모두 4축(사냥·제작·소환·탑). m4 보상 기록서 X5/월 —
기록서 총 공급 증가 폭(주 2+월 5)은 결정 축 순환 범위. 시뮬 전후는 커밋 참조.
미확인: m4 실물 렌더·수령(동일 경로) — IAB 9루프째 불능으로 이월.

### 2026-09-13 v5.264 — 의뢰 리셋 카운트다운

주간·월간 탭 헤더에 '다음 리셋 N일 남음'(월요일/1일까지 ceil). smoke는 계산
정합+주간 탭 클릭 후 렌더 검증. 미확인: 실물 표기(IAB 10루프째 불능으로 이월 —
실물 QA 대상 5건: 월간 탭·m4·w4·연출 픽셀·리셋 표기).

### 2026-09-13 v5.265 — 의뢰 수령 알림 배지

weeklyClaimable/monthlyClaimable을 questClaimable에 합산 — v5.162 배지 체계가
주간·월간 의뢰의 '완료 미수령'까지 자동 커버. 미확인: 배지 실물 점화(IAB 11루프째
불능 — 실물 QA 대상 6건: 월간 탭·m4·w4·연출·리셋 표기·배지).

### 2026-09-13 v5.266 — 공략 카드 '접속 리듬' 라인

일일 N/6·주간 N/4·월간 N/4 완료 수 + '수령 가능!' 힌트(v5.265 판정 재사용).
카드가 세트·강화·각성·탑·리듬 전체를 커버. 미확인: 카드 실물 렌더(IAB 12루프째
불능 — 실물 QA 대상 7건).

### 2026-09-13 v5.267 — 의뢰 생태계 반영 12800h 재측정

✦29·CP 3,393,164(±0%)·강화석 33,627(소모 증가)·재미 16.5 액션/일 — 의뢰 확장
(기록서 +7권/주)이 인플레 없이 결정 축 순환. 수치는 커밋 참조.
IAB 13루프째 불능 — 실물 QA 대상 7건 이월 유지.

### 2026-09-13 v5.268 — verify 'give 상태 키 존재' 검사

상품 give의 S.<키>= 세팅이 freshState에 선언된 키인지 정적 검사(오타→조용한
미지급 방어). freshState 추출은 중첩 브레이스 스캐너(첫 닫기} 절단 오탐 함정).
**교훈**: 검증 도구가 오탐을 뱉으면 도구부터 의심하고 원인(이스케이프·추출 범위)을
분리하라 — 이번엔 자기 검증 스크립트의 \\b 리터럴화가 범인이었다.
IAB 14루프째 불능 — 실물 이월 7건 유지.

### 2026-09-13 v5.269 — 오프라인 정산 배지

timepod·☰에 미수령 offlinePending 점, 수령 시 즉시 소등. timepod 에 id 부여 —
클래스 쿼리 폴백이 스텁에서 노드별 새 인스턴스를 만들어 회귀가 실브라우저 1개
노드와 어긋나는 것을 id 로 정합(v5.260 원칙의 실례). 미확인: 배지 실물 점화
(IAB 15루프째 불능 — 실물 이월 8건).

### 2026-09-13 v5.270 — 의뢰 생태계 반영 25600h — 최신 곡선 정본

✦47(전 ✦45)·CP 5,030,464·12800h+ 17.1 액션/일·강화석 80,128·골드 적체 없음.
초초장기(실플 ~2년)에서 의뢰 기록서가 결정 축으로 순환 — 최신 코드 기준 곡선
정본. IAB 16루프째 불능 — 실물 이월 8건 유지.

### 2026-09-13 v5.271 — 칭호 개선 배지

titleUpgradeable(골드 효과 우위 미장착 보유 칭호) — 드로어 칭호 항목·☰ 점.
골드 축만 정렬 기준으로 쓴 이유: 경험치·제작은 상황별 가치라 노이즈 위험.
미확인: 배지 실물(IAB 17루프째 불능 — 실물 이월 9건).

### 2026-09-13 v5.272 — 의뢰 스냅샷 접속 즉시 확정

enterHome 에서 weeklyState/monthlyState 호출(try-catch) — 리셋 후 첫 오픈
지연으로 진행이 새지 않던 불리 해소. **교훈**: 스냅샷·카운터 기준 시점이
'유저가 언제 열었나'에 달리면 설계 오류 — 진행의 정본은 이벤트 시점이어야 한다.
미확인: 실물(IAB 18루프째 불능 — 이월 9건).

### 2026-09-13 v5.273 — 모달 수·README 정합 게이트

README '화면(모달) N종'이 실제 MODALS 키 수와 같은지 verify 대조(현재 45=45).
모달 추가 시 문서 갱신 누락을 배포가 막는다. 자기 검증(46 주입 탐지) 완료.
IAB 19루프째 불능 — 실물 이월 9건 유지.

### 2026-09-13 루프 57 — 실질 작업 없음 선언 (원칙 준수)

게임 코드·경제·리듬·배지·검증(정적+런타임+결정론)·시뮬(25600h)·문서의 실질
작업 후보를 재검토 — 모두 완결 또는 유보 판정 유지(세트 8단계: 실측 구성이라
구조적 불가·쿨타임 축도 도달 불가로 무의미). "실질 작업 없으면 커밋하지 않는다"
규칙에 따라 이번 루프는 배포 없음. 게이트 전체 통과로 라이브 무결 확인만 수행.

**다음 세션 최우선**: 브라우저(IAB) 회복 후 실물 QA 9건 스윕(스냅샷 인덱스 목록) —
그 결과에 따라 신규 과제가 열린다. 세션 시작 시 IAB 탭 1개로 시작할 것.

### 2026-09-13 v5.274 — 시뮬 캐주얼 시나리오 — 유저 세그먼트 곡선 첫 측정

'casual' 인자(일 8h 접속+오프라인 8h 상한 정산) — 400h: R 25h·E 100.5h·
L 169h·CP 159,687(풀의 53%). 벽 없음·곡선은 접속 시간에 선형 비례. 전후 수치·
설계는 커밋 참조. 다음 후보: 캐주얼 1600h+(무한 축 도달성). 잔여 스크립트
커밋 3회째 — 패치는 Write→실행→즉시 rm 원칙 재확인.

### 2026-09-13 v5.275 — 캐주얼 1600h — 무한 축 도달성 실증·지표 분모 교훈

각성 +38(풀과 동급)·CP 54%·골드 싱크 정상·극한 완주만 늦을 뿐 벽 없음.
**교훈**: 재미 지표 '/h'는 24h 연속 분모라 캐주얼 곡선에서 구조적으로 낮게
나온다(4.1/일 — 감시 규칙 5/일 하회 오탐). 세그먼트별 분모 명시 필수 —
캐주얼은 '/접속일'(12.3) 기준. 측정 데이터만 커밋(코드 불변).

### 2026-09-13 v5.276 — 재미 지표 세그먼트 분모 병기

캐주얼 '/접속일'(funTimes÷접속시간×8h) 병기·감시 규칙 이 값으로 판정.
첫 환산식 오류(×24 중복)를 200h 빠른 실행으로 즉시 발견·수정 — 지표 변경은
짧은 실행으로 값 검증 후 커밋할 것. 시뮬만(코드·게임 불변).

### 2026-09-13 v5.277 — 구간 밀도 세그먼트 분모 병기

캐주얼 구간별 '/접속일'(구간 접속시간 기준) 병기 — v5.276 총평의 잔여 사각
해소. 검증 200h: 구간 16.8·14.7/접속일, 총평과 정합. 시뮬만.
IAB 27루프째 불능 — 실물 이월 9건 유지.

### 2026-09-13 v5.278 — 캐주얼 2400h — 극한 완주 시점·재미 규칙 적용 범위

평균 23.4·완주 0부위@2400h(완주 ≈3000h+)·각성 +45(풀 1600h 추월). 재미 지표
캐주얼 장기 3.5/접속일(규칙 하회) — **규칙 적용 범위를 온보딩~중반(600h까지)으로
명시**, 장기 캐주얼은 방치 설계의 자연 저밀도이므로 성장 지표로 판정. 시뮬 데이터만.
IAB 28루프째 불능 — 실물 이월 9건 유지.

### 2026-09-13 v5.279 — 캐주얼 5000h — 전 축 완주·이중 세그먼트 완결

+25 10부위 완주(완주 3000~4000h)·각성 50 완주 → 결정 ✦11 진행·CP 115만.
재미 지표 저밀도(600~3200h 4.2)는 3200h+ 6.9로 회복 — '완주 사이 협곡'이지
정체가 아니었다. 두 세그먼트 전 수명 무결점 실증으로 이중 세그먼트 과제 완결.
시뮬 데이터만. IAB 30루프째 불능 — 실물 이월 9건 유지.

### 2026-09-13 v5.280 — 캐주얼 12800h — 초장기 무결점·세그먼트 격차 배율 실증

✦27(풀 ✦29와 ±2)·CP 194만(풀 57%)·골드 적체 없음·3200h+ 7.2/접속일 회복.
핵심: 캐주얼-풀 격차가 접속 시간 비율(1/3)보다 작다 — 오프라인+의뢰 보상이
격차를 상쇄. '접속 없는 유저도 성장' 약속이 초장기까지 유효. 시뮬 데이터만.
IAB 34루프째 불능 — 실물 이월 9건 유지.

### 2026-09-13 v5.281 — 실물 QA 9건 스윅 완료 + 재화바 말줄임 결함 수정

**경위**: IAB 가 61루프째 불능(tabs.list 만 살고 new/activate 전부 실패)이던 중,
전역 playwright 헤드리스 크로미움 우회로를 확립해 이월 9건을 전량 실물 확인했다.
세이브 주입은 `addInitScript`(게임 스크립트보다 먼저 localStorage 심음) — 종전
언로드 세이브 부홑 함정·'가져오기' 경로 우회 없이 결정론적으로 상태를 만든다.

**9건 결과 — 전부 통과(DOM 20/20 + 픽셀 판독)**:
월간 탭(5탭 강조·m1~m4 렌더) · m4 수령(토스트·기록서 +5·수령 완료 ✓) ·
w4 수령(기록서 +2·버튼 완료 전환) · 리셋 카운트다운(주간 7일·월간 17일) ·
레전더리 연출(광선·LEGENDARY·z30·2.2초 소멸) · 오프라인 배지(timepod 점 +
정산 모달 9.9만 G 실체) · 칭호 개선 배지(메뉴 칭호 항목 점) · 공략 카드
리듬 라인("일일미션 1/5 · 주간 의뢰 1/4 · 월간 1/4").

**신규 결함 1건 발견·수정 — 재화바 말줄임**:
`.cur span:last-child{max-width:64px}` 가 B1/G-12 '재화바 전체 자릿수 노출'
(fmtFull)과 모순. 백만 골드(9자 ≈ 66px)부터 "2,000,…" 처럼 잘리는데, 십억
골드가 "2,000만~2,000,999" 로 **잘못 읽히는** 수준(픽셀 판독으로 확인).
→ max-width 64→118px(조 단위 15자 ≈ 112px 커버, ellipsis 는 극단값 안전장치로 유지).
검증: 천만·십억 대 sw==cw(말줄임 없음)·명패(92px 고정)와 무겹침·뷰포트 내.

**오독 정리(비전 판독 함정 기록)**: '세트효과' 를 "서브호과"로 읽음(마크업 정상),
'06:01' 시계를 "00:00 겹침"으로 오인 — 시계가 길잡이 배너(z7) 반투명 배경에
비칠 뿐 가독성 무해(미수정). 비전 지적은 DOM 좌표·마크업 대조로 정합 확인 후
반영한다는 원칙의 실증 사례.

### 2026-09-13 루프97 — 전 모달 45종 실물 렌더 스윕: 결함 0건 + 라이브 반영 확인

**동기**: v5.281 재화바 말줄임은 스모크가 CSS를 못 보는 구조적 사각(4장)에서
나온 결함이었다 — 같은 류가 더 있는지 MODALS 전수를 실물로 검사했다.

**방법**: 전역 playwright 헤드리스(루프96 우회로) + addInitScript 로 엔드게임
상태(각성 50·가이드 9단계·골드 25억·주간/월간 진행)를 주입, 45종 모달을 순차
openModal 하며 리프 텍스트 오버플로(sw>cw+2 / sh>ch+2)와 패널 뷰포트 이탈을 측정.

**결과 — 결함 0건.** 최초 스윕의 플래그 12건은 전부 측정·판정 오탐:
- VP 8건: openModal 직후 rect 는 `pop .17s` 팝인 애니메이션 중간 transform 값을
  잡는다 — 350ms 안정화 후 전 모달 9~444 × 13~839(뷰포트 내)로 정상.
- OV 3건(optionReroll ↻ · arena ⓘ/1,088 · settle 06:01): 수평 초과 없음(sw==cw)에
  세로 3~6px 줄높이 미세 초과뿐, 클립 없음 — settle 은 픽셀 판독으로 잘림 없음 확증,
  나머지도 동일 패턴으로 무해.

**라이브 반영 확인**: 배포본 https://ggonggee.github.io/hwasin-play/ —
Ver 5.281·`game.js?v=5281` 캐시버스터 정상(push=배포 무결).

**함정 기록(다음 실물 QA 재발 방지)**: ① `#modal-root` 첫 자식은 `.scrim`(배경
딤)이고 본체는 `.modal` — firstElementChild 로 판정하면 '빈 렌더' 오탐. ② rect 측정은
pop 애니메이션 종료 후. ③ 리프 텍스트 sh>ch 소폭 초과는 클립 없으면 무해 —
판정 기준은 '수평 초과 또는 clip 상태의 세로 초과'로 좁혀야 한다.

### 2026-09-13 v5.282 — 시드 파라미터화 + 10시드 분산: 곡선 결론의 로버스트니스 실증

**동기**: 곡선 결론(벽 없음·600h CP)이 전부 시드 42 단일 궤적에 근거했다는 약점.
시뮬의 시드가 3곳(vm Math.random·srand·Battle.setSeed)에 하드코딩돼 있어
분산 측정이 불가능했다.

**변경**: `seed=N` 인자(위치 무관·casual 병용 가능, 기본 42). seed=0 은
xorshift 붕괴라 가드. **회귀 무결 실증**: 구버전(v5.281 stash)과 신버전 모두
600h CP 440,373 완전 동일 — 파라미터화가 궤적을 바꾸지 않는다.

**측정(10시드 × 600h)**: R 10.0~12.0h · E 48.0h(9/10, 나머지 48.5) ·
L 55.5~100.0h(중앙 90.75h — 13·777의 이른 궤적 2개는 첫 L 제작 연속 성공
운 궤적, 8개는 86.5~100h 군집) · CP 433,259~532,257(중앙 442,442 ≈ 시드 42
440,373) · 재미 이벤트 427~503회 · 최장 공백 10.0~23.0h.

**결론**: ① '벽 없음'은 전 시드에서 성립(공백 ≤23h < 24h). ② 공식 곡선 수치
(시드 42)가 중앙값 궤적임을 실증 — 단일 시드 의존 우려 해소. ③ 0장의 600h
CP 428,189(v5.261 기록)는 탑 의뢰(w4 v5.262·m4 v5.263) 기록서 보상 반영 후
440,373으로 갱신(스냅샷 수치가 게임 개선을 못 따라간 낡은 값이었음).

**종결 확인**: 미션 ② '강화 축 시뮬 반영'은 v5.229(위험 강화 직접 측정)·
v5.236(극한 +21~25)·v5.238(강화석 교환)에서 이미 완료 — balance-sim 10행의
"강화는 하지 않는다(v1)" 주석만 남아 미션이 미해소처럼 보이게 했다(주석 정정).

### 2026-09-13 v5.283 — 의뢰 리듬 날짜 경계 회귀: 2월 말·연말·ISO 주 비대칭 실증

**동기**: 리셋 카운트다운 회귀(v5.264)는 '실행일' 기준 산술만 봤다. 리듬 3층위
(일일·주간 월요일·월간 1일)는 달력 경계에서만 조용히 어긋날 수 있는데 실행일이
2월 28일·12월 31일일 확률은 낮아 사고가 나도 한 달/한 번 노출된다.

**회귀(smoke)**: vm Date 를 고정 시각으로 교체(인자 없는 생성·Date.now 모두)해
5 경계를 실증 — 종료 시 finally 원복(이후 스텝이 진짜 시간을 봄):
- 2026-02-28(토)→03-01(일): 월 키 '2026-2'→'2026-3'·남은일수 1→31
- 2026-12-31(목): '2026-12'·W53
- 2027-01-01(금): 월 키는 '2027-1'로 갱신되지만 **주키는 아직 '2026-W53'** —
  ISO 8601 은 목요일 소속 연도 규칙이라 주·월 갱신이 다른 날에 일어나는 비대칭.
  monthlyState() 가 이 경계에서 claimed 를 초기화하는 것까지 확인.
- 2027-01-04(월): 주키 '2027-W1' 갱신·weeklyState() claimed 초기화 확인.

**결과 — 게임 구현 결함 0건**(기대값은 Node 로 알고리즘 사전 검증 후 고정).
경계 정합이 영구 보호됐다. 교훈: eval 문자열에 JSON 끼워넣기는 JSON.stringify
이중 직렬화로(첫 실행 "[object Object]" 실패 — 회귀 스크립트 버그, 게임 아님).

### 2026-09-13 v5.284 — 7일 출석 완주 후 새 주기: 영구 단절 결함 수정

**발견**: 출석 주기 리셋 코드가 게임 전체에 없었다(전수 grep — claimed.attend
참조 6곳 모두 읽기/수령뿐). 7일 완주자는 8일째부터 출석 보상이 영구히 끊기고
attendClaimable 도 false — 일일 접속 훅의 핵심이 7일에 소멸했다. 날짜 경계
회귀(v5.283)를 작성하다 '7일 루프 경계'를 추적해 발견했다.

**수정**: rollDaily(자정 경과를 확정하는 유일한 공식 지점 — 입장권 충전·연속
접속 보상도 여기)에서 '전 칸 수령 완료'일 때 claimed.attend={} 로 새 주기
오픈. 도중 미수령이면 리셋하지 않는다(이어받기 우선). 보상 수량(B9/G-124
실측)은 그대로 — 반복 주기만 추가했고 이는 주간 의뢰(v5.249 매주 월요일
리셋)와 같은 '주기 반복' 원칙. 완주 문구도 '다시 방문하세요'(받을 게 없는
안내)에서 '자정 경과 후 1일차부터 새 주기'로 정정.

**검증**: smoke 회귀(완주 리셋·1일차 오픈·미완주 이어받기 3케이스) + 실물
헤드리스(완주+어제 세이브 주입 → 접속 6.5초 후 배지 틱 경유 rollDaily →
리셋·1일차 오픈·attendClaimable true 실증). 시뮬은 출석을 다루지 않아
(grep 0건) 곡선 영향 없음 — 밸런스 전후 측정 불필요.

### 2026-09-13 v5.285 — 세이브 용량 진단 추가 + 2400h 실측: 장기 세이브 안정 실증

**동기**: 시뮬의 localStorage 스텁은 무제한이지만 실물 브라우저는 5MB 상한이다.
무한 증가 상태 배열이 있으면 장기 이용자만 세이브 실패(_saveFailFlag)로
고생한다 — 로그(채팅·시스템)는 DOM 전용·재료는 고정 키임을 확인했고, 유일한
증가 후보 S.equips 의 증감 경로(제작 push ↔ 등급 업글 미장착 정리·수동 분해·
강화 파괴)를 추적했다.

**측정**: 시뮬 최종 진단에 세이브 직렬화 크기 라인 추가(비관측 — 시뮬엔 분해
정책이 없어 equips 는 실물보다 크게 자람). 2400h 풀: **5.4KB(상한의 0.1%)**
· equips 10점(착용 10부위)으로 수렴 — 등급 업글 정리가 미장착 누적을 막고,
결정 무한 축은 장비를 만들지 않아 25600h 에서도 동일 수준. loginReward 는
(day-1)%7 순환으로 8일차 이후에도 정상 지급 확인(출석 결함과 같은 류 아님).

**결론**: 세이브 용량 위험 0 — 이 루프의 실질 산출은 영구 지표(진단 라인)와
실측 근거다. 밸런스 변경 없음.

### 2026-09-13 v5.286 — 오프라인 정산 8h 상한 회귀 + README 의뢰 수 정정

**회귀**: 백그라운드 복귀 정산 회귀(v5.196)는 1시간·30초만 봤다. 부팅 경로
(computeOffline)와 복귀(_visibilitySettle) 양쪽의 min(elapsed, 8h) 상한을
12h·48h 방치로, 60초 엄격 임계(>60)를 정확히 60초 경과로 실증 — 방치 게임의
핵심 수급 경로 경계. 구현 무결(양 경로 모두 상한 정합).

**문서 정정**: README 리듬 라인이 '주간 3의뢰·월간 3의뢰'였다 — v5.262(w4)·
v5.263(m4) 탑 의뢰 추가 후 4의뢰로 못 바꾼 낡은 표기. 출석에도 '완주 후 주기
반복'(v5.284)을 명시. 게임 동작 변경 없음.

### 2026-09-13 v5.287 — 재미 지표 3축 확장: 행동 종류 분포 + 600h 실측

**동기**: 재미 지표가 밀도(액션/시간)·공백(최장 정적 시간)만 보던 사각 —
액션이 한두 종류에 몰리는 '행동 단조'는 총 밀도가 좋아도 지루하다.

**변경**: 시뮬 재미 집계에 종류 분포 축 추가. did 토큰을 체감 행동 6종
(제작·강화·합성·상급재료·세트·일일콘텐츠)으로 분류해 창 수·점유율·종류 수
리포트. 이후 모든 시뮬에서 자동 출력.

**600h 실측(시드 42)**: 6/6종 전부 등장 — 상급재료 70% · 강화 18% · 제작 7%
· 일일콘텐츠 5% · 합성 1% · 세트 0%. 상급재료 점유가 높으나 **실물 synth
모달에 배수 셀렉터(1/5/10/최대)+확정 합성이 있어** 유저 조작 수는 훨씬
적다 — 시뮬 정책(매 창 합성을 개별 액션 집계)의 인공물 성격이 강해 게임
변경 불필요로 판정. 종류 다양성 확보(6/6) 기록. 밸런스 변경 없음.

### 2026-09-13 v5.288 — 각성·결정 축 재미 집계 누락 수정 + 장기 다양성 정량 근거

**발견**: v5.287 로 종류 분포를 만들고 정합을 검증하다 두 누락을 찾았다.
① awakenStep 의 스텝 수(각성·심화·결정)가 did 에 전혀 안 붙었다 — 시뮬
주석("각성·결정은 did 에 텍스트가 붙는다")과 코드가 어긋난 종전 결함으로,
각성 축의 창이 재미 밀도·종류 모두에서 누락돼 밀도가 과소 측정됐다.
② '결정가호' 토큰은 v5.287 분류 6종에 없었다.

**수정**: did 조립에 각성+스텝 토큰 추가, 분류를 7종(각성·결정 신설)으로.

**전후(600h 시드 42)**: 이벤트 467→469회 · 종류 6/6→7/7 — 각성·결정 39창
(7%)이 새로 보임. **2400h: 7/7종 유지 — 각성·결정 229창(17%)이 장기 다양성
기둥으로 성장**(상급재료 58% · 강화 14% · 일일콘텐츠 8% · 제작 3%). 미션 ①
'엔드게임 다양성'의 정량 근거 완성 — 등급 전환이 끝난 뒤에도 행동 종류가
줄지 않는다. 게임 본체 변경 없음(시뮬 집계만).
**캐주얼 세그먼트 공정성(600h casual)**: 7/7종 동일 커버 — 상급재료 61% ·
강화 14% · 각성·결정 9%(풀 대비 분포 유사) · 감시 규칙 10.6/접속일 · 최장
공백 5.5h(풀 15h보다 짧음 — 오프라인 정산 재화가 접속 창을 촘촘히 채운다).
접속 시간 1/3으로도 행동 다양성이 유지됨을 실증.
**캐주얼 2400h(추가 실측)**: 7/7종 유지 — 각성·결정 121창(16% ≈ 풀 17%) ·
일일콘텐츠 13%(풀 8%보다 높음 — 복귀 창 밀도) · 세이브 5.4KB 동일 수렴.
장기 기둥 교체가 세그먼트 무관임을 직접 실증(외삽 판정 확정).

### 2026-09-13 v5.289 — iOS 사파리 ITP 세이브 삭제 고지

**동기**: Apple ITP 정책상 스크립트 저장소(localStorage 포함)는 7일 사용
없으면 삭제되고(홈 화면 추가 사이트는 제외), 이는 방치 게임의 '며칠 쉬고
재접속' 리듬과 정면 충돌한다. 세이브 소실은 재미 검증 자체를 무너뜨리는
플랫폼 위험 — 결함 보고 전에 알려진 표준 동작이라 자체 대응했다.

**변경**: 설정 '진행도 백업' 행(2026-09-10 신설) 설명에 경고 상시 노출 —
"⚠ iPhone·iPad 사파리는 홈 화면 추가 없이 7일 미접속 시 삭제될 수 있습니다".
백업 기능 자체(내보내기·가져오기)는 기존 것을 그대로 쓴다.

**검증**: smoke 회귀(고지 문구·백업 버튼 존재 — 문구 소실 방지) + 실물
헤드리스 렌더(문구 가시·레이아웃 무결 픽셀 확인). 게임 밸런스 무관.

### 2026-09-13 v5.290 — 투기장 종료 즉시 미션 완료 팝업 전면 전환(대표 요청)

**요청**: "투기장이 끝나면 미션 완료 팝업이 화면을 가리고 나타나도록."

**종전 동작과 문제**: 튜토리얼 8단계(투기장 입장)에서 전투가 끝나도 결과
카드가 3초 떠 있고 → 홈 복귀(closeModal) → tutPoll(0.5s 간격)이 감지해
그제야 미션 완료 팝업이 열렸다(v5.114 예약 구조). 최대 ~3.5초 지연 + 그
사이 홈 전투 화면이 노출돼 온보딩 흐름이 끊겼다.

**수정**: arenaResult(전투 종료 콜백)에서 튜토리얼 미션 대기 중
(!seenTutorial && tut.missionPending)이면 결과 카드 대신 즉시
openModal('missionReward') — modal-root 스크림이 배경을 잠가 화면을 가린다.
승패는 토스트로 남겨 결과 정보 박탈 방지, 결과 카드의 3초 타이머는 이
경로에 걸지 않는다(타이머의 closeModal 이 미션 팝업을 닫는 사고 방지).
일반(튜토리얼 완료) 유저의 결과 카드는 변화 없음.

**검증**: smoke 회귀(튜토리얼 경로 즉시 오픈·missionPending 소진·일반 경로
결과 카드 유지) + 실물 헤드리스(스크림 on·'미션 완료' 렌더·배경 딤 픽셀
판독 확인). 밸런스 무관(온보딩 타이밍만).

### 2026-09-13 v5.291 — 모험 팝업 신설 + 중요도별 아이콘 재배치(대표 요청)

**요청**: 하단 던전 파트를 '모험' 카테고리로 묶어 팝업, 외부 공략·소통은
메뉴로, 코스튬은 상단 메뉴로, 패키지는 상점으로 — 중요도별 정리.

**변경**:
- 우측 플로팅: 공략·소통 제거 → **'모험' 단일 버튼**(공략·소통·유저소통은
  드로어에 이미 존재해 외부 제거로 이동 완료).
- MODALS.adventure 신설: 전투·던전 6종(요일던전·골드던전·보스·월드보스·
  시련의탑·약탈) 그리드 — 클릭 시 각 모달 직결. 잠금은 각 모달이 처리.
- 하단 레일: 11개 → **퀘스트·길드·마을 3종**(전투류는 모험으로).
- 드로어: 13개 → 14개(코스튬 이동, 4/4/3/3).
- 패키지 모달(ACCOUNT_PACKS 한정 상품) 삭제 → 상점 '기타' 탭을 '패키지'로
  개명하고 한정 상품 섹션 통합(구매 상태·계정당 1회·루비 규칙 동일).
  모달 수는 adventure +1 / package −1 로 **45종 유지**.

**검증**: smoke 회귀(레이아웃은 index.html 소스 정합 — 스텁 DOM 계층 없음·
런타임은 render 직접 방식 — openModal 의 modalBody 는 childNodes 더미로 비어
스텁 한계) + 실물 헤드리스(플로팅 단일·모험 6종·드로어 14·레일 3·상점 한정
섹션·픽셀 판독). 밸런스 무관(진입점 재배치만).

### 2026-09-13 v5.292 — 전투 연출 재구성(대표 요청): 중앙 홀드·보스 중앙·위치 리셋

**요청**: 요일던전은 영웅이 중앙에 멈춰 서서 밀려오는 몬스터를 잡게, 보스전·
월드보스는 보스가 중앙에 이미 위치한 상태로 시작(화면 밖 등장 중 처치되는
문제 해소), 이동 후 돌아올 때 캐릭터 위치 리셋.

**변경(Battle)**:
- centerHold(kind 'mobs' — 요일·골드·점령·약탈): 영웅 중앙 삼각 배치(고정
  오프셋, RNG 무소비)·이동 금지. 몹은 스폰 시 중앙 방향 고정 vx/vy로 선형
  이동, 중앙 근접(55px) 정지 — 우측에서 밀려와 둘러싼다.
- 보스(kind 'boss' — 보스·월드보스·길드레이드): x:W+40(화면 밖)·vx:-10
  걸어들어옴 → **x:W*0.62 중앙 즉시 배치·vx:0** + 등장 알림(흔들림·음·토스트,
  홈 군주와 같은 어휘). 아군은 좌측에서 전진해 보스를 때리러 간다.
- 반격 판정: centerHold·보스전은 '아군 근접 영움과의 거리'(보스 리치 95) —
  중앙 배치 보스가 종전 전열 판정(W*0.26)으로 반격 못 하는 결함 예방.
- startDungeon 입장 시 layoutHeroes() 재호출 — 이전 전투에서 이동해 둔
  위치(우측 깊숙이)에서 다음 전투를 시작하던 결함 해소(위치 리셋).

**함정(중요)**: centerHold 몹 이동을 홈 flocking(arrival 감속)으로 재사용하면
D1 결정론이 붕괴한다(실측: 100회 21종 해시) — 감속이 '위치 의존 속도'라
프레임 분할 적분이 갈라진다. 선형 이동+정지선 구조로 해결. 주석에 근거 명시.

**검증**: smoke 소스 정합 회귀 + D 시나리오(kind 'mobs')가 centerHold 경로를
실제로 돌며 D1~D5 통과. 실물 헤드리스 픽셀 판독(일반 던전: 영웅 중앙+몹
우측에서 접근 / 보스전: 보스 중앙+아군 좌측 접근). 시뮬 600h 전후 완전 동일
(CP 440,373·전 지표) — 시뮬은 실전투를 돌리지 않는 구조라 밸런스 무영향.

### 2026-09-13 v5.293 — 드로어 최종 정렬(대표 요청): 퀘스트·길드·마을 이동

**요청**: 퀘스트·길드·마을 아이콘을 메뉴(드로어) 우편 옆으로 — 순서는
'우편, 출석, 퀘스트, 길드, 마을, 설정'.

**변경**: 드로어 14개 → **17개(4열 5행)**, 첫 6순위를 요청 순으로 정렬.
하단 레일(#content-rail)은 남은 항목이 없어 **제거** — ☰ 토글의 레일 연동
코드에 null 가드(노드 없어도 ☰ 정상). 퀘스트 배지(refreshClaimBadges 의
[data-modal="quest"] 쿼리)는 드로어 이동으로 계속 작동. CSS 는 역사 보존.

**검증**: smoke(v5.291 회귀 갱신 — 첫 6순위 정합·17개·레일 부재) + 실물
헤드리스(메뉴 순서 실측 일치·퀘스트 클릭 → 퀘스트 모달 오픈·픽셀 판독).

### 2026-09-13 v5.293 부록 — 게이트 우회 미수 사고(교훈 기록)

v5.293 작성 중 `npm run check`의 출력을 grep으로 필터링하는 바람에 verify [E]
실패(제거된 #content-rail 조회 잔존)가 종료코드로 전달되지 않은 채 커밋했다.
**pre-push 훅이 push를 정확히 차단**해 라이브는 무결했고, 참조 제거+amend로
정정했다(배포본 8001c91). 재발 방지: **check 결과는 파이프 종료코드로 판정하라 —
`npm run check; echo $?` 또는 `&& ` 체인에서 grep 을 끼우지 않는다.** grep 은
매치를 찾으면 성공을 반환해 실패를 덮는다. 이 저장소의 게이트 사고 유형
(_patch 잔여·사본 덮어쓰기·이번 종료코드 상실)은 전부 '게이트 출력을 사람이
읽고 해석하는 단계'에서 났다 — 자동화 구간(훅·CI)은 매번 막아줬다.

### 2026-09-17 v5.294 — 콘텐츠 확장 루프 개시: 잔불의 미궁(신규 던전 · 자체 설계)

**배경**: 대표가 "콘텐츠 확장 루프(자체 설계 플래그로 영웅·던전·이벤트 추가)로
전환" 지시. 경쟁력 판정(뼈대는 실측 강건·완제품은 아님)에 따라 던전→이벤트→영웅
순서로 확장 — 영웅은 곡선·상점·합성 전반을 건드리는 가장 침습적 축이라 나중.

**설계(자체 설계 — ⚠비전미확인 해소, 근거는 game.js EMBER_MAZE 주석 ①~④)**:
위험 수준 선택 던전. 문 3개(잔불 8천/타오르는 1만6천/굶주린 불꽃 2만6천) —
게임 고유의 위험-보상 언어(위험강화)를 일일 던전으로 이전. 보상은 강화석
40/90/180(위험당 약 2.2배)+골드 100만/300만/1000만 raw. 골드는 골드던전 5단계
(1500만) 이하로 봉쇄, 진입 일 1회 무료(dailyLeft('ember',1))로 경제 봉쇄.
보상은 승리 시에만(showDungeonResult rewarded 게이트) — 패배하면 재화 소모
없이 기회만 소진. 최고 문 기록 stats.emberBest(freshState 추가 — ddStage 관례).

**구현**: EMBER_MAZE/EMBER_REWARD_TXT 상수 · embermaze 모달(모험 팝업 7번째,
일 1회 남은 회수·적 전투력·보상 표시, 입장 styledConfirm [예]에서만 dailyUse) ·
전투는 enterDungeonFight kind:'mobs'(v5.292 centerHold 연출 경로) —
렌더 무지급 원칙 준수(표시는 상수만).

**검증**: smoke 신규 스텝(3문 구조 단조·렌더 무지급·일 1회 게이트 — 버튼 클릭만
으로 미소진·[예] 확정 시에만 소진·reward 콜백 raw 지급·emberBest) + 모험 회귀
7종 갱신. 시뮬 전후(시드 42·600h): CP 440,373→459,744(+4.4%) · 최장 공백
15.0h→11.5h · 200~600h 밀도 11.3→12.8/일 · **기록서 0→6권(심화 각성 재화가
강화석 유입으로 해금 — 막혀 있던 탑→심화 루프 풀림)** · 결정가호 30→52회 ·
골드 보유는 30M→18M(신규 골드가 기존 싱크에 흡수 — 인플레이션 없음) · 세이브
4.6→4.7KB. 문 해금 곡선: 24h 1문→48h 2문→96h 3문(초반 4일 성장 서사).

### 2026-09-17 v5.295 — 콘텐츠 확장 루프-2: 주간 축제(이벤트 시스템 · 자체 설계)

**설계(근거는 game.js FESTIVALS 주석 ①~④)**: 리듬 3층위(일일→주간→월간)에 '주간 변주'가
없어 주 내내 동일한 하루가 이어졌다. 주간 의뢰와 같은 경계(ISO 주·월요일)로 3테마가
순환 — 골드 축제(골드 획득 +20%)·성장 축제(처치 경험치 +20%)·채굴 축제(사냥터 재료
드랍률 +20%). 로테이션은 주 번호 %3 — 결정론적이라 상태 추가가 없고(마이그레이션 무관)
시뮬(FakeDate 1970 에폭) 재현성도 유지된다.

**구현 관문 3곳(전부 기존 승수 관문 관행 — 기본 수치·확률 테이블 불변, 실측 경제 보존)**:
addGold 의 non-raw 체인(골드 — raw 계약 보상 제외) · 킬 경험치 expMul(성장 — 벤치 50%
분배가 상속) · 드랍 확률 p(채굴 — 점령전 dropBuff×1.15 와 같은 곱셈 레이어. 수량이 아니라
확률인 이유: 일반 처치 드랍이 1개라 ×1.2 반올림이 효과를 지움). UI: 전장 좌상단
#festChip 배지(refreshHUD 5초 갱신·클릭 시 공지) · 공지 신규(미열람 점 자동 점등) ·
도움말 '축제' 토픽(정본 파생) · 주 롤오버 시 토스트 1회(신규 세이브 최초 초기화 제외).

**검증**: smoke 신규 스텝(3주 순환 결정론·골드 관문 배율 비율 1.2 — 잔여 버프가 목 주차
사이 오염시키는 함정 발견·측정 구간 buffs 중립화로 해결·raw 불변·관문 3곳 소스 정합·
롤오버 토스트 스파이·배지 렌더 무지급). 시뮬(시드 42·600h): CP 459,744→455,994(−0.8%
— 시드 분산 433k~532k 대비 노이즈, **곡선 불변 = 축제는 변주이지 인플레가 아님을 실증**) ·
리더 Lv 223→230(성장 축제 효과 실측) · 행동 7/7종 유지 · 세이브 4.6KB. 12800h 장기 완주:
CP 3,402,121→3,544,518(+4.2% — 결정 구매 싱크 34,860M→38,250M 로 흡수) · 결정 ✦31(✦30
수준 유지 — 장기 드리프트 없음) · 리더 Lv 1102→1145 · +25 10부위 · 세이브 10.2KB · 벽 없음.

### 2026-09-17 v5.296 — 실물 QA(잔불·축제) 10항목 통과 + 축제 배지 3px 겹침 수정

**실물 헤드리스 QA(라이브 v5.295 · 확립 우회로)**: ① Ver 5.295 ② 축제 배지 텍스트가 실계산
테마와 일치(2026-W38 '성장 축제') ③ 배지 클릭→공지 오픈+축제 공지 존재 ④ 도움말 '축제'
토픽 본문(3테마 목록+'이번 주' 정본 파생) ⑤ 잔불 모달 3문·보상 3종·입장 버튼 3개·잔여 1
⑥ 입장 [예]→전투 개시 ⑦ 일 1회 소진(잔여 0) ⑧ 소진 후 '오늘 완료'+disabled ⑨ 주 롤오버
토스트 1회 발화·최초 초기화 미발화·재호출 중복 없음 — **전부 통과**.

**결함 1건 발견·수정**: 축제 배지(#festChip top:44px)가 시계 박스(timepod bottom 47)와
**3px 포개짐** — 실물 rect 실측(배지 top 124 vs 시계 bottom 127)으로 확인. top 44→50으로
수정(3px 여유). 스크린샷 판독에서 배지가 안 보였던 건 별개 원인: 신규 세이브의 튜토리얼
오버레이(ob-step)가 최상위였고, seenTutorial=true 상태에선 배지가 최상위로 정상 노출·
클릭 도달(elementFromPoint=festChip 실측) — 결함 아님.

**교훈**: 스크린샷 목격 판독은 온보딩 상태(오버레이)에 좌우된다 — 신규 세이브로 찍으면
온보딩 레이어가 젊은 화면을 가린다. 배치 결함 판정은 rect 실측(격차·elementFromPoint)이
먼저고 스크린샷은 보조다.

### 2026-09-17 v5.297 — 콘텐츠 확장 루프-3: 축제 의뢰(이벤트 심화) + 영웅 추가 에셋 조사

**영웅 추가 조사 결론(아트 의존 — 보류)**: assets/heroes/sheets/ 는 9영웅과 정확히 1:1
(earth_n~shadow_r 9종, 여분 시트 없음). 새 영웅은 새 스프라이트시트·초상 없이는 못 만들고
이 환경에서 게임 아트 생성이 불가하므로 **영웅 축은 에셋 확보 전까지 보류** — 대표에게
에셋 제작 요청이 필요하다(요청 사양: 직업별 신규 스프라이트시트 1세트[9~10 동작] + 초상 1장).

**축제 의뢰(구현)**: 주간 의뢰 5번째 슬롯(id 'fest') — 축제 테마에 상응하는 행동을 유도해
버프+목표가 한 세트가 되게(골드 축제=처치 6,000 · 성장 축제=소환 20 · 채굴 축제=제작 25).
보상 강화석 50(주 1회·기존 4종 보상 위상 대등·잔불 3문 180/일 대비 보수적). 상태 추가 없음 —
진행 축은 weeklyState 스냅샷 base에 이미 전부, 수령 플래그는 w.claimed['fest'] 동적 키
(daily counts 관례). 주 경계=축제 교체(같은 ISO 월요일)라 테마-의뢰 정합 자동 보장.
렌더는 WEEKLY_QUESTS.concat([festivalQuest()]) 재사용, 표시 텍스트만 FEST_REWARD_TXT 상수.

**검증**: smoke 신규 스텝(3주 Date 목으로 테마×축×목표 매핑·주간 탭 렌더 — el() 3번째
인자가 _html 인 스텁 함정·렌더 무지급·수령 +50·weeklyClaimable 반영). 시뮬(시드 42·600h):
CP 455,994→466,103(+2.2%) · **최장 공백 15.5h→10.0h 개선** · 행동 7/7종 · 세이브 4.7KB.
12800h 장기 완주 수치는 커밋 메시지 참조.

### 2026-09-17 v5.297 부록 — '대장장이의 눈물 0개' 병목 조사: 시뮬 합성 소각 (게임 결함 아님)

**증상**: 600h 시뮬 진단 E탐침에 '부족=[대장장이의 눈물 0/20]' 상시 — E 장비 3종 제작 불가처럼
보였고, 재료 상위 5에는 특정 재료만 900 상한 도달(눈물 0)로 치우침.

**조사 결론(게임 무결 — 시뮬 도구 정확도 결함)**:
- 획득 경로·UX 완비: E 사냥터 n=1 몬스터 고정 드랍 35% + 요일던전 matGainGrade 등급 균등
  실체화(6000회 직접 검증: 6종 각 ~1000회로 균등) + 재료 칩→드랍 몬스터 지목→사냥 이동
  팝업(openMatMonsterPopup, v5.111/v5.163). 플레이어 관점 병목 아님.
- 범인은 balance-sim synthStep: R→E(0.8%)·E→L(0.08%) 합성을 재료가 30개를 채우는 족족
  무조건 돌려 E 재료를 소각 — 요일던전으로 받은 눈물이 항상 0~29에서 진동.
- 합리적 플레이어는 기대손해(30개→성공 기대 0.008/0.0008개) 합성을 회피한다.

**정책 수정(3안 비교 실측)**: ①종전(전합성 무조건)=눈물 0 · ②첫 수정(N→R만)=E탐침 해소
그러나 R 재료 전종 2000 포화 '댐' 발생 — 상급재료 381→228창·최장 공백 10.0→23.5h로 행동
다양성 붕괴(과교정) · ③**채택: N→R(50%) 상시 + 상위 합성은 원재료가 보유 상한의 90%를
넘었을 때만**(초과분은 어차피 버려지는 재화 — 재활용의 기회비용 0).

**전후(시드 42·600h)**: E탐침 4종 전부 부족=[] 해소 · 재료 상위 5 균형 회복(성수 1241·
서리결정 1199 등 댐 없음) · CP 466,103→454,000(−2.6% 노이즈) · 액션 474→454회·최장 공백
10.0→18.5h — 종전 밀도가 '소각→부족→다양한 사냥' 인위 순환으로 과대측정됐던 것이 정직해진
것(게임 무변경). **이 정책 변경으로 시뮬 기준선이 리셋됨 — 이후 전후 비교는 2026-09-17
이후 기록 기준.** 게임 소스 무변경(버전 bump 불요) — 합성 확률(G-31 실측)도 불변.

**교훈**: '진단 라인의 병목 표시'는 수급 결함이 아니라 시뮬 정책의 자기발생 소비일 수
있다 — 원인 좁히기는 ①경로 존재 확인(게임 코드) ②관문 균등성 직접 검증 ③소비처 점검
순으로. 또한 정책 수정은 과교정 부작용(댐)까지 실측해 3안 비교로 확정한다.

**새 기준선(본 정책 · 시드 42)**: 600h CP 454,000·공백 18.5h·액션 454회 / **12800h CP
3,544,518 · 결정 ✦31 · 세이브 10.2KB · 액션 9,232회 — 재료 상위 5에 '대장장이의 눈물 900'
등장(0→상한 도달, 소각 게이트의 장기 실증) · 벽 없음.**

### 2026-09-17 시뮬 — 새 기준선 시드 로버스트니스 10시드 재실증 + 세트 빌드 CP 격차 발견

**배경**: 저확률 합성 소각 게이트(직전 부록)로 시뮬 기준선이 리셋됐다 — "벽 없음" 결론이
새 정책 위에서도 시드에 강건한지 v5.282 관례대로 재검증.

**10시드 600h 결과(seed=1·7·42·99·123·512·1024·31337·65535·99991)**:
- 등급 도달: N 0.5h 전 시드 동일 · R 10~12h · E 48.0~50h(일관) · L 59.5~110h(분산).
- CP: 9개 시드 454k~561k(중앙 ≈ 시드 42의 454k) — **seed=99만 1,093,610 이상치**.
- 최장 공백: 전 시드 ≤22.0h — 벽 없음 결론 유지(v5.282 상한 ≤23h와 동일 수준).

**seed=99 이상치 원인(결함 아님 — 세트 빌드 격차)**: 리더가 강철맹세 6세트(성좌 수호계,
dmg30+def40)+작열 3세트 조합에 수렴 → 세트 배율 ×5.333(다른 시드의 작열6+주술3 ×2.286).
def40의 유효 체력 환산(v5.6 방어 세트 전투력 기여 설계)이 곱셈으로 얹혀 조합 간 ×2.3
CP 격차가 난다. 위험강화 성과(228시도/124성공)는 타 시드와 유사 — 세트 조합이 유일한
차이. 시뮬 setAffinity 정책(진행 중 세트 완성 유도)은 합리적 근사, 초기 재료 운으로
수렴 세트가 갈리는 정상 분산.

**시사점(게임 무변경·기록)**: 세트 조합 연구('강철맹세+작열3 메타')가 실 플레이어에게
×2.3 전투력 우위를 준다 — 빌드 조사 재미로 존치. 공략 카드는 '세트 6세트 노린다'
수준만 안내해 정보 비대칭이 있으므로, 추후 공략 카드에 세트 조합 가이드를 추가할
가치(별건·미결정). 최장 공백 18.5h(@355h)의 정체도 규명: 336h 일일 콘텐츠 소진 후
360h(다음 시뮬-일)까지 방치 레벨링만 하던 구간 — 시뮬-일 경계+dailyStep 하루 1회의
구조적 공백으로 게임 결함이 아니다.

### 2026-09-17 v5.298 — 세트 전투력 배율 정보 공개: 공략 카드 현재 배율 + 세트 도감 단계별 ×N

**배경**: 10시드 스윝에서 발견한 세트 빌드 격차(작열6+주술3 ×2.29 vs 강철맹세6+작열3
×5.33)는 빌드 연구 재미로 존치하되, 효과 문구만으론 '받는 피해 40% 감소'의 전투력 체감이
불가해 정보 비대칭이었다(직전 부록의 시사점 후속).

**변경(정보 공개만 — 세트 효과 수치·setDamageMul 공식 불변, 밸런스 무변경)**:
① 공략 카드 세트 라인에 '현재 배율 ×N'(setDamageMul 정본 접근자 — 표시가 코드와 어긋날 수
없음) + 도급 안내. ② 세트효과 도감(setfx) 카드의 각 단계에 단독 전투력 환산 배지 ×N
(공격% × 방어[유효 체력] 환산 — setDamageMul 단일 세트 계산과 동일 공식)과 합산 규칙
안내. 최적 조합 강제가 아니라 '비교 가능한 정보'로만 제공.

**검증**: smoke 공략 카드 스텝 확장(표시 배율 = setDamageMul 실측 정합·setfx 배지 11개
[작열 3단계+8종 1단계]·규칙 안내) — 전체 통과. 시뮬 무관(표시 전용).

### 2026-09-17 v5.299 — 체감 소폭 2종: 축제 D-N 카운트다운 + 잔불 최고 문 기록 표시

**① 축제 배지 D-N**: festChip 끝에 다음 교체까지 남은 일수(daysToWeeklyReset 정본 —
주간 의뢰와 같은 경계)·title 툴팁·도움말 축제 토픽에 잔여 일수 추가. '다음 주는 무엇의
주인가'를 기다리게 하는 카운트다운 — 로테이션 콘텐츠의 기대감은 교체 시점을 알 때 성립한다.
max-width 170→190(카운트다운이 잘려도 무해하도록 텍스트 우선).

**② 잔불의 미궁 최고 문 기록**: stats.emberBest(1~3)를 모달 헤더에 '최고: O문 완주'로
표시 — 위험 선택 던전의 최고 기록이 통계에 갇혀 게임에 안 보였다. 반복 도전 욕구 자극.

**검증**: smoke 갱신 — 배지 D-N 정규식·도움말 토픽 본문 잔여 일수(appendChild 방식은
collectText 로 자식까지 — _html 직접 읽기 오탐 교정)·잔불 최고 기록 3문/0문 경계.
전체 통과. 시뮬 무관(표시 전용).

**캐주얼 2400h 기준선 갱신(v5.299 시점 — 소각 게이트+콘텐츠 4종[잔불·축제·축제 의뢰·세트 정보] 반영)**:
N 0.5h · R 24.5h · E 97.0h · L 169.0h · CP 773,443 · 각성 +47 · 세이브 5.5KB · 최장 공백 7.5h
· 액션 5.9/접속일 — 종전 캐주얼 기록(R 25h·E 100.5h·L 169h, v5.280)과 ±3h 내 동일:
신규 콘텐츠 4종이 캐주얼 곡선을 왜곡하지 않음(정보·리듬 추가는 세그먼트 무관 유지).

### 2026-09-17 루프(밤) — 전 모달 46종 실물 렌더 스윕 재실행: 결함 0건

**배경**: 루프97(45종 스윕) 이후 모달 1종 추가(잔불)+공지 1건+도움말 토픽+공략 카드 확장
+주간 5행+세트 배율 배지+v5.299 요소까지 화면이 많이 바뀌어 방어적 재스윕.

**결과**: 46종 openModal 전수 — 1차 스윕 이상 11건(VP 7+OV 4) → 420ms 안정화 2차 재측정에서
**전부 무결** 확정: VP 7건은 pop .17s 팝인 중간값 오탐(루프97 패턴 ①재현), optionReroll '↻'
sh36>ch30·settle '06:01' sh52>ch48·arena 'ⓘ'/'1,088' 1~4px 세로 초과는 줄높이 무해 패턴
(루프97 패턴 ②, 수평 sw==cw — 잘림 없음). **수평 텍스트 오버플로 0건·뷰포트 이탈 0건.**

신규 요소(embermaze·adventure·setfx 배율 배지·strategy 확장·quest 주간 5행·notice 신규
공지·help 축제 토픽) 전부 ok. 게임 무변경 — 기록만.

### 2026-09-17 v5.300 — 콘텐츠 확장 루프-4: 용광로 시련(월 1회 이벤트 보스전 · 자체 설계)

**설계(근거는 game.js FORGE_TRIAL 주석 ①~④)**: 리듬 3층위 중 월간 레이어에 '도전형 이벤트'가
없었다(월간 의뢰 4종은 전부 수령형). 잔불의 미궁(일 1회)의 월간 상위판 — 고정 foe 30,000
(잔불 3문 26,000의 위) 단일 보스, kind:'boss' 중앙 배치 연출(v5.292 경로). 패배하면 그 달
기회 소진, 다음 달 재도전. 진입 게이트는 monthlyState().claimed 동적 키 — 새 달 claimed
통째 리셋으로 별도 상태·마이그레이션 불필요. 보상 강화석 300+골드 1,200만 raw(잔불 월
5,400 대비 5.5%·골드던전 5단계 이하 봉쇄 유지), 승리 시에만 지급·렌더 무지급.

**구현**: FORGE_TRIAL/FORGE_REWARD_TXT 상수 · forgetrial 모달(47번째 — 모험 팝업 8번째,
nav_forge 아이콘) · monthlyState 리셋 재사용.

**검증**: smoke 신규 스텝(구조·렌더 무지급·버튼 클릭만으로 미소진·[예] 확정 시에만 소진·
kind:'boss'·reward raw·소진 라벨) + 모험 회귀 8종 갱신 — 전체 통과. 시뮬(시드 42·600h):
첫 클리어 96h(CP 25,016 ≥ foe/1.5 시점) · CP 454,000→539,359(+18.8% — 경제 기여는 설계상
미미[월 강화석 300]이라 대부분 궤적 변화: 새 액션이 RNG 소비를 바꿔 세트 수렴 등이 갈림.
시드 분산 454k~1,093k 범위 내) · 세이브 4.8KB. 12800h 장기 수치는 커밋 메시지 참조.

### 2026-09-17 시뮬 — 세트 선호 정책 v5.298 정합: 배율 가산(진행 중 세트 한정) + 이상치 해소

**배경**: v5.298로 세트 배율(×N)이 게임에 공개됐다 — 시뮬의 합리적 플레이어 근사
(setAffinity)도 그 정보를 반영해야 정확하다. 종전 정책은 착용 수(진행도)만 봤다.

**정책**: 진행도가 지배하고, 1조각 이상 진행 중인 세트에만 최고 단계 배율 기여(×N−1)
가산. 0조각 세트에 배율을 주지 않는 이유 — 새 세트를 0부터 만드는 비용은 재료가 지배하며
실제 플레이어는 4조각 모은 세트를 버리고 최고 배율 세트를 새로 만들지 않는다. 배율 산식은
setDamageMul 단일 세트 계산과 동일.

**실측(600h)**: 시드 42 — 궤적 완전 불변(CP 539,359 동일·×2.714 주술6+작열3 → 12800h
재실행 불요). **시드 99 — 이상치 해소: CP 1,093,610(×5.333 강철맹세6+작열3) → 464,109
(×2.286)**. 시드 7·512 ×2.286 유지. 분산 454k~1,093k → 454k~539k로 축소.

**해석(게임 무변경)**: '정보를 보고 동률 시 배율을 고르는' 플레이어의 표준 궤적은
×2.29~2.71 — ×5.33 조합(강철맹세+작열 동시 진행)은 의도적으로 노리는 빌드 연구자만
도달한다. 즉 세트 빌드 연구 보상(×2.3 우위)은 유효하되 자동 수렴은 아님 — v5.298 정보
공개와 v5.297 격차 기록의 정합적 후속. 콘텐츠 확장 4종(잔불·축제·축제 의뢰·용광로)의
600h 기준선은 이 정책에서도 유지(시드 42 불변).

### 2026-09-18 루프 — 용광로 시련 실물 전투 완주 QA: 승리 경로 3회 실증 + 보스 순삭 관찰

**실물 완주(라이브 v5.300 · 고전투력 세이브 3케이스)**: 리더 CP 300,070 / 29,500 / 14,500
(마지막은 foe 30,000 대비 열세 0.48배) — **3회 전부 승리·보상 정확 지급**(강화석 +300·골드
+12M raw·sysLog)·claimed 소진·'이번 달 완료' 라벨 전부 확인. 월 1회 게이트·rewarded
경로의 실물 실증 완료.

**관찰(밸런스 후보 — 미조치)**: 3회 모두 전투가 2.5~3초 내 종료(접전 프레임 목격 불가).
원인은 보스 HP 공식 `max(300, foeCP×0.5)` — 용광로는 HP 15,000이라 파티 DPS에 순삭.
이는 전 보스 던전 공통 관행이라 오늘 변경 보류(기존 보스 밸런스 전수 재측정 필요).
시사점: '월간 시련의 긴장감(패배 위험)' 설계 의도가 체감에 안 나옴 — 열세(0.48×)에서도
이긴다는 건 표기 전투력과 실제 난이도의 괴리. **다음 루프 후보**: enterDungeonFight cfg에
hpMul 옵션 추가(기본 1 — 기존 던전 불변) 후 용광로만 hpMul 3(HP 45,000, 10~20초 전투·
패배 가능성)로 긴장감 회복 — 자체 설계 신규 콘텐츠의 파라미터 조정이라 규칙상 가능.
시뮬 임계(foe≤CP×1.5)는 실제보다 보수적이라 곡선 측정에는 안전(영향 없음).

### 2026-09-18 v5.301 — 용광로 보스 장기전화: hpMul cfg(용광로만 ×3, 기존 던전 불변)

**배경**: 직전 루프 실물 QA 실측 — 보스 HP 공식(foeCP×0.5)이 파티 DPS에 2.5~3초 컷이라
'월간 시련의 긴장감' 설계 의도가 체감에 안 나왐음(0.48× 열세에서도 승리).

**변경**: startDungeon cfg에 hpMul 옵션(미지정 1 — 기존 던전 공식·D 시나리오 해시 불변,
D1 실측 동일) 추가, spawnDgBoss HP에 곱 적용. 용광로 시련만 hpMul 3(HP 15,000→45,000,
45초 내 처치) — 안내 문구 '장기전(HP 3배)'. 신규 콘텐츠 파라미터 조정(자체 설계)이며
기존 보스 던전 수치는 불변(실측 관행 보존). 시뮬 정책(임계값 근사)은 hpMul 무관 —
곡선 무변화(600h 동일성 실측).

**v5.301 실측(라이브·0.2초 폴링 전력 스윕)**: 리더 CP 3,100 → **9.8초 접전 후 승리**
(hpMul 3 작동 실증 — 첫 스킬 버스트로 못 죽이고 전투 지속), CP 9,100/18,100/45,100 →
0.6~0.9초 순삭. 파티 DPS가 개전 첫 버스트(스킬 4종 전개+크리)로 폭발하는 구조라
성장 후 순삭은 불가피하며 잔불 3문과 같은 자연 보상 곡선 — 추가 상향(×10 등)은
성장한 플레이어의 월 1회 기회를 패배로 묶을 위험(다음 달까지 재도전 불가)이라
**현재 ×3 유지 판정**. '긴장감'은 도전 적기(초중반)에만 존재하면 된다는 설계 결론.
시뮬 600h CP 539,359 동일(hpMul 무관·곡선 무변화 실증) · D1 해시 불변.

### 2026-09-18 v5.302 — 용광로 시련 D-N 카운트다운(다음 재도전까지)

소진/패배 후 헤더에 '다음 재도전까지 D-N'(daysToMonthlyReset 정본 — 월간 의뢰와 같은
경계). 월 1회 콘텐츠의 대기(최장 30일)가 공백이 아니라 기대로 남게 — 축제 배지 D-N
(v5.299)과 같은 어휘 통일. 표시 전용(밸런스·시뮬 무관). smoke: 소진 렌더 D-N 정규식 추가.

### 2026-09-18 루프 — 첫 로드 성능·에셋 404 감사: 무결 (게임 무변경)

**첫 로드 성능(라이브 3회)**: game.js 실전송 238KB(gzip — 원본 510KB의 47%) · DCL
121~534ms · load 180~758ms(변동의 대부분은 TTFB 42~249ms=네트워크) · 로드 완료 시점 게임
즉시 ready(S·enterHome 접근 가능). 스크립트는 body 끝 동기 로드라 렌더 블로킹 없음.
**최적화 불필요 판정** — 병목은 코드가 아니라 네트워크. (참고: 주석 제거 시 373KB지만
gzip 전송 238KB면 개선 여지 미미 — 주석은 이 저장소의 핵심 자산이므로 유지.)

**에셋 404 감사(라이브)**: 전 모달 47종 순회 + 홈 사냥 + 던전 전투(스프라이트) 중
이미지 로드 실패 **0건** — requestfailed·4xx 응답 감시.

### 2026-09-18 루프 — 캐주얼 12800h 새 기준선 재측정(✦29·격차 ±2 유지) + 강화석 장기 잔여 원인 규명

**캐주얼 12800h(소각 게이트+세트 선호+콘텐츠 4종 기준선)**: 결정 ✦29(풀 ✦31 — 격차 ±2,
구 기록 ✦27의 패턴 동일) · CP 2,033,957(풀의 48%) · +25 10부위 완주(평균 25.0) · 세이브
10.2KB · 액션 7.4/접속일 · 완주(정체 중단 없음) — **캐주얼 초장기 무결·세그먼트 공정성 유지**.
강화석 잔여 125,610 ≈ 풀 125,933 — 수급처(잔불·축제 의뢰·용광로)가 접속 무관 일/주/월
리듬이라 잔여도 세그먼트 무관.

**강화석 장기 잔여 원인(관찰·조치 보류)**: 결정(✦) 비용 = 기록서 (20+5k)권 + 강화석
(300+40k)개 — ✦31까지 누적 강화석 소비 ≈ 27,900개로 잔여(12.6만)의 22%뿐. **결정 구매가
기록서 병목(탑 상자 수급)으로 느려 강화석이 자연 축적**되는 구조. 밸런스 판단: 12,800h(≈533일)
시점의 재화 포화는 종반 자연 상태이고 소비처 상향은 기존 ✦ 진행자에게 역진 — 신규 소비처
(예: 강화석→기록서 교환)는 심화 각성 속도(곡선)를 바꾸는 밸런스 변경이라 별도 설계 루프로.

### 2026-09-18 v5.303 — 신규 콘텐츠 2종 통합 공지(잔불의 미궁·용광로 시련)

잔불(v5.294)·용광로(v5.300)가 모험 팝업 '발견'에만 의존해 공지가 없었다(축제만 공지됨).
통합 안내 공지 1건을 최상단에 추가 — 미열람 점 자동 점영(noticeSeen 관례)으로 기존
이용자에게 능동 알림. 입장 조건·위험·보상 요약 + 축제 배지 안내 포함.
smoke: NOTICES[0] 검증 갱신(신규 콘텐츠 공지)+축제 공지 존재 검사(some).

### 2026-09-18 v5.304 — 도움말 '시련' 토픽(잔불의 미궁·용광로 시련·축제 의뢰 안내)

신규 던전 2종(잔불 v5.294·용광로 v5.300)이 도움말에 없었다(토픽 7종은 구 콘텐츠 중심).
'시련' 토픽 추가 — 문별 적 전투력(EMBER_MAZE)·용광로 foe/HP 배율/보상(FORGE_TRIAL)·
축제 의뢰 목표(festivalQuest) 전부 정본에서 파생(도움말 수치 파생 관례). 표시 전용.
smoke: 도움말 '시련' 토픽 존재 검증 추가.

### 2026-09-18 시뮬 — 재미 지표 '시련' 종류 분리(7→8종): 확장 시리즈의 기여 가시화

콘텐츠 확장 시리즈(잔불 v5.294·용광로 v5.300·축제 의뢰 v5.297)가 전부 '일일콘텐츠'에
묶여 재미 종류 분포에서 기여가 안 보였다. '시련' 종류 분리(잔불미궁|용광로시련|축제의뢴) —
600h 재측정: **시련 25창(4%) 분리 표시 · 8/8종 · CP 539,359 불변**(집계 변경만, 궤적 무영향).
이제 신규 콘텐츠의 재미 기여가 독립적으로 관측된다. 게임 무변경.

### 2026-09-18 시뮬 — 캐주얼 8종 분포 실측: 세그먼트 무관성의 종류 축 확립

'시련' 분리 직후 캐주얼 600h 재측정 — **8/8종 커버**(시련 23창·6%, 풀 25창·4%보다 접속
시간 대비 비율이 높음). 잔불·용광로·축제 의뢰가 접속 무관 일/주/월 리듬이라 캐주얼에게도
동일하게 주어진다는 설계의 종류 축 실증 — "접속 1/3으로도 전 행동 종류를 경험".
곡선(R 24.5h·E 97h·L 169h)은 2400h 기록과 동일·최장 공백 2.5h. 게임 무변경.

### 2026-09-18 시뮬 — 새 기준선 25600h 초장기 재검증: 완주·✦48·벽 없음

구 25600h 기록(✦47·CP 503만)이 구 정책 기준이라 새 기준선(소각 게이트·세트 선호·콘텐츠
4종·hpMul)에서 재검증: **완주(정체 중단 0) · CP 6,228,929 · 결정 ✦48 · 리더 Lv 1,624 ·
세이브 16.1KB(상한 5MB의 0.3%) · 액션 17.7/일 · 최장 공백 18.0h.** 결정 축 속도는 구 기록
(✦47)과 동일 수준 유지 — 정책 교체가 엔드게임 속도를 왜곡하지 않음. CP +24%는 궤적
변화(콘텐츠 4종 보상+세트 수렴) 분. 초장기(≈1,067일) 무결성까지 새 기준선이 커버.

### 2026-09-18 v5.305 — 재화 사슬 감사(대표 요청) + 나쁜 막힘 1건 개선: 주사위→소환서 교환

**감사 방법**: 실물 가이드 체인 실주행(신규 세이브·재화 잔고 추적) + 코드 전수 조사(획득·
소비 지점) + 시뮬 3각 검증. 결과는 **docs/design/20260918-재화사슬-감사.md**(재화 11종의
획득→사용→사슬→수명→없으면 판정 + 잘/나쁜 막힘 목록 + 내일 다시 오기의 기둥).

**발견한 나쁜 막힘 — 주사위 소비처 단선**: 획득처는 다양(일일 미션·투기장 승+3·가이드·출석)
한데 소비가 옵션 리롤 하나(N5~L50/부위) — 옵션 장비는 중반+부터라 초중반 주사위는 쌓이기만
하는 '못 쓰는 재화'. **개선: GOLD_SHOP dice 통화 라인 '영웅 소환서 X3 = 주사위 90개'**
(환율 근거: 소환서 골드 정본가 150만/장×3=450만 상당 ≈ L리롤 1.8회분 — '리롤 2회 포기=
소환 3장'의 직관적 트레이드오프). 잉여가 성장 축(소환→조각→합성)으로 흐르게 사슬 연결.

**검증**: smoke 골드상점 스텝 확장(dice 상품 1개·90개·지급 +3) 통과 · 시뮬 정책(리롤 예산
200 보유 초과분 교환) 600h: CP 539,359→558,649(+3.6% — 교환 3회의 완만 가속, 노이즈
상단)·세이브 4.7KB. 잘 만든 막힘(재료 파밍 팝업·기록서→탑 유도·용광로 D-N·축제 D-N)은
현행 유지 판정.

### 2026-09-19 v5.306 — 시간 의존 경로 전수 조사: 투기장 주간 롤오버 세이브 결함 수정

게임 내 `Date.now()`/`new Date()` 40곳을 달력 경계형(주·월·요일·일자)과 경과시간형으로
분류해 smoke 회귀 커버를 대조했다. 대부분 커버됐으나 **투기장 주간 롤오버(매주 월요일
12시 — 원작 안내문 N2 벤치마크로 의뢰·축제의 ISO 월요일 00시와 경계가 다르다)만
무커버**였다.

- **smoke 신규 스텝** '투기장 주간 롤오버': 경계 산술(월 11:59=전 주·12:00=새 주,
  arenaWeekKey 인자 주입으로 결정적) · 구세이브 ''봉인(즉시 초기화 금지) · 롤오버 리셋+
  토스트 1회 · 같은 주 재진입 금지 · 키 변경 두 경로의 즉시 save(save 스파이).
- **실물 clock QA(T3/T4)** 가 결함을 잡았다: 리셋 토스트는 떴지만 **세이브에 반영이 안
  됐다** — arenaWeekRoll 이 S만 바꾸고 save()를 안 호출. 리셋 직후 창을 닫으면 재접속마다
  '랭킹이 초기화되었습니다' 토스트가 반복됐다(점수 리셋은 멱등이라 실피해 없음). 키가
  바뀌는 두 경로(봉인·롤오버)에 즉시 save 추가 — weeklyState/monthlyState 롤오버 관례와
  정합. 수정 후 T4 실측 pts=0·주차 '2026-9-21' 세이브 반영 ✓.
- 무해 판정(미조치 기록): 광고 제단 2슬롯 로테이션(ADPOOL)이 `getDate()` 기준이라 31일→
  1일에 같은 슬롯이 하루 반복된다 — 하루치 추천 상품 반복일 뿐 경제·진행도 영향 0.
- 밸런스 무변경(세이브 반영 시점만 수정)이라 시뮬 수치 영향 없음.

검증: smoke 신규 스텝 통과 · 실물 QA 6시나리오(T0~T4·29검사) 전부 통과 · npm run check
전체 통과. QA 스크립트는 docs/qa/20260919-미래시각-롤오버-실물QA.cjs(내부 보관).

### 2026-09-19 v5.307 — 오프라인 정산 [수령] 즉시 저장: 중복 수령 결함 수정

v5.306에 이은 시간 의존 경로 조사의 경과시간형 잔여 — **오프라인 정산 settle 모달의 UI
경로**(자동 오픈·수령)만 실물 미검증이었다. Playwright 실물 QA(T5: 3일 미접속 세이브
부팅)를 만들어 돌린 결과 결함 발견:

- **정상 확인**: 부팅 450ms 후 settle 자동 오픈 · 3일치가 8h 상한 4,532,640(453.3만)·
  480:00 표기로 정확 · [수령] 클릭 시 모달 닫힘·지급(addGold 관문의 칭호·티어 곱 반영
  5,986,668)·offlinePending 소진.
- **결함**: 수령 onclick이 상태만 바꾸고 save()를 안 호출 — 컴백 유저가 [수령]을 누르고
  주기 저장 전에 창을 닫으면 세이브에 offlinePending이 남아 재접속에서 **같은 금액을 또
  수령**할 수 있었다(중복 지급 가능). v5.306 투기장 롤오버와 같은 '상태만 바꾸고 저장 안
  함' 유형 — 이 유형이 2건 연속 나왔으니 이후 상태 변경 onclick 리뷰 시 save 호출부터
  확인할 것.
- **수정**: 수령 onclick 마지막에 save() 추가. smoke 신규 스텝 '오프라인 정산 수령 —
  지급·소진·즉시 저장'(modal render → 버튼 클릭 → save 스파이).
- 밸런스 무변경(세이브 시점만) — 시뮬 곡선 영향 없음.

검증: smoke 전체 통과 · 실물 QA 7시나리오 36검사 전부 통과 · npm run check 전체 통과.

### 2026-09-19 v5.308 — 소진·수령 확정 즉시 저장 전수 시정: 상한 우회·무한 수령 창 폐쇄

'상태만 바꾸고 저장 안 함' 유형이 v5.306(투기장 롤오버)·v5.307(오프라인 수령) 2건 연속
나와 재화·플래그 변경 지점 전수 스캔(±12줄 save 판별)을 돌렸다. 주기 저장이 5초 간격이라
일반 지급은 손실 창이 작지만, **차감·수령 직후 5초 내 새로고침**으로 세이브를 롤백하면:
소진 카운트가 되돌아가 일일 상한 우회(무료 입장 반복)·claimed 가 되돌아가 무한 수령이
가능했다. 15곳 발견:

- **소진 게이트 9곳**(광고 제단·마을 환전·요일덱전·월드보스·탑 소탕/도전·잔불 미궁·
  약탈·골드던전·일일 퀘스트) — 입장권/재료 차감도 전부 같은 줄에서 일어난다. 전부
  `dailyUse()` 를 지나므로 **관문에서 save() 일괄 연결**(신규 소진 경로도 자동 저장).
- **claimed·루비 확정 6곳 개별 save**: 미션 완료 보상(_missionPaid) · 유료 패키지 구매
  (루비 차감+give) · 한정 패키지 수령 · 7일 출석(attendLastDate) · 우편 모두 받기 ·
  우편 개별 받기.
- smoke 신규 스텝 '소진·수령 즉시 저장'(dailyUse 실행 save 스파이 + 6곳 앵커 소스 정합).
- 밸런스 무변경(저장 시점만) — 시뮬 곡선 영향 없음.

검증: npm run check 전체 통과. 남은 원칙: **재화·소진 플래그를 확정하는 새 onclick 은
save() 를 마지막에 붙인다**(dailyUse 경유면 자동).

### 2026-09-19 v5.309 — 즉시 저장 2차 시정(공용 관문·클로저 결제 13곳): 소환·제작·강화·용광로 등

v5.308의 1차 스캔(S.weekly/monthly 형태 grep)이 놓친 사각 — **클로저 지역 결제(payCur)·공용
함수·지역변수 claimed** — 을 2차 스캔(차감/지급 패턴 ±8줄 save·dailyUse 판별)으로 덮었다.
13곳에 즉시 save 연결:

- **공용 관문 3**: summonRun·matSummon(소환 전 경로 — 권·루비·지정) / mkBuy(골드·회색코인·
  강화석·주사위 통화 상점 전체).
- **개별 10**: 제작 시작(재료·골드 차감) · 각성의 결정(기록서·강화석) · 장비 강화(**파괴
  경로 포함** — 장비 소멸이 저장 안 되면 새로고침으로 부활) · 용광로 월 1회 소진(롤백 무한
  재도전 차단) · 점령전 입장 · 길드 창설 · 영웅 레벨업 · 버프 구매 · 코스튬 구매 · 조각팩.
- 2차 재스캔 잔여 8건은 전부 오탐(관문 내부/onclick 끝 save가 ±8줄 창 밖) — 실제 커버 완료.
- smoke 앵커 12곳으로 확장. 밸런스 무변경(저장 시점만).

검증: npm run check 전체 통과. **원칙 확립(재발 방지): 재화·소진·진행도를 확정하는 모든
지점은 공용 관문(dailyUse·summonRun·matSummon·mkBuy)을 경유하거나 onclick 끝에 save()를
붙인다.** 이로써 '상태만 바꾸고 저장 안 함' 유형 결함은 3버전(v5.306~309)에 걸친 전수
시정으로 종결.

### 2026-09-19 시뮬 — 25600h v5.305(주사위 교환) 반영 재측정: 기준선 불변 확증 (게임 무변경)

구 25600h 기준선(9fa2239)이 주사위 교환 커밋(v5.305) **이전**에 측정됐다는 갭을 발견해
재측정했다(600h만 +3.6% 반영돼 있었음). 결과: **CP 6,228,929 · 결정 ✦48 · 세이브 16.1KB ·
리더 Lv 1,624 — 기존 기준선과 완전 동일.** 초반의 소환서 가속(+3.6%)은 장기에서 수렴한다
(말기엔 영웅 9/9·직업 조각 수천 개 과잉이라 소환 축이 한계 도달 — CP는 세트·강화·각성축이
지배). 기준선 수치는 그대로 유효. 최장 공백만 27.0h(@CP 5.66M, 말기 1회) 관측 — 정책 변경이
액션 타이밍을 옮겼을 뿐 종전 18.0h 판정과 동일 유형의 시뮬-일 경계 구조적 공백(결함 아님).
강화·킬률·세트(×2.714) 진단도 전부 종전 기록과 동일 수준.

### 2026-09-24 v5.310 — 골드 무한 복제(하위 화면 잔존) · 튜토리얼 ✕ 가림 · 세이브 문자열 HTML

신규 유저 실플레이 + 정적 감사(코드리뷰어)로 찾은 **라이브 결함 3건**. 전부 라이브 v5.309 에서
실클릭으로 재현한 뒤 고쳤고, 고친 쪽에서 같은 스크립트로 소멸을 확인했다.

1. **골드 무한 복제 (상)** — 장비 상세 [분해]→[예] 뒤 `openModal('inventory')` 가 불려도 장비 상세가
   그대로 남았다. 이미 사라진 장비의 [분해] 를 다시 누를 때마다 `addGold` 재지급(라이브 실클릭:
   L 1개 재클릭 1회 +6,600,277). 원인: `subBody()` 하위 화면(장비 상세·강화·영웅 상세·제작 결과)은
   `_subKey` 를 안 세우는데 `openModal` 은 `if(_subKey) closeSub()` 로만 닫았다. v5.123 이 제작 결과
   한 곳만 호출부에서 막았고 나머지는 열려 있었다(영웅 상세 → 다른 화면 전환 시에도 잔존).
   - 1차: `openModal` 이 하위 오버레이를 **조건 없이** 닫는다(같은 키 새로고침은 그 위에서 이미 return).
   - 2차: 분해·강화·장착이 `S.equips` 에 없는 장비면 재화를 안 움직이고 화면을 닫는다.
   - ⚠ 튜토리얼 STEP 4(강화)는 종전에 **이 잔존 화면의 [강화]** 를 짚고 있었다. 이제 잔존이 없어
     착용창 `.eq-fn5 .round-btn` 경로(원래 subs 정의)로 간다 — 실클릭 워커 완주로 확인.
2. **튜토리얼 STEP 7(영웅 합성) ✕ 가림 (중)** — 소환창이 열린 채 STEP 7로 넘어가면 손가락이 좌상단 ✕ 를 짚는데,
   모달 위로 올라온 STEP 박스(`#onboard.over`)가 바로 그 ✕ 를 덮어 **손가락대로 눌러도 박스만 눌렸다**
   (라이브 실클릭: BLOCKED→STUCK). CSS 주석의 "오른쪽 70px 을 비워 ✕ 를 안 가린다" 는 ✕ 가 오른쪽에
   있던 시절의 전제였다. `.tut-highlight.mclose{z-index:46}` 도 무력 — ✕ 는 `#modal-root`(z:20)
   스태킹 컨텍스트 안이라 z:25 박스를 못 넘는다.
   - 박스 `pointer-events:none`(누를 요소가 없는 안내판) + 목표와 겹치면 `tutDockBox()` 가 `.ob-low`
     로 전장 아래로 비킨다. 겹침은 '위에 붙였을 때의 예상 위치' 로 판정 — 현재 위치로 재면 매 프레임
     위아래로 깜빡인다. verify [K] 에 두 규칙 계약 추가.
3. **세이브·입력 문자열 HTML (중)** — `el()`/`toast()`/`sysLog()` 가 innerHTML 이라 길드명
   `<h1>x</h1>` 이 태그로 렌더됐고, [진행도 가져오기] 세이브에 심은 태그는 길이 제한 없이 실행 가능했다.
   출력 수백 곳 대신 입구에서 막는다: `safeText()`(길드 창설·약탈 대상) + `scrubSaveStrings()`
   (mergeDefaults — 세이브의 모든 문자열 값에서 `< >` 제거). 정상 세이브에는 `< >` 가 없다(채팅·로그
   비저장, S 에 HTML 을 넣는 코드 없음 — 전수 grep).

**검증**: npm run check 통과 + smoke 신규 2건(분해 재클릭 복제 차단·세이브 태그 무력화) — **두 검사
모두 옛 game.js 에 대해 실패함을 확인**(복제 +9,900,000 검출). 실클릭 QA 스크립트 2종(내부 보관):
`docs/qa/20260924-튜토리얼-손가락추종-실클릭.cjs`(tutTarget 이 짚는 좌표를 page.mouse 로 누름 —
el.click() 은 덮여 있어도 통과해 가림을 못 잡는다) · `docs/qa/20260924-하위화면-잔존-골드복제-실물QA.cjs`.
둘 다 `live|local` 인자로 라이브·로컬을 같은 기준으로 비교한다.

감사에서 **문제 없음으로 확인**된 것: ISO 주차·연말연초(2026-12-28~2027-01-04) 축제 순환(%3 중복 없음),
투기장 주간 롤오버, 절전·투기장 헤더 타이머 정리, 공용 결제 관문의 이중 결제 창.

### 2026-09-24 v5.311 — 다중 창 세이브 덮어쓰기(진행 소실) 차단 + 자율 QA 스윕 2종

**결함(상)**: 같은 브라우저에서 게임을 두 창으로 열면(백그라운드 탭 + 메신저 링크로 새로 연 탭) 두 창이
세이브 하나를 5초마다 번갈아 썼다. 새 창에서 진행해도 옛 창의 자동저장·닫기가 옛 상태로 덮어써
**진행이 통째로 사라졌다**(라이브 실측: 새 창 골드 999,999,999 → 6.5초 뒤 옛 창의 3,347).
- `load()` 가 `claimTab()` 으로 주도권 키(`hwasin_save_v1_owner`)에 자기 탭 id 를 쓴다 — 최근에 연 창이 주인.
- 다른 창은 `storage` 이벤트로 즉시, 늦어도 다음 `save()` 의 `tabOwned()` 확인(5초)에서 알아채고
  저장을 봉인 + 전체 화면 잠금(`#tab-lock`, z:80). [이 창에서 계속] = 새로고침(최신 세이브 이어받기).
- ⚠ 잠길 때 '마지막 저장' 을 하면 안 된다 — 새 창이 이미 읽은 뒤라 그건 곧 덮어쓰기다.
- 검증: smoke 신규 1건 · verify [K] `#tab-lock` z-index 계약 · 실물 QA
  `docs/qa/20260924-다중창-세이브덮어쓰기-실물QA.cjs`(라이브 결함 2건 재현 → 로컬 6/6 통과, 주도권 이동까지).
- 부수: [장착] 확인창 onYes 에도 처분 장비 재확인(분해와 대칭 — 감사 지적, 현재 도달 경로는 없음).

**자율 QA 스윕(결함 0)**: `docs/qa/20260924-전모달-버튼-터치가능성-실물QA.cjs` — 47모달 보이는 버튼 131개를
실제 좌표 hit-test(가림)·뷰포트 이탈·클릭(+확인창 주 버튼) 에러로 검사. 375×812·360×640 둘 다 0건.
하네스 주의: 앞 모달 클릭이 켠 절전 화면·소환 연출이 다음 모달 측정을 가린다(정리 코드 포함) ·
`#summon-fx` 의 innerHTML 을 비우면 소환 연출이 null 에러를 낸다(오탐).
**재클릭 복제 계열 전수 감사(결함 0)**: 보상·상점·던전·의뢰·우편·정산 핸들러 전부 클릭 시점 재조회.
구조 관찰: `.b2/.b5-ovl` 확인창은 openModal/closeModal 이 지우지 않는다 — 새 보상 버튼을 그 위에
만들 때는 onYes 안에서 상태를 반드시 재조회할 것.

### 2026-09-25 v5.374 — 리뷰(v5.368~370) 확정 8건: 장착 확인창 세트 배율 합산값 · 손해 확인창 손가락 · 절전 요약 순감소 등

**리뷰 워크플로**(3렌즈 → 반박 검증, 17에이전트): 발견 14 · 확정 11(그중 '안전 확인' 2) · 기각 3. 수정:
- 장착 확인창 세트 줄: 세트별 단독 ×값 → **계정 합산 배율 전후 한 줄**(equipPreview 가 setDamageMul m0/m1 을 함께 잰다). 단독값은 다른 세트가 함께 켜져 있으면 실제와 크게 어긋났다
  (응시+광란+강철맹세 = 방어 70% 상한: 실제 −15.8% 인데 단독 −46.2%). 세트 줄은 이름·단계만.
- 손해 판정 근거(총 전투력)를 총 줄에도 증감률·빨강으로 · 영웅 줄과 방향이 갈리면 '이 영웅은 오르지만 세트 해제가 보유 영웅 전원에게 적용' 이유를 붙인다.
- 손해 확인창(safeNo)에서 튜토리얼 손가락이 [장착](텍스트 매칭)을, 금색은 [취소]를 가리키던 충돌 → showConfirmDialog 가 `ov.dataset.safeNo` 를 달고 tutTarget 이 먼저 금색 [취소]를 짚는다
  (tutPrimaryIn 의 매칭 순서는 전역으로 바꾸지 않는다).
- setProgLine(nm, it): 미착용 사본을 볼 때 '착용 중'이 [장착] 버튼과 모순 → '같은 조각 착용 중(이 장비를 입어도 진행 +0)'. 장비 상세에서 두 번 계산하던 것 한 번으로.
- 절전: 경과 mm:ss 음수(시계 되감기) 0 으로 · 해제 요약 골드·재료는 **순변화 부호**(절전 중 [다시 제작] 등으로 줄 수 있다 — 종전 '+0' 으로 감춤).
- 무로깅 catch 2곳(장착 가정 계산·절전 요약) → console.error(try 유지). 가정 계산이 실패하면 손해 판정도 끈다(기본 문구·금색 [장착]).
- 실물(360): 응시(리더)+광란(2번 영웅) 6세트 · L 투구 교체 확인창 '재검 도르카 전투력 1,398 → 679 (−51.4%) · 총 전투력 2,796 → 1,328 (−52.5%) · ⚠ 응시 6세트 해제 ·
  세트 배율(전 영웅 공통) ×4.00 → ×1.86 · 총 전투력이 내려갑니다' · 손가락 = 금색 [취소] · 미착용 심연 투구 사본 '같은 조각 착용 중'.

### 2026-09-25 v5.373 — 교정 시뮬 재판정(6결정) 반영: 게임 수치 변경 0건 · 공략 카드·근거 정정 · 시뮬 보정 상수

**재판정 워크플로**(v5.367 K1 교정 시뮬 위에서 잘린 시뮬 시절 결정 6개를 다시 잼 — 5시드 기준선 + 결정별 짝 비교). 결론: **게임 수치·보상을 바꿔야 하는 결정 0건 · 대표 결재 필요 0건**.

**현재 시뮬 기준선**(교정 시뮬 v5.367, 5시드 42·7·11·23·99 중앙값 — 원자료 scratchpad/rebase/):
- 풀: R 3.5h · E 17h · L 27h · CP 100h 275,032 · 200h 457,284 · 600h 1,029,639.
- 캐주얼(하루 8시간): R 3.5h · E 48.5h · L 72.5h(rebase 표기, 시드 폭 E 32~48.5 · L 51.5~74) · CP 100h 40,650 · 300h 327,716 · 2400h 1,639,152.
- '리더 착용' 지표(다른 지표 — 섞지 말 것): 캐주얼 E 전 부위 49.5h · 첫 L 75.5h · L 전 부위 145h / 풀 18.5h · 29.5h · 79h.
- v5.370~372 영향(s42 바이트 대조): v5.371 스냅샷 = 기준선 바이트 동일 → 차이는 전부 v5.372(세트 전투 효과). 캐주얼 300h 는 121.5h, 풀 200h 는 71h 부터
  궤적이 갈린다(작열 6·주술 3 착용 → 쿨감이 전투를 바꿔 난수 흐름이 달라짐). 최종 CP 캐주얼 300h 322,867 동일 · 풀 200h 457,284 → 459,383(+0.5%) · 창당 킬 ~2,500 동일 — 곡선 영향 잡음 수준.

| 결정 | 판정 | 새 근거(요지) | 후속 |
|---|---|---|---|
| v5.347 영웅 강화 비용 체증 | 유지·근거 수정 | 풀 600h 체증=고정 300(0단계), 캐주얼 2400h 체증 각 +5~6(CP +4.9%) vs 고정 +10(+8.3%), 골드 지출 같음. '600h 소진'은 공격적 구매에서만 | 풀 강화 0단계·남는 조각 7,880(85% 파티 밖) — 후속 설계 |
| 공략 카드 | **수정(반영)** | 영웅 약 2일·레전더리 약 6일(하루 8시간·전 부위, 첫 L 약 3일) · +20 부위당 평균 약 4.7억(2억~9억) | 반영 완료 |
| v5.356 투기장 순위 버프 완화 보류 | 유지 | 장기 CP +5~10%(2400h +3~5% 실재), 2~4배는 캐주얼 100~120h 경계 효과 · 실전투 승률 91.4% | 너프 권고 없음(U1) |
| v5.358 대장간 주문서 | 유지·근거 2건 정정 | 제작 0회 구간이 더 일찍(풀 63.5~96h·캐주얼 121.5~146.5h 이후) · 납품 약 3건/일 · CP ±잡음 · 가호 '밀려나지 않음' 틀림 | 후반 L 주문 망치 잉여(캐주얼 200~290개 [추정]) — 교체 검토 시 대표 |
| v5.359/365 길드 토벌 | 유지 | 100일 46단계 · 24일 이후 주기당 0~1처치 · 기록서 +9 = 탑 교환(월 53~62권) 대비 +4.4~5.3% · 첫 30일 +10.7~12.2%(관찰) | — |
| v5.360 골드 레벨업 노출 보류 | 결정 유지·근거 교체 | '압축' 폐기 → '과매수 함정'(P100 캐주얼 100h −22%·200h −29%, 600h 부터 역전) · 적당한 구매(P10~25)는 중립~이득 | 노출하려면 조건 '비용 ≤ 보유 골드 10~25%' · 금색 버튼 임계는 측정 후 |

- 반영: 공략 카드 문구 2곳 · '3.2억' 잔재 5곳(game.js v5.229 주석·공략, balance-sim 주석, HANDOFF 571·654) · 업적 주석 '첫 성공 ≈ 6일' · 각 결정 항목 머리에 재판정 포인터.
  3.2억은 K1 과 무관한 별도 오류(v5.229 표본 편향 — +20 미도달 부위까지 평균). 해석해·몬테카를로 20만 회: 부위당 평균 4.76억(시도 36.6회·망치 81.5개).
- 시뮬 보정 상수(옵션을 켤 때만 쓰임 — 옵션 없는 기준선 불변): `GUILD_CAL` 0.17 → **0.23**(교정 전장 실전투 0.230~0.247) · `arenawin` 기본 0.95 → **0.92**(실측 91.4%). 옛 값은 0×0 전장 시절.
- 대표 결재 대상(바꾸고 싶을 때만 — 부서 권고는 전부 현행 유지): ① 투기장 버프 완화(U1) ② 토벌 첫 달 기록서(U1) ③ 영웅 강화 고정 300(매출) ④ 남는 조각 해소 설계(상품이면 매출) ⑤ 주문서 후반 L 보상 교체(라이브 보상).
- 추가 측정 과제: 풀 goldlv · 금색 버튼(P100) 초반 영향 · 풀 영웅 강화 0단계 원인 · s7 orders+guild 동시 · v5.358/356 5시드 보강.
- ⚠ 재판정 보고의 '기록서를 팔던 GRAYSHOP 은 폐지됨'은 **사실이 아니다** — v5.6 이 길드 탭 하단 '회색코인 교환'으로 되붙였고 '영웅 기록서 1권'(회색코인 900)이 있다
  (공략 카드 '영웅 기록서(탑 상자·회색코인)'는 맞다 — 고치지 않음). 토벌 기록서 분모 판단에는 영향이 작다(회색코인은 길드 레이드·약탈·점령전으로만 느리게 들어온다).

### 2026-09-25 v5.372 — 4차 발견 K3 2단계: 세트 효과 전투 적용(쿨감·응시·광란·주술·강철맹세)

- v5.367(1단계)은 미구현 세트 줄을 '준비 중'으로 흐리게만 했다. 이번에 구현: `setBattleFx()`(장비의 순수 함수) → Battle 이 layoutHeroes(= partyCP 를 재는 시점)에서 `setFx` 로 캐시.
  장비를 바꾸는 경로는 전부 refreshParty 를 부른다(장착·파괴·제작 자동 장착·시뮬 장착) — 매 공격 재계산은 장비 전수 순회라 시뮬 시간을 늘린다.
- **쿨감**(tier.cdr): 작열 6 15%·8 35%(대체 — dmg 처럼 세트 안에선 최대값), 광란·그늘칼·주술·강철맹세 20%, 세트끼리 합산, 상한 `SET_CDR_CAP` 50. `skillCD = sk[2] × cdMul`.
- **응시**(gaze): 일반 공격만, 대상별 맞기 전 체력 ≥ 50% +70% · 보스 +50%(합산 최대 +120%). **광란**(frenzy): 실제로 나간 일반 공격 4회마다 무효화 1회분(쌓이지 않음) — 몹 반격·투기장 적 공격에서 소모.
  **주술**(curse): 궁극기 직후 1차→2차→3차 순번으로 한 스킬 추가 발동(무작위 대신 순번 — 새 난수는 재현성을 깬다, v5.216 선례). **강철맹세**(iron): 투기장 적 치명타 피해 ×0.6.
- 타격 처리를 `strike(피해, 범위, 단일, 크리)` 로 묶었다(주술 추가 발동이 같은 경로를 탄다). ⚠ 세트가 없으면 gz 는 원래 피해 그대로·추가 발동/카운터 없음 → D1~D9 해시 불변(확인).
  layoutHeroes 이어받기에 `_frz/_nullify/_curse` 추가(빠지면 갱신 한 번에 무효화가 사라진다).
- 그늘칼 0·1번(치명 100% 초과·즉사)은 전제 메커니즘이 없어(치명 상한 30%, 즉사 판정 없음) 비-live — 화면 '(발동 조건 없음)'. 세트 화면: 전투 적용·전투력 숫자 별도 줄에 '전투 적용' 표식, 방식이 다른 줄은 보충 설명(tier.note).
- 전투력(heroPower·세트 ×N)은 불변 — 사냥 위험 판정·던전 권장·시뮬 CP 곡선은 그대로, 실제 전투(처치 속도·탑·월드보스·투기장)만 강해진다(이용자 유리).
- smoke D10(순수 함수 · 광란+주술 2차 스킬 쿨 최대 5.40초(기본 9) · 무효화·주술 순번 관측 · 3조합 재현성·60fps=헤드리스·전투 중 갱신 · 투기장 강철맹세+광란 재현성·갱신), 4A 검사 갱신.
- Battle.setFxNow()·setState() 읽기 전용 노출(검증용).

### 2026-09-25 v5.371 — 4차 발견 K6(최소판): 도감이 멈추지 않게 — 미조우 카드 → 사냥터 · '도감 미기록' 태그

- 문제(시뮬 5런 전부 · 실물 60일 세이브): 추천 사냥(huntSafeTier)이 리더 전투력 이하 최고 단계로 건너뛰어, 전투력이 한 번에 뛰면 사이의 종을 영영 만나지 않는다 →
  도감 16~18/20 에서 멈춤 · 등급 완성 보상 4개 중 2개 · 전종 완성 보상 0회(100일). 미조우 카드는 onclick 없는 막다른 카드였다(v5.153 '정보가 없어 안 데려간다' —
  그러나 몬스터 선택 화면이 이미 모든 종의 이름·그림을 공개한다).
- 미조우 카드: '??? · 탭 → 사냥터' → 몬스터 화면 해당 등급 탭 + 그 행 강조(`MODALS.monster._focus` — 렌더에서 바로 지워 1회만, 금색 외곽선 2회 맥동·중앙 스크롤).
  발견 카드도 같은 강조. 몬스터 화면: 한 번도 처치하지 않은 종에 '도감 미기록'(.mon-new).
- 도감 안내 줄: '미조우 N종 — 카드를 누르면 그 사냥터로 갑니다 · 전종 완성 보상: …'(수령 뒤엔 보상 문구 생략). 등급 칸 누르면 등급 보상 토스트(title 은 폰에서 안 보인다).
- `CODEX_RW_TXT`: 등급·전종 보상 문구 정본 — 지급 함수(onKill RW·codexAllReward)가 돌려주는 문구와 화면 안내가 같은 값. 렌더·칩 클릭은 지급하지 않는다(smoke 확인).
- U6(도감 숙련 별)은 **보류** — 원안(1천~100만)은 한 종에만 쌓이는 구조라 성립하지 않고, 순환 사냥 정책은 전투력 −4~−23%(시드 편차 큼)라 보상·경제를 새로 설계해야 하는 M~L 규모.
- 실물(360): 18/20 세이브 → 도감 안내 3줄 · 미조우 카드 2장 → 빙결흡혈귀 클릭 → 몬스터 화면 영웅 탭, 그 행 강조·중앙·'도감 미기록' 1개 · 가로 넘침 0 · 페이지 에러 0.
  smoke '4차 K6'(스텁은 replaceChildren 이 내용을 못 옮겨 몬스터 화면을 Node2 에 직접 렌더해 본다). 튜토리얼 실클릭 완주 막힘 0.

### 2026-09-25 v5.370 — 리뷰(v5.367) 확정 2건: 레벨업 필요 경험치 정본 한 곳 · 결과창 부가 정보 예외 로깅

**리뷰 워크플로**(3렌즈 리뷰 → 반박 검증): 확정 3(중복 1) · 기각 7(시뮬 잔여 검사 허점·실행 시간 증가·장신구 치명의 전투력 미반영 등 — 결함 아님 또는 범위 밖).
- `expNeed(lv)` = (lv||1)×250 — 전투 레벨업 루프·벤치 루프·영웅 카드(바·툴팁)·정산·절전이 각자 '레벨×250'을 적고 있었다(v5.367 의 '공용 정본' 주석은 사실이 아니었다 — 정산 화면은 여전히 자체 식).
  정산 화면은 leadExpPct 호출로. 값은 같으므로 결정론 해시 불변(100회 2f661908ff227f24 · D6 eac59f3251def0b8 그대로).
  smoke: expNeed 값 · '레벨×250' 직접 계산 잔재 탐지 · 레벨 상한 검사가 expNeed 형태를 인식(종전 정규식은 0개 탐지로 실패했다).
- showDungeonResult 의 resultExtra 예외를 빈 catch 가 흔적 없이 삼켰다 → console.error 로 남김(try 는 유지 — 다시 던지면 결과창이 반쯤 그려진 채 멈춘다).
- 실물: 리더 exp 42% 주입 → 정산 바·문구 42% · 절전 42% · 예외 던지는 resultExtra → 결과창 '도전 성공!' 정상 + 콘솔 'resultExtra 실패: 테스트 던전 Error: boom'.

### 2026-09-25 v5.369 — 4차 발견 K4: 방치 결과가 남는다(시스템 기록 보존 · 절전 세션 요약)

- pushChat 이 채널 구분 없이 40줄에서 잘라, 1.6~3.2초마다 들어오는 가짜 채팅이 [시스템] 기록을 약 100초 만에 밀어냈다(실측) → 시스템 30줄 + 그 외 40줄 채널별 상한(리본 제외).
  ⚠ chatTick 빈도·난수는 건드리지 않았다(전역 Math.random 순서).
- 정산 화면 '획득 재화' 칸은 보유량(pwCells) → '보유 재화'.
- 절전 세션 요약: startPowerSave 에서 `_pwSnap`(메모리 전용 — S 에 넣지 않음) · 절전 화면에 '절전 mm:ss · 처치 +n' · 해제 시 '절전 N분 N초 — 처치 +k · 골드 +g · 재료 등급 +n' 긴 토스트(6초)+[시스템] 기록(10초 미만·시계 되감기면 생략).
  growthBurst 는 쓰지 않는다(#modal-root 안 — 모달이 닫힌 홈에선 안 보인다, K1 과 같은 함정).
- smoke '4차 K4'(시스템 줄 200개 가짜 채팅 뒤에도 보존 · 채널 상한 · 절전 요약 문구). QA 함정: 앞선 검사가 절전 화면을 열어 두면 start 가 곧바로 돌아가 옛 기준점이 쓰인다 — 먼저 닫을 것.
- 실물: 절전 13초 → '절전 00:12 · 처치 +3' · 해제 토스트/기록 '절전 13초 — 처치 +3 · 골드 +4,395 · 재료 일반 +2' · 페이지 에러 0.

### 2026-09-25 v5.368 — 4차 발견 K2: 세트를 결정 지점에 · 장착 확인 전후 전투력·세트 해제 경고

- 세트는 중반 최대 레버(검증 실측: 60h 세이브에서 빠진 응시 2조각만 채우면 리더 +68.5%)인데 결정 지점에 소속 표시가 0 이었고, 장착 확인창은 되돌릴 수 없는 교체(기존 장비 파괴)인데
  수치가 0 이었다(실측: 응시 6세트 상태에서 L 결정 투구 +0 장착 → 리더 −46%·세트 해제, 사후 토스트뿐).
- `SET_OF`/`setOfItem`(SET_PIECES 역인덱스 — 한 아이템 한 세트 smoke 검사) · `setTierMul` · `setProgLine`(대장간 사이드·제작 팝업·장비 상세 '세트 응시 4/6 → 착용 시 5/6 · 6세트 ×1.86').
- `equipPreview(e,hid)` — equipItem 을 실제로 돌려 전후 전투력·세트 단계를 잰 뒤 finally 로 원상 복원(⚠ 복원을 빼면 확인창만 열어도 장비 파괴). 장착 확인창에 출격 영웅·총 전투력 전후,
  깨지는 세트(빨강)·발동 세트(초록), 손해면 '전투력이 내려갑니다' + `showConfirmDialog opt.safeNo` 로 [취소] 금색(금색 1개 규칙). cpDeltaLine 음수 부호('(+-46.9%)' → '(−…)' 빨강).
- 공략 카드 '3조각부터 발동'(작열만 해당) → '대부분 6조각부터 발동(작열만 3조각)'.
- 보류: 정체 계단 패널(wipeRemedies)의 '세트 완성' 수단(M — 교정 시뮬 기준으로 후속).
- 실물(360): 확인창 '재검 도르카 전투력 444 → 250 (−43.7%) · 총 685 → 380 · ⚠ 응시 6세트 해제 ×1.86 → ×1.00 · [장착] [취소*]' · 장비 상세·대장간 세트 줄 · 넘침 0 · 페이지 에러 0.
  smoke '4차 K2'(역인덱스 1:1 · 가정 계산 뒤 배열 참조·e 필드까지 불변 · 응시 6 → L 투구 해제 감지·감소).

### 2026-09-25 v5.367 — 4차 발견: ⚠ 시뮬 도구 결함 교정(K1) + 표시 = 적용 묶음(4A)

**4차 발견 워크플로**(5렌즈 발견 → 렌즈별 반박 검증, 10에이전트): 후보 15 → 유지 13. ⚠ 결과 JSON 에서 일부 검증 문구가 다른 후보에 붙어 있었다(K3↔K7, K10↔K13 — 제목으로 맞춰 읽음).
**K1(영향 5) — balance-sim 이 창마다 500초만 전투했다**: battleWindow 가 pump(1799.9) 1회 → 게임 pumpFrame 폭주 가드(1만 스텝 = 500초)에 잘려 약 1,300초가 버려졌다(v5.182 시뮬 도입 때부터).
창당 킬 ~700 은 실제 30분(~2,500 — 브라우저 실측)의 28%. 게다가 vm 에서 Battle.resize() 가 안 불려 전장이 0×0(권장보다 높은 사냥터는 결과가 완전히 달랐다).
→ 400초 조각 펌프 + 창 끝 잔여 검사(가드·창 길이를 바꾸면 즉시 throw) + 부트 뒤 Battle.resize(). 게임 코드·결정론 무관.
**영향: 시뮬 기준선 전부 이동** — 100h 최종 CP 32,447 → 약 275,000(단일 시드), R 10h→3.5h · E 48h→17.5h. 그 위에서 내린 판단(v5.347 영웅 강화 비용 · v5.356 투기장 버프 영향 ·
v5.358 주문 예약선 · v5.359/365 토벌 H0·R·기록서 + · v5.360 골드 레벨업 노출 보류 · 공략 카드 '영웅 약 3일·레전더리 약 6일')은 **재판정 대상** — 5시드 재측정 진행 중, 결과는 **v5.373 항목**(재판정 완료: 게임 수치 변경 0건).
**4A(표시 = 적용)**:
- 세트 효과(K3 1단계): tier.live(전투 적용 줄 인덱스) · setLiveFx · 도감 요약·발동 카드는 live 줄 · 세트 효과 화면은 나머지를 흐리게 '(준비 중 — ×N 미포함)'. 쿨감·광란·응시 조건부·그늘칼·주술·강철맹세 받는 치명타 미구현.
  2단계(쿨감 등 실제 구현)는 스킬 사용 시점이 바뀌어 v5.216 처럼 결정론을 깰 수 있어 별도 판단(작열 6 착용 D1 형식 검사를 먼저).
- 치명(K7): heroCritRate/heroCritMul 이 '반지' part 만 세어 **귀걸이·팔찌·인장(part '장신구')이 전투에서 0** 이었다(주석 의도와 다름 — 이용자 유리 수정, 상한 그대로).
  영웅 스탯 탭 치명타율·피해·이동속도 = Battle.critInfo(실제 값, 종전 '5.0%' 공식), 공격속도 '기본'. 스킬 탭의 '(영웅 등급에 따라 해금)'·🔒 제거(전투는 4스킬 모두 사용 — v5.216), 설명 = 실제 범위·위력.
- K5: 길잡이 배너가 캔버스 상단 사냥터 라벨·'N 연속 처치!'를 가림 → `_gbhPx`(syncGuideOffset 캐시) 만큼 내림(전투 중 0).
- K8: 절전 화면 경험치 바 = 처치 수 % 100(가짜) → leadExpPct(정산 화면과 같은 정본) · 의미 없는 카운트다운 제거, 대기 금액/떠나면 받는 것 안내.
- K9: 몬스터 화면 '권장보다 약하면 전멸합니다'(사실 아님) → '반격이 아파지고, 크게 약하면 전멸(최하급으로 후퇴)' · '/마리' 골드 = huntGoldPerKill(onKill 과 같은 식 — 종전 약 25배 과장).
- 효과음(K10 검증분): race 결과(탑·월드보스·레이드)가 항상 fail → 보상 race 는 claim · 신기록은 resultExtra 가 _dgResultSfx='legendary'(직접 재생 제거 — 패배음 겹침·즉시 결과에서 누락이던 것).
  골드 레벨업 무음(v5.364 전엔 궁극기 재발동 부작용으로 우연히 소리) → craft · 10레벨 이정표 awaken.
- K11: 업적 '영웅 합성 8'(구조상 불가 — 합성 대상 4명) → 로스터 파생 · 처치/제작/소환/재료 합성/레전더리 사다리 끝 연장(보상 없음). 월드보스 순위표 재보정·코스튬 획득처(매출 연결)는 보류.
- 보류: K2(세트 결정 지점 정보·장착 전후 경고, M) · K4(방치 결과 요약, M) · K6(도감 숙련 — 교정 시뮬 기준으로) · K12(**대표 확인** — 과금 상품 설명 불일치: 곡괭이 패키지 곡괭이 미지급·'채굴 권한' 효과 0 · 보유형 골드 코스튬 두 벌 효과 비중첩·방치 골드 한정).
- smoke '4차 4A' 신설. 결정론 해시 4종·D7~D9 불변(시뮬 창 교정은 게임 무관, 장신구 치명은 D 시나리오 장비 없음).

### 2026-09-25 v5.366 — 리뷰(v5.363~365) 확정 3건 — 모두 v5.364 K2 의 회귀

독립 리뷰(3차원 13에이전트, 반박 검증): 확정 3건 + 안전 확인 1 · 반박 6건.
- **① 자동 연전 중 제작 판정이 끝없이 밀림(중)**: K2 의 craftAutoCheck 보류(전투 중·결과 카드) 조건이 투기장 자동 연전 동안 한 프레임도 풀리지 않는다
  (결과 카드 3초 타이머가 같은 콜백에서 곧바로 재입장 · 입장권 40초 충전이라 연전이 사실상 무한 — 검증 실측 9,139프레임 중 판정 가능 0). 게다가 그 사이 대장간을 열어
  [즉시 완성]을 누르면 이미 끝난 제작에 제작서 30장이 빠졌다. → 3초 타이머: 제작이 끝나 있으면 S.arenaAuto=false · 토스트 · closeModal(다음 프레임 판정).
  ⚠ 판정을 연전 사이에 끼워 넣지 마라(arenaFight 의 closeModal 이 제작 결과 팝업을 지운다). craftInstantConfirm: endAt 이 지났으면 차감 없이 안내. 진행 문구 '제작 완료 · 전투가 끝나면 결과가 나옵니다'.
- **② 솔로 탑 → 홈 솔로 복귀가 이어받기(하)**: 영웅 목록이 같아서(둘 다 [p[0]]) 던전 쿨·누적 피해·**탑에서 걸어간 위치**가 홈으로 샜다(중앙에서 17px 어긋난 채 고정).
  → `_layMode`(직전 배치 mode) — mode 가 바뀌면 목록이 같아도 새로 배치.
- **③ 이어받기가 읽히지 않는 옛 필드만 옮김(중)**: _lockTarget/_lockUntil(읽는 곳 0) 대신 실제로 쓰이는 `_moving`(시뮬 공격 게이트)·_lockedRow·_rowLockUntil·dieAnimT, 위치를 잇는 경우 _lastX/_lastY.
  _moving 이 빠지면 걷던 영웅(투기장·보스전)이 갱신 직후 헛공격해 시드 난수를 뽑았다 — smoke D7 은 centerHold(안 걷는) 던전이라 못 잡았다.
  **smoke D9 신설**: 투기장 시나리오 2시드 × 1~60프레임 각각 갱신 = 끝까지 관람 해시(수정 전 코드에선 3프레임째 실패 확인).
- verify [M] 이 cssText 안의 grid-template-columns:repeat(n,1fr) 도 잡게 넓힘(리뷰 제안).
- 실물: 자동 연전 중 제작 완료 → 다음 결과 카드에서 연전 멈춤·제작 결과 · 끝난 제작 [즉시 완성] 차단(제작서 불변). 결정론 해시 4종·D7·D8·D9 통과, 기준선 32,447 불변.
- 반박(수정 안 함): 홈 전멸 부활 대기 중 편성 변경 우회(이번 diff 가 오히려 좁힘) · 자동 스크롤이 무관한 아이템 버튼으로(실측 불일치) · 보스 처치 토스트 등.

### 2026-09-25 v5.365 — 3차 발견 C묶음: 토벌 피해 이월(K3) · 직업 특성 약속 이행(K6) · 상한 재료 표시(K7)

- **K3**: 길드 토벌 새 주기에 dealt=0 을 없앴다(참전 수·참전 보상만 리셋). 첫 판(v5.359)은 R 1.18 대 후반 3일 CP 성장 약 3% 라 60일 이후 주기 13개 중 10개가
  88~99% 깎고 전량 회복(검증 재구성). 벤치 원형은 혼자 못 잡는 보스라 리셋이 무해했지만 처치형 단계제에선 참전이 증발한다. 이용자 유리 — U1 무관.
  시뮬(캐주얼 2400h orders=1 guild=1): 100일 41/41단계(s42·s11, 종전 35/34) · 0처치 주기 34개 중 10~11(종전 14~15) · 기록서 +8(월 2.4권 — 기준 35권 대비 +7%, 한도 10% 이내) · 길드코인 +3,665.
  레이드 화면·v5.359 공지 문구를 '참전 보상 초기화 · 입힌 피해 유지'로. smoke '길드 토벌' 새 주기 검사를 반대로(피해 이월 · 이월 피해로 처치).
- **K6**: 튜토리얼 마지막 '변경 불가' 특성이 가짜였다(두 특성 모두 ×1.02, 회피 +10%·회복 +10%·빠른 사냥 속도 미구현). 약속을 지킨다(v5.330 선례):
  `CLASS_TRAITS.fx`(마법형 dmgTaken 0.90 · 전투형 heal 1.10) + `classFx(k)` — 몹·보스 반격 hitF · 자연 회복 · 방패 회복(표기 +22%)에 곱. 문구(bullets·sum·전투력 구성 행)는 fx 에서.
  회피는 '받는 피해 −10%'로 구현(기대값 동일 — 회피 판정은 난수를 새로 뽑아 재현성을 흔든다). 투기장(PvP) 미적용(승률 92~94%). classTrait '' 이면 정확히 ×1 → 해시·시뮬 불변.
  smoke 'D8 · 직업 특성 전투 재현성'(마법형·전투형 각각 같은 시드 2회 = 같은 해시). grantClassStarter 는 신규가 시작 보유라 무동작(구세이브 호환용) — 그대로.
- **K7**: 사냥 드랍 연출(drop 'mat')을 matGain 실제 증가분 > 0 일 때만 — 상한이면 인벤토리로 날아가는 아이콘이 거짓이었다(실측 30초 flyLoot 13회·증가 0).
  ri 는 matGain 호출 전에 평가되고 drop 은 난수 무사용 → Math.random 순서 불변. 재료 소환(유상 열쇠)은 avoidCap — 그 등급이 전부 상한이면 상자를 '보유 상한'(cap)으로.
  재련 환급(초과분 골드화)은 하지 않는다(새 무상 골드 수도꼭지 — 기록서 경유 ✦ — 검증 기각, 대표 결재 사안).
- **시뮬 주의(단일 시드 이탈)**: 캐주얼 s7 orders=1 guild=1 최종 CP 1,974,888 — 궤적이 갈라져 시뮬 정책이 '강철맹세 6 + 작열 3'(×5.333, 숨은 강한 조합) 에 도달.
  같은 s7 옵션 없음은 837,517 로 정상. 이번 변경의 결함이 아니라 정책 경로 의존 — 곡선 판단은 여러 시드 중앙값으로.
- 실물: 직업 비교 화면(360) 문구 = 적용 효과, 넘침 0 · 페이지 에러 0. 결정론 해시 4종·D7·D8 통과, 기준선 100h 32,447 불변.

### 2026-09-25 v5.364 — 3차 발견 K2: refreshParty 가 전투 상태를 초기화하던 것 (궁극기 재발동·기여도 0)

**문제**(검증 실측): refreshParty = layoutHeroes() 가 영웅을 새로 만들어 skillCD·dmgDone·hp·dead 를 초기화 — 호출 18곳(제작 성공·강화·골드 레벨업·장착·소환·각성·칭호·코스튬·편성 등).
홈에선 조작마다 궁극기 즉시 재발동 + legendary 음 + 흔들림(한 세션 legendary 22회 중 희소 사건 3회), 전투 중엔 기여도 0/0 · 같은 시드 결과 피해 −24~−81%
(월드보스 기록·길드 토벌 피해가 이 값), 창 회전·가상 키보드 resize 도 같은 경로. 전투 중 제작 자연 완료는 대장간·결과 팝업이 전투 화면을 덮었다.
**수정**:
- `layoutHeroes(keep)` — 0 전체 초기화(입장·사냥터 전환·편성 소스 교체·D1~D6) / 1 refreshParty: **출전 구성(hid 목록)이 같을 때만** skillCD·atkT·dmgDone·hp·dead·respT·애니·락온·위치 이어받음
  / 2 resize: 상태만 잇고 위치는 새로. cp·partyCP 는 새로 계산(레벨업 즉시 반영). 던전 → 홈 1인 복귀·리더 교체는 구성이 달라 종전대로 새로.
  ⚠ **이어받는 영웅은 bRnd(atkT) 를 새로 뽑지 않는다** — 첫 시안은 난수 횟수를 맞추려고 뽑게 뒀는데, 그러면 전투 중 갱신이 시드 흐름을 밀어 같은 시드 결과가 갈렸다(smoke D7 실측: rngDrawCount 661→704, 승→패).
- 궁극기 전용 효과음 'ult'(110Hz 톱니 하강) — legendary 는 희소 사건 전용(v5.352 bossdown 분리와 같은 원칙).
- craftAutoCheck: 전투 중·결과 카드(dgResult·arenaResult) 동안 판정 보류(결과는 같고 표시만 전투 뒤).
**검증**: 결정론 해시 4종 불변 + **smoke D7 신설**(같은 시드, 전투 중 refreshParty 3회 → 쿨·기여도 보존 · 결과 해시 = 끝까지 관람).
실물(375×812): 홈 골드 레벨업 5연타 → 궁극기 쿨 동일·ult/legendary 음 0회 · 레이드 입장 직후 제작 만료 → 전투 중·결과 카드 동안 '대기' → 그 뒤 제작 결과. 페이지 에러 0.
  (길드 레이드가 새 계정에서 0.25~0.5초 만에 끝나는 것은 변경 전 스냅샷도 같다 — 파티가 보스에게 즉시 전멸하는 기존 동작, 이번 변경과 무관.)
**시뮬 기준선 이동**(난수 흐름 변화 + UI 갱신 공짜 궁극기 제거): 100h 최종 CP 32,689 → **32,447**(R 11.5→10.0h · L 79→77h) · 캐주얼 2400h s42 810,942 → 828,792 ·
풀 600h s42 482,190 → 390,325(단일 시드 이탈) — **5시드 중앙값 486,501 → 484,024(−0.5%)** 로 체계적 변화 없음. 이후 리뷰·회귀의 기준선은 32,447.

### 2026-09-25 v5.363 — 3차 발견 A묶음: 첫 세션 흐름(K1 길잡이 9단계 1시간 · K4 소환 결과 잘림 · K5 소환 점·보상 칩)

**3차 발견 워크플로**(5렌즈 발견 → 렌즈별 반박 검증, 10에이전트): 후보 15 → 유지 7(K1~K7). 기각: 망치·강화석 후반 무가치(시뮬 정책 착시)·용광로 시련 고정 적·투기장 즉시 결과·조각 누수(U2 — 자율 적용 부적합)·망치→골드 역교환(보류 확정 경로 우회)·원소 화로의 탑·탑 담금질 3택1(D6 무입력 계약 충돌)·거점 수성전.
- **K1**(검증 영향 4): 길잡이 9단계 고서 제작이 1시간(GUIDE_RECIPE.final sec 3600) — 첫 60분 중 53분이 '[바로가기]'만 뜬 배너로 멈춤, 제작서 920장으로 즉시 완성 가능하다는 안내 0(실측).
  `guideCrafting()` · guideGoldShort 가 결제한 길잡이 제작이면 0(종전: 결제 뒤 잔액으로 '골드 99.8만 부족 [골드던전]' — 레벨업 등에 골드를 쓴 이용자) ·
  배너 '길잡이 9/9 · 고서 제작 중 · N분 남음' + [즉시 완성](제작서 ≥30) · `craftInstantConfirm()` 하나로 통일(대장간 진행 패널·배너 공용 — 이중 차감 방지) ·
  ⚠ 배너 경로는 대장간을 먼저 연다(b2Overlay 는 #modal-root 안이라 모달이 닫힌 홈에선 보이지 않는다 — 실측으로 잡음) ·
  대장간: 모루가 차 있으면 [제작] → '제작 중'/'모루 사용 중' 비활성(종전 금색 [제작] → [제작 시작] → '이미 제작중입니다.') · 결제한 아이템엔 '(N 부족)'·[골드던전] 없음 ·
  첫 렌더 때 진행 패널 버튼 줄(#forgeFin)로 scrollIntoView(종전 [즉시 완성] top 761 > 본문 746) · 10분 넘는 제작 시작 순간 긴 토스트(창 닫아도 진행 · 제작서 30장 즉시 완성).
  루비 상점 '즉시 완성권 X20/X200' → '제작서 X20/X200' — **종전 설명 '제작 즉시완성 20회분'은 틀렸다**(즉시 완성 1회 = 30장, v5.124 에서 비용이 30으로 바뀐 뒤 갱신 안 됨). 가격·수량 불변, 공지로 알림. 대표 확인 사항으로 보고.
  3600초 자체는 줄이지 않았다(기획 판단 — '판단이 필요한 것').
- **K4**: 영웅·재료 소환 결과 격자가 인라인 `repeat(4,1fr)` → 360~414 폭 전부에서 4번째 카드가 모달 밖(HANDOFF 3-5 함정 재발) → `.grid.c4`. verify **[M] 인라인 repeat(n,1fr) 금지** 게이트 신설. 실측: 360/375/414 넘침 0.
- **K5**(1단계): 소환 점(영웅소환권 ≥10 · N/R 로스터 미완 · 튜토리얼 뒤) · 길잡이 단계를 넘긴 제작 결과에 보상 칩(`GUIDE_CHAIN.rn` 이름) + 영웅소환권이면 비금색 [소환하기] · 길잡이 토스트에 재화 이름.
  2단계(재화 아이콘 표 CUR_META 통일 — 📦·🎟️·📜 겹침)는 M 규모라 별건.
- 실물(375×812): 9단계 제작 → 6.5초 뒤 배너 '고서 제작 중 · 60분 남음'·[즉시 완성] · 대장간 [제작 중] 비활성·부족 표시 없음·[즉시 완성] 보임 → 배너 [즉시 완성] → 예 → 길잡이 9/9 완주·제작서 920→890·완주 배너·칩 '주사위 +160' · 소환 점 · 페이지 에러 0.
  smoke '길잡이 제작 중 — 부족 0·배너·즉시 완성 단일 경로 · 보상 칩'. 튜토리얼 실클릭 막힘 0.

### 2026-09-25 v5.362 — 리뷰(v5.359~360) 확정 3건: 상한 재화 보호 누락분

독립 리뷰(3차원 10에이전트, 반박 검증): 확정 3건 · 반박 4건(gboss NaN 고착 경로 없음 · 부재 적립 보류는 의도 · forgeLast 가 pre 경로에서도 갱신 — '마지막으로 본 아이템'으로 수용 · 리더 교체는 이번 diff 밖).
- 광고 제단 '골드 X500,000'만 soldOut 이 빠져 50억 근처에서 광고 1회 소모·무지급·'획득' 토스트(검증 실측, 매달 약 10일 로테이션) → goldRoomBase 판정.
- **같은 계열 잔존(과금 포함)**: 재료를 대가로 파는 곳이 재료 상한을 무시 — 재료상점(루비 600 무지급 실측)·길드상점 흑염석/눈물·루비 패키지 '눈물 X40'·웨이브 상자 재료 교환.
  `matFull(k,n)`(다 받지 못하면 참 — 부분 손실도 차단: 흑염석 1998 에 X5 = +2 만 들어가던 것) → soldOut 배선(mkBuy 렌더 '보유 상한' + 클릭 시점 결제 전 재판정) · 상자 교환은 클릭 가드.
  남은 것(의도적 비대상): 한정 패키지(ACCOUNT_PACKS)는 여러 품목 묶음이라 재료 한 품목 상한으로 전체를 막지 않는다.
- '보유 상한' 라벨이 62px 버튼에서 '상/한'으로 쪼개짐 → `보유<br>상한`. ⚠ 공용 `.shop-card .sh-left .btn` 에 keep-all 을 걸지 마라 — '광고 보고 받기'가 3줄(카드 +20px)이 된다(검증 실측).
- 품절 토스트 '교환할 수 없습니다' → '받을 수 없습니다'(루비 구매에도 쓰이므로).
- 실물: 흑염석 상한에서 길드·재료 탭 버튼 '보유/상한' 2줄 비활성 · 비활성을 풀고 강제 클릭해도 길드코인·루비·재료 불변 · 페이지 에러 0. smoke '상한 재화 보호' 확장.

### 2026-09-25 v5.361 — 영웅 상세·장비창 초상 · 레벨업 연타 스크롤 보존 (2차 미검증 U4)

- 영웅 상세 가운데 그림과 장비창 가운데 큰 칸이 직업 아이콘이라 같은 직업 영웅이 구분되지 않았다 → 초상(assets/heroes/portraits, HERO_PORTRAIT 9종, 없으면 직업 아이콘).
  128px 원화 축소라 `pixelated` 를 쓰지 않는다(칠한 원화 — 계단이 진다). `.hd-por`(92px 액자, 등급색 테두리) · `.por-img`.
- 레벨업·강화·탭 전환마다 heroDetail 이 하위 화면을 새로 만들어 스크롤이 맨 위로 튀었다 → 같은 영웅 상세면 sub-body scrollTop 을 보존(그린 뒤 setTimeout 0).
- 실물(360×640): 상세 초상 로드 OK · 스크롤 109 → 레벨업 3연타 → 109 · 레벨업 버튼 금색 · 장비창 초상 214px 로드 OK · 페이지 에러 0.
  (골드 레벨업 무음·'Lv2' 토스트는 v5.354 growthBurst 로 이미 해소.)

### 2026-09-25 v5.360 — 골드 레벨업 곡선 측정(2차 #4 ②③) · 상한 재화 보호·대장간 기억·장착 전후(2차 미검증 U5·U3·U1)

> ⚠ v5.373 재판정(교정 시뮬): 노출 보류 **결정 유지·근거 교체** — 아래 '초반 약 3배 압축'은 잘린 시뮬 값이라 폐기. 새 근거는 '과매수 함정'(살 수 있으면 사는 P100 은 캐주얼 첫 2주 CP −22~29%). 상세는 v5.373 항목.

**#4 ② 시뮬 `goldlv=P`**(기본 0 = 기준선 불변 — 남는 골드의 P% 이하면 리더 레벨 구매, 제작·장착 뒤): 3시드(42·7·11) 중앙값.
| | 캐주얼 2400h P0 → P10 → P25 | 풀 600h P0 → P10 → P25 |
|---|---|---|
| 최종 CP | 839k → 943k(+12%) → 1,055k(+26%) | 482k → 489k(+1.4%) → 535k(+11%) |
| 200h CP | 64.6k → 101.6k → 98.4k | 203k → 197k → 204k |
| R 등급 도달 | 24.5h → 7.5h → 3.0h | 11.5h → 7.5h → 3.0h |
| L 등급 도달 | 169h → 127.5h → 77h | 79h → 79h → 92h([추정] 골드가 레벨로 가며 결정 가호 20~46회로 감소 — P0 42~43회) |
- 판단(#4 ③): 레벨업을 강하게 노출(영웅 버튼 점·길잡이 안내)하면 캐주얼 초반이 약 3배 압축된다 — **점·안내는 넣지 않았다**. 살 수 있을 때 버튼만 금색(같은 화면 강화 버튼 규칙과 정합).
  노출 강화는 초반 곡선 재설계(사냥 권장 CP·E/L 곡선)와 함께 판단할 일. 풀 프로필은 전투 경험치 레벨이 지배(Lv 239 중 골드 구매 12~17회)라 영향이 작다.
**U5 상한 재화**(반박 검증 없이 코드로 확인 — 2차 미검증 목록): 상한에 막히는 구매·수령이 값만 빼던 것.
- 입장권 30장: 골드 구매(500만)·광고 보상(광고 1회 소모) → `soldOut` — mkBuy 가 렌더 때 '보유 상한' 비활성 + 클릭 시점 결제 전 재판정(v5.356 패턴). 광고 카드도 같은 판정.
- 골드 50억 근처: 루비 환전 2종 `soldOut`(`goldRoomBase(n, raw)` — addGold 관문 배율까지 넣어 판정) · 방치 정산 [수령]은 여유만큼 주고 나머지 대기(부분 지급) ·
  부재 적립 [수령]은 골드가 잘리면 보류(부분 지급하면 일 수·7일 상한 회계가 흔들린다). 방치 수령 토스트는 실제 증가분(종전: 버프 곱하기 전 금액).
**U3**: 길잡이 뒤 대장간은 `S.forgeLast`(마지막으로 본 아이템, freshState '', 표시 설정)로 연다 — 종전 매번 일반/무기/첫 칸. 우선순위 pre > 튜토리얼 > 길잡이 > forgeLast.
**U1**: 제작 결과 [리더 장착]의 전투력 전후를 클릭 시점 heroEntry 로 재해석(팝업이 떠 있는 사이 레벨이 오르면 옛 레벨로 계산됐다).
- **리뷰 v5.357~358**(3차원 12에이전트, 확정 2건 모두 하): ① 주문 창을 연 채 자정을 넘기면(기한 1일 행) [납품]이 화면의 옛 주문으로 판정·토스트하고 orderDeliver(i) 는 새 주문을 처리
  (검증 실측: 보이지 않던 심연 하의 소모 · 토스트/로그 불일치) → 클릭 시점 ordersState() 로 전환 반영 후 행이 바뀌었으면 소모 없이 다시 그림.
  ② 제작 결과 창 금색 2개(리더 장착 + 주문 납품 — 같은 아이템을 두고 배타적) → 주문 납품도 '다시 제작'과 같은 금색 양보 규칙.
  반박: 시간대 이동 되감기 가드(의도된 트레이드오프, 문서화) · 손상 orders 렌더 크래시(조작 데이터 한정) · smoke 동어반복(변이 테스트로 반박) · orderGen 빈 풀(현재 데이터 E30·L43) · 망치·주사위 환산가치(순환 이익 없음 확인).
- smoke '상한 재화 보호 · 대장간 마지막 선택 · 레벨업 버튼 · 장착 전후 재해석'. 남은 미검증(영향 2): U2 비리더 조각 적체 · U4 영웅 상세 중앙 그림·버튼 튐 · U6 몬스터 도감 숙련(신규 콘텐츠).

### 2026-09-25 v5.359 — 길드 토벌: 재의 골렘 단계제 (워크플로 2차 #14, 반박 검증 수정안)

> ⚠ v5.373 재판정(교정 시뮬): 값 **유지**(모든 기준 통과). 아래 단계 곡선·'기준 월 35권 대비 +5%'·'H0 20,000 이면 첫 주기 처치 불가'는 잘린 시뮬 값 — 새 수치는 v5.373 항목(100일 46단계·기록서 +9 = 탑 교환 대비 +4.4~5.3%).

**문제**(실측): 레이드 적 전투력은 항상 내 전투력×2.2 라 성장해도 상대 난이도가 그대로이고, HP 바는 누적 점수로 그린 연출 값이라 15% 에서 영구 정지(누적 40만점부터).
보상은 전투력과 무관(회색 11~29·길드코인 21~39). 매일 2회 하는 콘텐츠인데 진척 축 0. 시뮬에 길드 없음.
**구현**(GBOSS · gbossHP · gbossState · gbossCycLeft · gbossApply · _gbLast · 레이드 cfg.resultExtra · 레이드 화면):
- 단계 k HP = **고정 표** H0×R^(k−1)(H0 8,000 · R 1.18 — 전투력 비례 아님). 참전 1회 = 내 피해(st.dmg, 전투 불변) + 길드원 몫 = 내 피해×1.5(난수 없음). 0 이 되면 처치 → 다음 단계(연쇄 가능).
- 3일 주기(dayIdx/3) — 새 주기엔 HP·참전 수·참전 보상만 리셋, 단계·최고 유지. 주기 감소(시계 되감기)는 무시(#16 원칙). S.gboss(freshState, 이관 플래그 아님).
- 보상: 처치마다 길드 코인 50·주사위 20 · **기록서는 5단계마다 1권**(검증: 매 처치면 월 +28% — ✦ 병목) · 참전 보상 4단(주기 내 1·2·4·6회 → 길드 코인 5·10·15·20 — 전투력 무관 참여 축).
- ⚠ 기존 참전 보상 4줄(guildRaidScore·guildScore·gray·guildCoin)은 한 글자도 안 바꿨다(guildScore = 칭호 조건). foeCP 공식·Battle·결정론 무접촉(해시 불변).
  전투 중 보스 HP 바는 판마다 가득 찬 보스(전투 쪽 불변) — 공유 풀 변화는 결과 카드 한 줄('N단계 처치! → N+1단계 등장' / 'HP a% → b%')과 레이드 화면에서만.
- 레이드 화면: 초상 '재의 골렘 N단계' · 실제 HP 바·수치 · 주기 D-n · 최고 단계 · 참전 수 · 참전 보상 4칸(✓) · 규칙 한 줄. 종전 15% 멈춤 주석은 근거 주석으로 교체.
**시뮬**(balance-sim `guild=1`, 기본 꺼짐 = 기준선 불변 · `guildcal=` 기본 0.17 = 실전투 스윕 중앙값, 판당 0.14~0.29 · 약탈 활성화 끔 기준):
- 캐주얼 2400h 3시드: 3일 2단계 · 7일 6 · 14일 16~17 · 30일 25~26 · 60일 31~32 · 100일 34~35단계. 주기 34개 중 0처치 14~15·1처치 12~13·2+처치 7(초반 급성장기).
  기록서 +6/100일(월 약 1.8권 — 기준 월 약 35권 대비 +5%, 검증 한도 +10% 이내). 길드 코인 +약 34/일(참전 기본 약 70/일 대비). 풀 600h(25일) 29단계.
- H0 를 20,000 → 8,000 으로 내린 이유: 20,000 이면 캐주얼 3일차에도 1단계(첫 처치를 첫 주기에 못 봄). 참전 보상을 주기당 75 → 50 으로 줄인 이유: 길드 코인 유입 +55% → 약 +48%.
- 측정 못 한 것: 길드 코인 소비(길드 상점 — 영웅 소환서 20장 = 200)는 시뮬 미모델 → 전투력 영향 미측정[추정: 소환서 하루 3장 수준]. 약탈 활성화(피해 +50%) 켠 이용자는 단계가 더 빠르다(cal 0.23~0.31).
- smoke '길드 토벌 — 단계 HP 표·주기·처치 보상·기록서·참전 보상·기존 적립 불변'. 실물(375×812): 레이드 2회 — 'HP 100% → 97%', 처치 판 '1단계 처치! → 2단계 등장 · HP 98%'
  + 칩 길드코인 +110·주사위 +20, 즉시 저장, 레이드 화면 '재의 골렘 2단계 · HP 9,293 / 9,440 (98.4%)' · 참전 보상 2칸 ✓ · 가로 넘침 0 · 페이지 에러 0.
  QA 함정: 결과 카드의 3초 자동 복귀 타이머가 살아 있는 동안 다음 레이드를 강제로 열면 그 타이머가 새 결과 카드를 닫는다(스크립트 경로 — 실제 UI 는 결과 카드가 화면을 덮는다).

### 2026-09-25 v5.358 — 대장간 주문서 (워크플로 2차 #3, 반박 검증 수정안) + 리뷰(v5.354~356) 확정 1건

> ⚠ v5.373 재판정(교정 시뮬): **유지**. 단 '결정 가호 밀려나지 않음'은 틀림(6런 중 5런 −7~−24% — 시뮬 정책 순서 탓, 매출 무관), '망치 구매 대체'는 풀에서만 성립. 새 수치는 v5.373 항목.

**문제**: 장비를 다 맞춘 뒤 '제작'이 죽는다 — 시뮬 풀 600h 는 168h, 캐주얼 2400h 는 240h 이후 업그레이드 제작 0회, E/L 재료는 보유 상한(900)에서 버려지고 분해는 항상 손실.
**구현**(ORDER_* · orderGen · ordersState · orderCands/orderReady/orderClaimable · orderDeliver · openOrders(subBody — 새 MODALS 키 아님) · ordStripHTML):
- 해금 = 리더 10부위 전부 L(slotKeyOf) + 튜토리얼 뒤, 한 번 열리면 유지(S.orders.on). 매일 3칸 — 0·1번 E(1~2개), 2번은 날짜 해시로 절반 L(1개). '특수'(고서·물약 보유 효과) 제외.
- 생성 = 날짜 문자열 FNV 해시 결정론(Math.random 없음 — 새로고침·시뮬 재현성). 전환은 isLaterDay(#16) — 되감기로 새 주문 불가. 미완료는 받은 날 포함 3일 유지(캐주얼).
- 보상 ORDER_RW: E×1 망치1·주사위10 / E×2 망치2·주사위20 / L×1 망치4·주사위40·원시 골드 200만(< L 제작가 50% — 제작→납품 순환 이익 없음). 기록서·탑 상자·강화석 제외(✦ 병목·가치 없음).
- 납품 = 미착용·영웅 귀속 없음·강화 0 인 같은 이름·등급 장비. 판정·소모·지급·저장이 orderDeliver 한 곳(클릭 시점 재조회, 즉시 save). S.stats.orders 누적.
- 화면: 대장간 위쪽 줄(⚠ 등급 탭 줄 안에 넣지 마라 — 탭 on 토글이 GORDER 인덱스로 children 을 짚는다) → 주문 창(행: 아이콘·보상·보유 n/qty·남은 기한, [납품]/[제작]=그 아이템 사전 선택 대장간)
  · 납품 도장 연출(ordStamp) · 제작 결과 창에서 주문을 채우면 금색 [📜 주문 납품 (n/q) ▶] · [제작] 버튼 점(지금 납품 가능) · 해금 순간 토스트·로그.
**시뮬**(balance-sim `orders=1`, 기본 꺼짐 = 기준선 불변 · `orderres=백만` 제작 골드 예약선 기본 30 — 135 면 18일 중 3건만 납품돼 정책이 사실상 꺼졌다):
| 5시드 중앙값 | 풀 600h 기준 → 주문 | 캐주얼 2400h 기준 → 주문 |
|---|---|---|
| 최종 CP | 486,501 → 498,191 (+2.4%) | 825,931 → 833,564 (+0.9%) |
| 결정 가호 | 43 → 42회 | 227 → 272회 |
| 망치 구매 골드 | 1,200M → 760M | 1,880M → 960M |
| 해금 · 납품 | 168~192h · 약 2.9건/일 · 제작 5.8회/일 | 216~312h · 약 3건/일(약 89일 265건) |
- 판단: CP ±3% 이내·결정 가호 밀려나지 않음(검증 기준 충족). 주문 망치가 망치 구매를 대체 → 남는 골드가 결정 가호로. 시뮬 제작은 즉시 판정이라 모루 시간(E 1h·L 2h)은 반영 안 됨(상한 추정).
  '제작 비중' 지표는 주문을 넣으면 당연히 오르는 자기확증형이라 판단에 쓰지 않았다(검증 지적). 리텐션 효과는 측정 불가[추정].
- smoke '대장간 주문 — 해금·결정론·납품 대상·이중 수령·순환 이익 없음'(60일 생성 분포·특수 제외·착용/귀속/강화 제외·이중 수령·기한·되감기·구세이브).
- 실물(Playwright 375×812): 해금 → 대장간 줄 '0/3 · 납품 가능 1' → 주문 창 3행 → [제작] 이 주문 아이템 선택 → 제작 결과 [📜 주문 납품 (1/1)] → 납품: 망치 +1·주사위 +10·도장·즉시 저장·줄 갱신. 페이지 에러 0.
- **리뷰 v5.354~356**(3차원 7에이전트): 확정 1건(하) — 영웅 상세 [등급업 합성] 경로에서 합성 카드 깜빡임이 heroDetail 하위 화면에 가려진 채 실행 → fuseFocusCard 가 #modal-root 직속 .sub-ovl 이 있으면 생략(⚠ closeSub 로 상세를 닫지 마라).
  반박 2건 + '투기장 전투 중 주간 정산' 은 검증자 판단대로 수정 안 함(최종 상태가 이용자에게 유리 · 전투 중 유예는 자동 연전 중 정산 지연을 되살린다).

### 2026-09-25 v5.357 — 기기 시계 되감기 반복 지급 차단 (워크플로 2차 #16, 반박 검증 통과)

- 발견(실측): 기기 날짜를 하루 앞뒤로 돌릴 때마다 일일 초기화(입장권 +5·던전·탑 되살아남)·접속 보상·투기장 티어 골드·출석이, 월을 앞뒤로 돌릴 때마다
  탑 월간 정산(주사위 800 — 일일 임무 주사위 약 13일치)이 반복 지급됐다. 복귀 적립(awayBank.hi)만 막혀 있었다. 원인 = 전부 '다르면 넘김' 동등 비교.
- 수정 원칙: **저장된 날짜·키 자체를 고수위로 — '나중일 때만' 넘긴다**(`isLaterDay` · `weekOrd`/`monthOrd` · 투기장 주차 날짜 비교). 되감긴 동안은 초기화·지급 없이 그날 카운터 유지.
  적용: rollDaily · 출석 4곳(`attendDoneToday`) · weeklyState · monthlyState(탑 정산·용광로 시련 재개방 포함) · arenaWeekRoll.
  **오프라인 골드도 같은 구멍**(발견엔 없었음 — 시계 하루 앞 = 12시간치, 되돌렸다 다시 앞으로 = 또 12시간치): `S.offHi`(정산을 마친 최고 벽시계) —
  computeOffline·_visibilitySettle 은 max(시작, offHi) 부터 세고, 보이는 동안 5초 틱(_wallGapCheck)이 offHi 를 지금으로 올린다(켠 채 되돌렸다 제자리도 0).
  ⚠ 숨김·숨김 정산 대기 중엔 offHi 를 올리지 마라 — 복귀 때 숨김 구간 정산이 0 이 된다. offHi 는 freshState 0(이관 플래그 아님 — 0 = 기록 없음).
- **재기준(크게 되감으면 다시 잡기)을 넣지 않은 이유**(검증 지적): 3일 되감아 재기준 → 하루씩 앞으로 = 4번 바꿀 때 3번 지급이 다시 생긴다. 임계를 키워도 '바꾼 횟수당 지급'은 같다.
  대가(공지로 알림): 시계가 앞서 있다가 교정된 이용자는 실제 시각이 마지막 처리일을 지날 때까지 초기화·오프라인 정산이 없다(이미 앞당겨 쓴 날).
  일일 hi 를 별도 필드로 두지 않은 이유: smoke 가 `S.daily.date='2000-01-01'` 로 롤오버를 강제하는데 별도 hi 는 그 경로를 막는다.
- 남은 약점(의도적 비대상): 앞으로만 가는 조작(미래를 당겨 쓰기)은 클라이언트 단독으로 막을 수 없다 — 대신 되돌아오면 그만큼 초기화가 멈춰 스스로 상쇄된다.
  제작 타이머(S.craft.endAt)·투기장 입장권 40초 충전도 시계 앞당김에 반응한다(입장권은 상한 30 = 실시간 20분치라 경미). 네트워크 코드 0 — 다른 이용자 공정성·매출 영향 없음.
- smoke '시계 되감기 무지급 — 일일·출석·주·월·투기장·오프라인 · 앞으로는 정상'(**구 코드에서 8개 항목 전부 실패 확인**). 과거 시각을 주입하는 기존 정산 검사 5곳은 `S.offHi=0` 으로 시작.
- 실물: `docs/qa/20260925-시계되감기-반복지급-실물QA.cjs`(Playwright clock — 그 시각에 저장·봉인 → 시계 변경 → 새로고침, 11단계) PASS:
  9/25→9/24→9/25→9/24 동안 일차·입장권·골드던전 소진·출석 불변, 9/26 정상 롤오버 · 10/1 월 정산 800 1회 → 9/30 → 10/1 재정산 0 ·
  하루 앞 오프라인 6,798,960(12h) → 되감기 → 제자리는 고수위 이후 5분치(47,215)만. 페이지 에러 0.
  QA 함정: 시계를 바꾼 뒤 봉인 없이 새로고침하면 unload 저장이 lastSeen 을 새 시각으로 덮어 오프라인 정산이 0 으로 보인다(실제 이용자 경로 아님).

### 2026-09-25 v5.356 — 투기장 순위 버프 주차 가드·주간 정산 자동 (워크플로 2차 #2) + 리뷰(v5.350~353) 확정 3건

> ⚠ v5.373 재판정(교정 시뮬): 완화 보류 **유지**. 아래 '중반 2~3배·최종 CP 잡음 범위'는 잘린 시뮬 값 — 실제 장기 효과는 CP +5~10%(2400h +3~5%, 실재), 2~4배는 캐주얼 100~120h 경계 효과뿐. 새 수치는 v5.373 항목.

**#2 투기장 순위 골드 버프(최대 +90%)** — 발견: 주당 ~40판이면 1~3위, 버프가 사냥·골드던전·탑·방치 골드에 곱해져 성장 압축.
- 1단계(적용): `arenaGoldBuffPct` 가 **지난 주차 순위면 0**(종전: 투기장을 다시 열어 리셋되기 전까지 지난주 버프가 계속 붙었다 — 이용자 불리 정정이라 [수정] 공지).
  `arenaWeekAuto()` — 한 번이라도 참여한 이용자는 홈 진입·5초 주기에서 주간 정산(주사위)·초기화를 받는다(종전: 투기장 화면 진입 때만). 미참여자는 제외(매주 초기화 토스트 소음).
- 2단계(측정 도구): balance-sim `arena=N`(주당 판 수, 기본 0 = 기준선 불변) · `arenawin=P`(승률 상수) · **`arenawin=real`(실전투 — arenaFight→헤드리스 펌프→arenaResult 정본)**.
  **실측 승률 92.5~93.8%**(casual 150/600h·full 200h, 80~160판, 초·중·후반 동일) — 적 CP 는 맞춰도 적 3인(N/R·레벨=CP 환산)이 같은 CP 아군보다 약하다.
  v5.331 주석의 '매칭상 승률 ~50%'는 추정이었고 틀렸다(ARENA_TIER_ROWS 주석 정정 — 1/10 결론은 유지).
  영향(상수 0.95 모델, 주 40판): casual s42 150h CP 22,331→61,965 · 200h 64,571→92,623 / s7 150h 21,532→72,987 · full 100h 32,689→99,651. **~300h 이후 수렴, 최종 CP 는 잡음 범위**(s42 810,942→710,496 · s7 839,493→879,099).
- 3단계(하지 않음 — 판단 근거): 순위 상승식 완화 후보를 계산했다(주 40판 기준: 현행 ~3위 / 완화안 c ~152위 / d·e ~90위, 160판이면 5~8위).
  그러나 이는 **라이브 이용자가 지금 받는 버프의 너프**다 — v5.331 검증자가 점령 주간 리셋을 'U1 위반'으로 기각한 선례와 같은 유형이라 넣지 않는다.
  매칭 강도(적 3인 실전력) 보정도 같은 이유로 보류. 되돌릴 근거가 생기면(초반 이탈·콘텐츠 소진 속도 지표) 이 수치로 다시 판단할 것.
  ⚠ 전투 공식·적 편성은 건드리지 않았다(결정론 해시 불변).

**리뷰 v5.350~353(독립 다차원 리뷰 + 반박 검증, 8에이전트) 확정 3건 반영**:
- 회색코인 교환 경합: 목록 필터는 그릴 때 1회라, 창을 연 채 사냥 드랍이 마지막 재료를 상한까지 채우면 '코인만 빠지고 무지급·토스트는 획득'이 재발(검증자 실브라우저 자연 드랍 재현)
  → `mkBuy(…, soldOut)` 가 **클릭 시점·결제 전에** 다시 판정(환불 로직 불필요). GRAYSHOP 호출부만 전달.
- 리더 교체 안내 72자가 1.9초 토스트(실제 1.5초부터 사라짐) → `toast(msg, ms)` 선택 인자(사라짐 애니메이션 지연도 같이 — CSS 1.5초 고정이라 setTimeout 만 늘리면 투명한 채 남는다) + `.toast.long` 폭 300px.
  기본 토스트 229곳 불변. 안내 6초, 채팅 기록에 장비 안내 포함.
- 가져오기 사전검사(mergeDefaults 만) ≠ 로드 이관 단위(mergeDefaults+computeOffline) → `migrateLoaded()` 하나를 둘이 같이 쓴다.
  computeOffline 은 숫자 아닌 lastSeen·offlinePending 을 0 으로 본다(종전 TypeError — 검증자가 `{valueOf:1}` 로 가져오기 통과→복구 화면 갇힘 재현).
- 반박된 2건: load() 이관 실패 catch 뒤 freshState(디스크 원본 보호 확인) · _wallGapCheck 로그인 전 발동(_loopOn 게이트 확인).
- smoke '리뷰 반영 — 교환 클릭 시점 품절 · 긴 토스트 · 가져오기 검사 = 로드 이관'(상점 렌더→상한 도달→클릭: 코인 불변 / 대조: 정상 교환), '투기장 순위 버프 주차 가드 · 자동 주간 정산 대상'.

### 2026-09-25 v5.355 — 길잡이 완주 뒤 '다음 할 일' (워크플로 2차 #5)

- 완주하면 배너가 사라지고 1.9초 토스트뿐이라 약탈 위치·오늘 남은 모험 12회가 안 보였다(실측) → 완주 순간 1회 배너(S.guideFinBanner 0/1/2, [모험] 버튼 → 모험 화면, 열면 2 로 끝)
  + 모험 버튼 점 advDotOn(오늘 모험 화면 미열람·남은 입장 있음, S.advSeenDay 로 소등). 두 필드 모두 freshState(이관 플래그 아님) — 기존 완주 세이브는 0 이라 배너 없음.
  ⚠ advDotOn 에 forgetrial 을 넣지 마라 — advLeftBadge('forgetrial') 가 monthlyState 를 불러 5초 배지 틱에서 월 롤오버(탑 정산)를 일으킨다(smoke 잠금).
  상시 순환 '강해지는 길' 배너는 검증 결론대로 제외. 완주 시스템 로그를 조건형→완료형 문구로. #side-float .fb position:relative.
- 실물: 8→9단계 완주 → 배너 '길잡이 완주 · 약탈이 열렸습니다 — 매일 던전·탑은 [모험]에서' + 모험 점 → [모험] → 모험 화면·배너 사라짐·점 꺼짐. 튜토리얼 실클릭 완주.

### 2026-09-25 v5.354 — 2차 발견 C묶음: 성장·보상 순간 연출 (워크플로 2차 #9·#11·#10·#4 ①·#15)

- **#9 합성 등장** fuseRevealFx: 초상·등급색 광선·'일반 → 희귀'·총 전투력 전후(실물 260→468 +80%) · 확인 뒤 새 카드로 스크롤·깜빡임. 카드·상세 두 경로 통일(상세는 종전 craft 음·일반 토스트).
  heroRevealFx 와 별도 함수(smoke 가 소환 경로 호출 수를 센다). ⚠ 버튼 문구 '확인' 유지 — 튜토리얼 7단계 손가락이 TUT_PRIMARY_TEXT 로 짚는다(실클릭 워커: s7 확인 → 진영 선택 → 완주 막힘 0).
- **#11** 세트 발동 = growthBurst 전후 카드(종전 토스트 2줄·전투력 +97% 미표시) — 장착 경로 2곳의 setTier 람다 복제를 setTierOf/notifySetGain 으로 통일.
  칭호: titleSyncOwn(notify) 가 새로 기록한 칭호를 반환하고 5초 주기 경로(true)에서만 토스트·승리음·로그(튜토리얼·인트로 중 무음, 로드·칭호 화면은 조용히 기록).
- **#10** 보상 수령음 claimSfx(claim 상승 2음) 9곳(오프라인·부재 적립·일일·주간·월간·출석·우편 단건·일괄). 골드 카운터 맥동은 방치 0.5초 틱분의 약 3배 이상 증가에만(setCur minDelta — 종전 3초 중 61% 부풀어 의미 상실).
- **#4 ①** 골드 레벨업: 클릭 시점 가격 재조회 + growthBurst 전후 카드(실물 Lv2 208→256 +23%). ②③(시뮬 goldlv 정책 → 버튼 금색·점 노출)은 시뮬 확정 뒤로 보류.
- **#15** 탑 웨이브 돌파 waveBanner(DOM 띠 + 승리음, 이번 도전에서 최고 기록을 처음 넘는 웨이브면 '🏅 신기록' + legendary — 상태 없이 n===max(2,이전최고+1)),
  남은 시간은 Wave 와 같은 줄 오른쪽(종전 둘째 줄이 영웅 레벨 배지와 붙어 '60s'·'60' 한 덩어리) · 돌파 직후 0.6초 초록. _instantRun 가드·dg/시드/흔들림 미접근(smoke 잠금). 결정론 해시 3종·D5·D6 불변.
- smoke '2차 C묶음 …' · 즉시 저장 앵커 갱신(우편 단건 claimSfx 삽입).

### 2026-09-25 v5.353 — 2차 발견 B묶음: 이관 실패 세이브 덮어쓰기 금지·복구 화면 · 창을 닫지 않은 부재 정산 · 리더 교체 안내·합성 점 (워크플로 2차 #17·#18·#6)

- **#17** load() 가 파싱과 이관을 한 try 로 묶어, 유효 JSON 인데 mergeDefaults 가 예외를 내면(예 stats:null·shards 문자열) 5초 뒤 새 게임으로 덮었다(실측 7.7억 세이브 초기화).
  이제 이관 실패 = 코드 결함: 슬롯 원본 유지 + _migfail_ 백업 + 봉인(_saveSealed) + 복구 화면 showLoadFail(#tab-lock 재사용: 원본 복사·다시 시도·새로 시작 두 번 눌러 확정).
  ⚠ [새로 시작]에 styledConfirm 을 쓰지 마라 — #modal-root(z20) 안이라 #tab-lock(z≥71) 뒤에 깔려 누를 수 없다(실물 발견). 가져오기는 mergeDefaults 사전 실행으로 거부.
  실물: 봉인한 첫 창에서 불량 세이브 주입 → 재부팅 → 복구 화면·슬롯 777,777,777 유지(6초 뒤에도)·_migfail_ 1개. (QA 함정: 첫 창을 봉인하지 않으면 그 창의 unload 저장이 주입 세이브를 덮어 가짜 실패가 난다.)
- **#18** ① 화면을 켠 채 절전(visibilitychange 없음)은 방치 골드 0 이었다 → 5초 주기 _wallGapCheck: 보이는 상태·숨김 정산 대기 없음에서 벽시계 간격 >60초면 _visibilitySettle(이중 정산 없음 — rAF dt 0.1초 상한).
  ② 숨김 탭으로 며칠 두면 5초 주기 rollDaily 가 매일 돌아 부재 적립 0 → 숨김 중 날짜 전환은 S._awayFrom(freshState, 이관 플래그 아님)만 붙잡고 보일 때/다음 부팅 awayCatchUp 이 적립.
  검증자 재현 스크립트로: 켠 채 3시간 절전 pend 0→1,700,220 · 48시간 +12시간 상한 · 숨김 3일 부재 적립 0→2일(닫았다 연 경우와 동일). 세션 중 적립도 토스트.
- **#6** 편성이 비면 리더 = 보유 최강(조용히 교체) → 옛 리더 장비가 홈·탑 솔로에서 빠짐: tickLeadWatch(편성 빔·옛 리더 장비 ≥1·60초 쿨다운·로드 직후/전투 중 무음) 토스트,
  제작 결과 장착 버튼 아래 '홈 출격 = 보유 중 가장 강한 영웅(자동)' 한 줄, 합성 가능 영웅이 있으면 영웅 버튼 점(heroFuseAvail — 튜토리얼 중 끔). 표시 전용.
- smoke: '이관 실패 세이브 — …'·'절전 간격 정산 · 숨김 날짜 전환 부재 적립 · 합성 가능 점'. 부팅 소스 슬라이스 검사를 load()~wire() 구간으로 견고화. 튜토리얼 실클릭 완주.

### 2026-09-25 v5.352 — 2차 발견 A묶음: 골드던전권 실사용·재료 상한 무지급/표시·주월 롤오버·길잡이 다음 목표·처치음 (워크플로 2차 #1·#8·#13·#7·#12)

2차 발견 워크플로(5렌즈 27에이전트, 후보마다 반박 검증): 채택 18 · 기각 4 · 저영향 미검증 6 — 결과 scratchpad wf2_result.json.
- **#1 골드던전권(S.goldTicket)** 은 루비 상점 X10/X30·광고로 팔고 정산 화면에도 보이는데 **읽는 코드 0건**(과금 후 무지급). v5.330 원칙대로 약속 이행:
  일일 3회 소진 뒤 1장당 1회 추가 입장(확인창 sub 에 권 사용 표기, 차감 즉시 save). ⚠ 자동 입장 연전은 일일 횟수 안에서만(auto 경로·autoNext 이중 차단) — 권을 연달아 태우지 않는다.
  시뮬은 goldTicket 을 읽지 않아 기준선 불변. 선례: tickMon 폐지·환불(2026-09-10)·v5.330 버프 이행 모두 부서 자율.
- **#8 재료 상한**: matGain 이 실제 증가분 반환, matGradeCapped, matGainGrade(opt.avoidCap). 회색코인 재료 교환은 avoidCap + 등급 전부 상한이면 soldOut
  (종전: 코인 −35·재료 불변·'획득' 토스트). 요일던전은 실제 증가분 sysLog·결과창 상한 경고(capNote)·목록 '보유 상한 — 보상 없음'. **사냥 드랍·요일던전의 균등 선택은 그대로**
  (유효 재료 유입이 바뀌면 곡선이 바뀜 — 2단계는 시뮬 확정 후).
- **#13** 창을 켠 채 월요일 0시·매월 1일을 넘기면 퀘스트 탭을 열기 전까지 주·월 스냅샷이 없어 진행이 빠졌다(실측 3,005킬) → 5초 주기에서 _loopOn·보일 때 weeklyState/monthlyState/flushLoginToasts.
  검증자 재현 스크립트(si_e2_weekonline)로 수정 후 월 00:01 에 W40·base 10000 확인(3,004킬이 새 주에 들어감).
- **#7** 길잡이 단계를 넘긴 제작 결과는 대장간을 다음 목표로 열고 [다음 길잡이 · 방패 ▶](종전: 방금 만든 아이템에 머물러 150만→5,000 비길잡이 [제작]이 주 버튼, 매 단계 4탭).
- **#12** 처치음: 같은 오디오 시각 coin 합치기(최대 3배)·리미터 버스(DynamicsCompressor, 단일 음은 문턱 아래)·우두머리 처치 bossdown(legendary 희소성)·pointerdown 시 AudioContext resume.
- smoke '2차 A묶음 …'(권 자동 연소 차단·수동 권 입장·상한 판정·avoidCap·롤오버 코드·다음 길잡이 버튼 — 전제 불성립 시 실패). 튜토리얼 실클릭 완주 막힘 0. 공지 [수정].

### 2026-09-25 v5.351 — v5.344~349 독립 리뷰 반영: 탑 월간 정산 알림이 그 세션에 뜨도록

- 리뷰 확정 1건(하): 정산 알림이 접속 보상 머리말로 오표기(v5.350 에서 {msg} 로 해소) + **실제 증상은 더 나빴다** — monthlyState 는 enterHome 에서 처음 불려
  큐에 쌓이는데 플러시가 그보다 앞이라 정산 세션엔 알림 0건, 다음 로드 때 뜸(검증 실측). → flushLoginToasts 를 weeklyState/monthlyState 호출 뒤로.
  실물: 지난달 도전 세이브 → 재접속 → 홈에서 '🗼 시련의 탑 월간 정산 — 17위 · 주사위 +800' 즉시·큐 비움. smoke 에 enterHome 호출 순서 잠금.
- 반박 8건: 하위 2구간(31~40위·참가) 도달 불가는 30명 목록상 의도(최소 600 — 공지·테스트와 일치, 표의 죽은 행만 남음)·월간 정산 시계 조작은 기존 월간 의뢰와 같은 수준·
  heroEnh 가 totalCP 기반 적 전투력을 올리는 구조는 기존(레벨·장비와 동일)·라벨 폭(캔버스 453 고정)·34px 버튼이 영웅 카드 합성 배지/그리드에 준 영향은 결함 아님 등.

### 2026-09-25 v5.350 — 복귀 적립(부재 최대 7일) (워크플로 #26) · 접속 보상 토스트를 홈 진입 후로

- 비운 날마다 탑 소탕(최고 기록×20만 골드·×1.5 강화석·상자 floor(best/4)) + 잔불 최고 문 몫을 S.awayBank 에 적립(최대 7일), 방치 정산 화면 위쪽 카드 [수령](원시 골드)·시계 점·홈 진입 안내.
  검증 반박 반영: ① 일 수 = 로컬 자정 간격 반올림(dayIdx — ms/864e5 내림은 서머타임 주에 하루 누락) ② 최고 처리 일자 hi 이후만 셈(시계 앞뒤 반복 적립 차단)
  ③ 금액은 적립 시점(부재 전) 진행도로 확정(수령 시점이면 복귀 후 기록 올리고 받아 천장 누수) ④ 매일 콘텐츠 기록 0 이면 몫 없음. 천장: 매일 하는 이용자 몫을 넘지 않는다.
  효과 과장 금지 — 14일 부재 격차의 약 9% 회복(검증 시뮬). 실물: 5일 전 세이브 → 4일 적립(골드 2,800만·강화석 480·상자 20) → 수령 1회·hi 보존.
- 접속 보상 토스트(_pendingLoginToast)는 부팅 직후(타이틀 화면)에 떠서 사라졌다 → enterHome 의 flushLoginToasts 로. {msg} 객체는 '🎁 N일차 접속 보상 —' 머리말 없이
  (v5.348 탑 월간 정산 알림에 접속 보상 머리말이 붙던 것 — 검증 지적). 방치 정산 '오프라인 골드 720:00'(12시간) → '12시간 00분'.
- smoke '복귀 적립 — 일 수·7일 상한·시계 조작 차단·금액 확정·수령'. 공지 [업데이트].

### 2026-09-25 v5.349 — 시뮬 탑 도달 모델 실전투 보정 (워크플로 #25) · 안전 사냥터 정의 단일화(리뷰) · #27 대표 결재 대기

- balance-sim TOWER_CAL = 2.2(로스터 중앙값): 종전 1:1 공식은 모든 측정점에서 실전투보다 4~8 Wave 낮았다(K 1.7 근접 ~ 2.9 원거리 고전투력, ±1~2 Wave 오차 잔존).
  `towercal=1` 로 종전 모델 재현(600h 풀 CP 560,259 동일 확인). 보정 후: 600h 탑 22→25 Wave·기록서 4→8 · 2400h 캐주얼 24→28 Wave.
  단일 시드 최종 CP 는 세트 경로 차이로 ±15% 흔들리므로(600h 560k↔482k) CP 비교는 시드 5개 중앙값으로 할 것. 게임 코드 영향 0.
- v5.341~343 독립 리뷰: 확정 1건(하) — showWipeAdvice 가 huntSafeTier 와 같은 계산을 인라인 복제 → huntSafeTier() 호출로 단일화. 반박 6건(투기장 결정론 커버리지 공백은 기존·
  impact O(n) 은 dmgText 보다 가벼움·320px 폴백 오탐·제작 팝업 대장간 진입은 의도·토스트 겹침은 기존 등). 리뷰어가 스냅샷 고정 튜토리얼 완주·실루엣 무번짐 확인.
- **#27 L 제작 천장 — 대표 결재 대기(돈 사안)**: 검증 결론의 형태(연속형 3실패 후 다음 L 1회 확정·성공 시 초기화·시간 스킵 없음·'불씨' 아닌 명칭)면
  캐주얼 최악 시드 -17%·풀 L 구간 -12.5% 로 불운 꼬리를 줄이지만, 10부위당 유료 확정 약 2.2회 = 제작서 약 530장(루비 약 7,400 상당)을 대체한다.
  유료 경로(확정 제작서) 수요를 줄이는 매출 영향이라 자율결정 범위 밖 — 구현하지 않았다. 판단 자료: 워크플로 후보 #27 검증 문서(scratchpad wf_result.json).

### 2026-09-25 v5.348 — 시련의 탑 순위 재조정·월간 순위 보상 실제 지급·내 순위 보이기 (워크플로 #24 (b) 묶음)

- TW_RANK 재스케일(이름·길드 유지, 숫자만): 종전 73~312 Wave 는 시뮬 최고(풀 12800h 32)의 두 배 이상 → 모든 이용자 영구 30위.
  새 값 45…12 → 22 Wave=17위(동점 NPC 가 위 — dgRankList 안정 정렬, towerRank 가 같은 규칙)·25≈14위·32≈9위, 1위 45 는 장기 목표.
- TOWER_REWARD 는 표에만 있고 지급 코드가 없었다 → monthlyState 월 경계에서 지난달 1회 이상 도전(towerTries 증가)했으면 그 시점 순위로 주사위(표 파싱 = 지급, 여러 달 비워도 1회).
  기록(S._tower)은 초기화하지 않는다(소탕 보상이 최고 기록 기준 — 초기화는 이용자 손해). 종전 '매월 15일 초기화'는 구현된 적 없는 문구 → '매월 1일 지난달 순위로 정산 · 기록 유지'.
  경제: 22 Wave 기준 월 800개(일일 임무 주사위 월 약 2,850 의 약 28%).
- 순위 목록(176px 스크롤 상자)이 내 줄로 자동 스크롤. 보상 화면에 '내 순위 N위 · 이번 달 정산 예상 · 도전해야 지급'. 공지 [업데이트].
- 보류: 월드보스 랭킹 보상표(🎒 X30…)도 지급 코드가 없다 — 보상 품목(🎒)이 판독되지 않아(⚠비전미확인) 이번엔 손대지 않았다. 품목 확정 후 같은 방식으로.
- (a) 권장 전투력 막대·(c) 정체 완화는 검증 결론대로 보류. 작업 중 node -e 이스케이프로 smoke 에 제어문자(0x08)가 들어간 사고 → 전 소스 제어문자 스캔 0 확인.

### 2026-09-25 v5.347 — 영웅 강화 전 등급 개방 (워크플로 #12 — 조각 소비처·죽은 탭·달성 불가 칭호)

> ⚠ v5.373 재판정(교정 시뮬): 비용 곡선 **유지·근거 수정** — '고정 300 이면 600h 소진'은 공격적 구매에서만 성립, '다른 싱크를 밀어내지 않음'은 곡선이 아니라 135M 구매 정책의 성질, '창당 1팩'은 실제 하루 1회. 풀 600h 영웅 강화 0단계·남는 조각 7,880개(85% 파티 밖 직업)는 후속 과제. v5.373 항목.

문제: 강화 자격이 '레전더리 이상'인데 로스터가 N5+R4 라 **누구도 강화할 수 없었고**, 효과(4강화당 발사 횟수)는 표시뿐 어디에도 반영되지 않았다.
9/9·기본 각성 12 이후 조각 소비처 0 → 소환권·조각 상품 사장(2400h 캐주얼 잔여 조각 3,001). 칭호 3종(L 영웅 1명/5명, +20강화 2명)은 영구 달성 불가.
- 효과 = heroPower 순수 배율 항 heroEnhMul(hid) = 1 + 0.01×단계(최대 20). ⚠ 발사 횟수를 전투에 넣지 마라 — RNG 소모 수가 바뀌어 D1 이 깨진다(v5.216 선례).
  전투력 구성 화면에 '영웅 강화 +N' 행. 강화 확정 즉시 save·refreshParty(종전 onclick 에 없었다). 죽은 heroEnhShots/heroEnhSkillName 제거.
- 비용 heroEnhCost(lv) = 300×(1+0.25×lv) → 영웅당 +20 약 20,250 조각. **고정 300 을 버린 근거(시뮬, 시드 42)**:
  고정 300 이면 풀 600h 에 파티 3명 +19 소진(최종 CP +12%)·골드가 소환서로 쏠려 망치 1,480M→80M·결정 가호 57→1회.
  체증 + 시뮬 구매 정책(강화 목적 소환서는 잔액 135M 이상 여유에서 창당 1팩)으로: 600h 풀 CP 552,664→560,259(+1.4%, 2단계) · 망치 1,440M(≈) ·
  2400h 캐주얼 800,458→825,931(+3.2%, 파티 각 +4) · 결정 가호 278→309회 · 기록서 골드 구매 1,560M→1,500M. 다른 싱크를 밀어내지 않는 장기 축.
- 칭호 재매핑: 화로의 스승 = 영웅 1명 +5 · 책략가 = 영웅 3명 +10 · 정점의 지배자 = 영웅 2명 +20(개발 메모 '추정 — 미판독' 노출 문구 정리). 착용형이라 효과는 겹치지 않는다.
- 문구: 그 직업에 윗 등급 영웅이 없으면 '모두 보유 중'(거짓) 대신 '다음 등급 영웅은 아직 없습니다 — [강화] 탭'. 강화가 같은 직업 윗 등급 합성 조각을 모자라게 하면 '합성 먼저' 경고.
- balance-sim: heroEnhStep(리더→파티 2·3번, 200 여유) · 진단 줄 '영웅 강화(#12)'. smoke '영웅 강화 — 전 등급 개방 · +1%/단계 · 상한 20 · 칭호'. 공지 [업데이트].

### 2026-09-25 v5.346 — 전투 중 레벨업 시 귀속 장비가 전투력에서 빠지던 결함

- 전투 레벨업이 h.cp = heroPower({grade, level}) 로 hero_id 없이 불러, 그 영웅에게 귀속된 장비(heroId)가 통째로 빠진 전투력으로 떨어졌다.
  refreshParty 주기 호출이 없어 방치 중엔 UI 를 만질 때까지 약해진 채였다(검증 실측: L+10 장비 리더 partyCP 160 vs 정본 239). → hero_id:h.hid 전달.
  영웅 상세 화면 전투력 표기도 같은 누락 → heroPower(e). 실물: Lv1→2 전투 레벨업 직후 partyCP 239 = 정본 239.
- smoke 'heroPower 호출 — 귀속 장비 누락 경로 없음'(객체 리터럴 호출에 hero_id 필수). 결정론 해시 불변(D 시나리오엔 장비 없음).
- 발견 경위: 워크플로 #12(조각 소비처) 검증 중 부수 발견. 시뮬은 heroPower 를 직접 써서 이 결함이 곡선에 드러나지 않았다.

### 2026-09-25 v5.345 — 터치 영역·작은 글자 (워크플로 #10)

실측(375×812, UI_SCALE 0.828): 작은 버튼 27px(화면 20~22px)·모달 ✕ 화면 22~24px·하위 ✕ 18px·드로어 라벨 11px(화면 9.1px)·스킬 쿨다운 9px(7.5px)·'인벤토리' 만 두 줄(그 칸 101 vs 81).
- .btn.sm/.btn.xs min-height 34px. ✕ 두 종은 모양 그대로 ::after 로 누르는 영역만 사방 8~9px 확장(겹침 없는 위치라 오탭 위험 낮음).
- 드로어 라벨 12px, 스킬 쿨다운 12px+그림자. 하단 내비 라벨 14px 한 줄 고정·좌우 여백 2px → 전 칸 높이 79 로 정렬.
- 전모달 터치 스윕(47모달·보이는 버튼 137) 375·360 모두 가림 0·화면 밖 0·오류 0. 튜토리얼 실클릭 완주 막힘 0.

### 2026-09-25 v5.344 — 제작 대기 연출·빈 상태 (워크플로 #18) · 사냥 라벨·스폰 페이드 (워크플로 #16 일부)

- **진행률 결함(검증 추가 발견)**: endAt 은 제작시간 배율을 쓰는데 진행률은 원래 sec 로 나눠, 버프(-50%) 중엔 시작 즉시 50% 로 표시됐다.
  S.craft.dur(실제 소요) 저장 + craftDur/craftProg 정본(구세이브는 sec 후퇴). 대장간 진행바·모루 링 모두 이 값을 쓴다.
- 모루 버튼: 제작 중 진행 링(::before conic-gradient --p, 0.5초 refreshHUD), 빈 모루는 '00:00'(끝난 타이머로 읽힘) 대신 '비어 있음'(nowrap 11px).
  대장간 진행 패널: '완성 예정 HH:MM' + 진행 중 흐르는 광택(.pbar.run — 레전더리 2시간 제작에서 바가 멈춘 것처럼 보이던 것; 동작 줄이기면 끔).
  (망치 불꽃·무한 맥동·재료 금점은 검증 권고대로 제외 — 장식이거나 '빈 모루' 신호와 중복)
- 빈 상태 .empty-state(그리드 전폭 + [대장간으로]): 인벤토리 탭(4칸 그리드 한 칸 79px 에 3줄로 깨짐 — 튜토리얼 직후 거의 모두가 봄)·영웅 착용 트레이(5칸 한 칸 5줄).
- #16 사냥 라벨: 좌상단 'WAVE N' 은 G-14 이후 시계에 완전히 가려졌다 → 우상단 '사냥터 · 등급 재료 · WAVE N' 한 줄 12px 굵게+외곽선, N등급은 밝은 색.
- #16 스폰 페이드: 스폰의 약 52% 가 화면 안쪽에 '툭' 나타났다 → 첫 그리기 벽시계(_bornW)부터 0.25초 투명→불투명·0.8→1배(발 기준). 그리기 전용·해시 불변.
  (입장 배너는 검증 결론대로 보류 — 효과 대비 우선순위 낮고 선택 화면 모달에 가려진다.)

### 2026-09-25 v5.343 — 타격 임팩트·피격 실루엣·모션 동기화 (워크플로 #15) + v5.338~340 리뷰 확정 2건

- **피격 번쩍임 = 오프스크린 실루엣**(silhouette): 스프라이트/시트 한 칸을 오프스크린에 그려 source-in 단색화 후 현재 변환 그대로 얹는다.
  종전 source-atop 사각 칠하기는 같은 사각형 안 이웃 몹·빛무리까지 물들였다(사망 잔상 흰 번쩍임 포함 — 같이 교체). ⚠ source-atop 재도입 금지(smoke 가 막음).
  흰 단계 기준 flash>0.05(같은 스텝 감쇠로 처음 그려질 때 0.07 — 0.04 기준이면 영영 안 보인다), fxFlash 끄면 흰 단계 생략.
- **임팩트 별**(fx 'impact'): 모든 타격 접촉점에 흰 코어 4각 별(치명 크게+방사선). 각도=순번(_impSeq, 난수 없음), 수명 0.24초(0.12초면 같은 스텝 갱신 탓에 1~2프레임),
  두 fx 수명 필터 모두 등록, 동시 16개, 품질 '하' 생략.
- **투기장 적 피격 반응**: hitFoe 에 flash·kb·impact·hitSfx(종전 35회 피격 동안 반응 0), drawFoe 실루엣(drawHeroSheet tint 인자)·넉백 오프셋, foes 루프 감쇠.
- **모션**: 스킬은 skillAnimT 로 0번 프레임부터(종전 임의 프레임 시작), 일반 공격은 시트 접촉 프레임(6)을 실제 타격 순간에 맞춤(atkT 읽기로 0.4초 감아올림 → 타격 후 7~14 벽시계),
  돌진은 대상 방향 × sin 이징. 전부 읽기 전용 — 해시 3종 불변·D5 통과. 헤드리스 60fps·최악 17ms(변경 전과 동일).
- 리뷰 확정 ①: 퀘스트 기본 탭의 주간·월간 분기가 죽어 있었다(questClaimable 이 주간·월간을 OR 로 포함) → dailyClaimable 분리. 실물: 주간만 → '주간' 탭.
- 리뷰 확정 ②: 길드 레이드·점령전은 길드 모달 위 openSub 오버레이라 currentModal='guild' — 복귀 목록의 'conquest' 는 죽은 항목이었다.
  입장 시 _subKey 우선 판정 + DG_BACK_PARENT 로 부모→하위 순 재오픈. 실물: 참전 2→1 → 즉시 결과 → 3초 뒤 guild+guildRaid 복귀.
  (QA 함정: '/입장/' 정규식이 '⟳자동입장' 토글을 먼저 눌러 입장 없이 '복귀 성공'처럼 보였다 — 입장 횟수 증감으로 확인할 것.)
- smoke 고정 길이 소스 슬라이스(enterDungeonFight +1500자)를 '다음 함수까지'로 — 앞에 코드가 늘면 검사가 잘렸다.

### 2026-09-25 v5.342 — 결과 연출 문법 확장 · 보스 HP 바 (워크플로 #17 ①②③ · #14 나머지)

- 투기장 결과 카드 = 던전 결과와 같은 rc-anim(제목 팝·승리 광선·패배 흔들림). 승급·강등은 토스트 → 카드 안 배너 .ar-promo(+승급음).
  growthBurst 는 티어 비교열을 1.4초 덮어 쓰지 않았다(검증). 결과 카드가 없는 튜토리얼 8단계 경로만 토스트 유지.
- 제작 결과: rc-anim — 희소성=연출 크기(N·R 팝만, E 보라 광선 rr-e, L 금 광선+legendaryFlash), 실패 흔들림.
  .sub-pop 안 광선(z:-1)이 팝업 배경 밑에 깔려 안 보였다 → .sub-pop .result-card{isolation:isolate}.
  결과 팝업 뒤 잔상('제작 중 · 남은 시간', 홈 자연 완료 시 마지막 모달 본문 예: 상점) → 대장간이거나 모달이 닫혀 있으면 대장간을 새로 그린 뒤 띄움.
  (④ 모달→모달 전환 애니메이션은 검증 결론대로 제외 — 탭형 즉시 전환이 관례이고 전모달 스윕 오탐만 는다.)
- 보스 HP 바: 기여도 패널(x6~156) 오른쪽~절전 버튼 왼쪽(x162~W-56)으로 이동(좁으면 전폭 후퇴), 잔상 띠(#ffd8a0, 0.25초 머묾→초당 50%, 그리기 전용 _gh* 필드·벽시계),
  HP % 표기. fxFlash 끄면 잔상 없음. 콤보: startDungeon·endDungeon·setHunt 에서 초기화(홈↔던전 이월), 던전에선 y=max(84,H*0.22)(부제·HP 바 아래).
- 결정론 해시 3종 불변·D5 통과(콤보·잔상은 표시 전용). 튜토리얼 실클릭 완주 막힘 0(resolveCraft 가 대장간을 먼저 그리는 변경 후 재확인).

### 2026-09-25 v5.341 — 튜토리얼 직후 '강해지는 길' (워크플로 #6 a·c·d)

실측: 튜토리얼 직후 리더(홈 출격)의 장비칸 10칸이 전부 비어 있고(튜토리얼 장착은 다른 영웅), 길잡이로 만든 장비는 인벤토리에만 쌓여
전투력 칩이 그대로였다. 리더가 권장을 넘어도(163초, 261>225) 12분 내내 최하급 사냥터(골드 ×1.9 차이)에 머물렀다 — 알림 없음.
- (a) 제작 결과 [리더 N에게 장착]: 리더의 그 부위가 **빈칸일 때만**(equipItem 은 같은 부위 기존 장비를 파괴 — 교체는 착용창 경고에 맡김).
  빈칸 술어는 전투력 계산과 같은 `equipped && (!heroId || heroId===리더)`. 튜토리얼 중 숨김. 장착 후 cpDeltaLine·세트 발동 토스트. 실물 130→137(+5.4%).
- (c) 몬스터 버튼 빨간 점 = huntUpgradeTier(): 안전 사냥터(showWipeAdvice 와 같은 정의 huntSafeTier)가 현재·**본 적 있는**(S.huntHintSeen) 단계보다 높을 때만.
  몬스터 화면에 '추천 … [여기서 사냥]' 행. 화면을 보면 점 소등(일부러 낮은 곳에서 재료 파밍하는 이용자에게 점이 계속 켜지지 않게).
- (d) 전투력 구성 '영웅 9종 합계' → '보유 영웅 N명 합계'. (b '다음 할 일' 카드는 길잡이 배너와 중복이라 제외 — 검증 결론)
- .nav position:relative(점 기준점). smoke '제작 결과 리더 장착(빈 부위만) · 추천 사냥터 점 · 구성 라벨'.

### 2026-09-25 v5.340 — v5.334~337 리뷰 확정 6건 + 전투 HUD 겹침 (워크플로 #14 일부)

독립 리뷰 워크플로(3관점 13에이전트, 발견마다 반박 검증) 확정분:
- **인트로 ✕ 되감김(상)**: 보상 팝업 ✕ → introDone 미설정 → 새로고침마다 인트로 재생 → 마저 받는 순간 startGuidedTutorial 재실행이 튜토리얼을 0단계로 되감음.
  ✕ = 건너뛰기(_introSkip: 남은 칸을 introClaimed 멱등 기록대로 지급 + introDone) + 시작 콜백 가드(tutStep>0 이면 리셋 안 함 — 구세이브 보호).
  검증자는 'v5.113 유실 재발'을 우려해 ✕ 동작 불변을 권했으나, 여기선 **지급 후** 끝내므로 유실이 없다. smoke 가 실행 경로로 검사(종전은 소스 정규식뿐).
- **길잡이 배너가 투기장 헤더를 매치 내내 덮음(상)** → 전투 중 배너 숨김(syncGuideBannerFight — 캐시만 비교하면 틱 사이 전투 전환을 놓쳐 실제 표시 상태를 본다).
- **배경 로드 탭 방치 수익 증발(중, v5.335 사각지대)**: 숨김 전이가 없어 _tabHideTs=0 → load() **뒤**에 document.hidden 이면 포착(초기값에 넣지 마라 — 정산 순서가 뒤집힌다). 실물: 포착 확인.
- 소환 수량 칩 '10회' 고정 라벨(소환권 2~9장) → 실제 값. 잠긴 창 visibilitychange 가드(_saveSealed). 반박 4건(ironhand 영구화·배치 조각 표기·콤보 가림·timepod 겹침 회귀 아님).
- **전투 HUD 겹침**(리뷰가 '회귀 아님'으로 반박했지만 실재 — #14): 전투 중 #stage-wrap.fight-on → 시계·축제·전투력 칩 숨김(기여도·보스 HP 바 왼쪽을 덮었다).
  투기장은 기여도를 top:60 으로(대전 헤더 55px 가 첫 줄 15px 덮음 → 0). 실물 390·360.

### 2026-09-25 v5.339 — 던전 즉시 결과 · 결과 뒤 복귀 (워크플로 #11)

후반 일일 전투 루틴이 '이미 이긴 싸움 관람' 7.7분·75탭(적 전투력 고정 → 18~90배 차이여도 전투 시간은 스폰 간격에 묶임).
- **Battle.finishNow()** = runUntilDone 을 무음 구간(_instantRun)으로 감싼 것 + 표시 전용 정리(콤보·흔들림·snapPositions). 결과는 관람과 **같다**:
  같은 stepOnce(FIXED_DT) 경로 + 전투 중 이용자 입력 없음(스킬 완전 자동). smoke **D6**(관람 도중 0.5·10·20초에 finishNow = 끝까지 60fps 관람 해시) 신설.
  ⚠ 보상을 '계산해서 지급'하는 별도 경로를 만들지 마라 — D6 이 깨진다. 무음 구간이 없으면 한 프레임에 coin 300+회 오실레이터가 생긴다(실측).
- 노출: enterDungeonFight 경유(투기장 제외) + 계열 첫 클리어 후(S.dgSeen — dgFamily = 이름 ' · ' 앞 + 'N단계' 제거; 승리 또는 race 완주 시 기록).
  기존 기록 소급(탑 _tower·요일 stats.ddStage·미궁 stats.emberBest). 버튼 #dgSkip(전장 우하단, 0.5초 HUD 틱 tickDgSkip).
- 결과 뒤 복귀: 길잡이 완료 후 + DG_BACK_OK 모달에서 입장했으면 3초 자동 퇴장이 홈 대신 그 모달로(골드던전 자동 입장 연전은 _dgLastBack 으로 첫 판 복귀처 승계).
- 실물(375·360): 탑(소급) 버튼 노출·클릭 43~72ms·효과음 2회·배너 0·3초 뒤 tower 복귀 / 요일 첫 판 숨김 → 클리어 기록 → 둘째 판 노출·dailydungeon 복귀. 공지 [업데이트] 추가.

### 2026-09-25 v5.338 — 일일 루틴 잔탭 (워크플로 #13)

- 일일 임무 [받기]가 openModal('quest') 로 기본 탭(임무목록)에 튕겨 5건 수령 = 10탭 → render() 제자리 갱신.
  퀘스트 기본 탭: 길잡이 진행 중이면 임무목록, 아니면 받을 것이 있는 첫 탭(일일→주간→월간). 실물 5탭 5건·주사위 +95·탭 유지.
- 탑 상자 교환이 1회마다 닫혀 기록서 18권 = 36탭 → 교환 후 팝업 재렌더(보유 수 갱신) + 즉시 save()(v5.309 규약).
  팝업은 .b5-wide(최대 320px)로 넓혀 품목명 두 줄 꺾임 제거(확인창·길드 공지 b5-pop 은 250 유지).
- 모험 타일에 남은 횟수 배지(advLeftBadge — 각 모달과 **같은 게이트 키**를 읽기 전용으로 읽는다: 요일 daily/5·골드 gold/3·월드보스 wb/1·
  탑 tower(+towerSweep, 1 Wave 도달 후)·약탈 raid/1(길잡이 중 '잠김')·미궁 ember/1·용광로 월 1회 소진 시 D-N). 보스는 횟수가 없어 배지 없음.
  입장권 보유는 반영하지 않는다(모달의 '가능 횟수'와 같은 의미). smoke '모험 타일 배지 — 게이트 키 일치 · 읽기 전용'.

### 2026-09-25 v5.337 — 영웅 소환 여러 회 (워크플로 #2)

소환 1회 ≈ 3.5탭(확인창·연출·결과·고급 조각 시 등장 확인) → 주간 30회 ≈105탭·월간 150회 ≈525탭·유료 소환서 800장 ≈2,800탭의 순수 노동.
일반 영웅 소환권 타일 확인창에 [1회][10회][최대 N회] 칩(튜토리얼 중 숨김·'소환' 글자 금지 — 손가락 TUT_PRIMARY_TEXT). summonBatch(n) =
summonRun 을 n 회 반복(판정·피티·stats.summons·즉시 저장 호출마다 동일 → 의뢰 집계 보존, smoke 앵커 불변). 결과는 한 장(요약 '소환 N회 · 고급 조각 K회'),
연출·영웅 등장 1회. 루비 타일·직업 지정은 1회 유지(루비 오소모 방지). 실물: 250장 [최대] 1회 → summons 0→250·결과 1장·오류 0. 튜토리얼 실클릭 완주.

### 2026-09-25 v5.336 — 칭호 판정 결함 (워크플로 #19)

- 길드 칭호(pioneer·outlaw, 레전더리·골드 +20%)가 **1위 NPC 길드 [신청] 한 번**으로 달성(가입 길드의 기준 점수를 더함) → 조건 = 내 기여 점수(S.guildScore) ≥ 51,000.
  이미 얻은 이용자는 mergeDefaults 1회 이관(_titleGuildV — FS_EXEMPT 등록, 신규 세이브는 load 신규 분기에서 1)으로 보존(U1).
- 조건형 칭호가 조건을 잃으면 보유 목록에서 사라지는데 착용 효과는 남던 불일치 → titleSyncOwn(): 달성한 칭호를 보유로 기록(5초 배지 주기·칭호 화면·로드).
  착용 중 칭호도 보유로 기록(효과가 이미 적용 중이라 줄지 않는다). t.own(lava)은 제외.
- 작열 8세트 = 영웅 분산 착용으로 도달(계정 전역 집계) — lava 유일 경로라 **유지**(3-6 보강).
- smoke '칭호 — 길드 1클릭 차단 · 달성 보유 기록 · 기존 획득 이관'.

### 2026-09-25 v5.335 — 숨김 탭 닫기 수익 증발 · 첫 보상 안내 모순 3건 (워크플로 #23·#9)

- **#23** 숨긴 채 창을 닫으면 숨김 구간 방치 수익 증발(5초 저장이 lastSeen 을 '지금'으로 밀어 computeOffline 이 닫은 시각부터 계산) →
  save() 가 숨김 중이면 lastSeen=_tabHideTs(숨긴 시각). 복귀 분기가 _tabHideTs=0 을 먼저 하므로 이중 정산 없음. _tabHideTs 선언을 save() 위로(TDZ).
  실물: 3h 숨김→닫기→재접속 offlinePending 1,699,757(기대 1,699,740·종전 0).
- **#9a** 인트로 '7일 출석 · 1일차'가 소환권만 주고 출석 미수령(그리드 1일 ✓·'내일 2일차' 문구와 모순, 출석 화면이 1일차 재제시) → 실제 1일차 수령
  (강화석 X40, 중복 가드) + 종전 소환권 1 은 환영 보너스로 유지(지급 감소 없음).
- **#9b** 환영 우편(루비 100 — 신규의 유일한 초기 루비)에 배지 없음 → MAIL_ITEMS 상수·mailPending() 배지(우편·☰). '화염검사' → '화염술사'.
- **#9c** 인트로 중 STEP 1/9 박스가 먼저 떠 카운트하다 1/20→0/20 되감김 → _introRunning(비저장 모듈 변수)으로 인트로 동안 박스·tutPoll 정지.
  해제 경로: startGuidedTutorial 대사 콜백(정상·✕) · introDone 조기 반환 · 새로고침(false 시작). 실클릭 튜토리얼 완주 110s 막힘 0.
- smoke '숨김 중 저장 lastSeen · 우편 배지 · 인트로 출석' 추가(시각은 게임 컨텍스트 Date 로 — 하네스 시계와 테스트 파일 시계를 섞지 말 것).

### 2026-09-25 v5.334 — 리뷰(v5.330~333) 반영 + 첫 세션 UX 3건(워크플로 #1·#7·#8)

**v5.330~333 독립 리뷰 워크플로**(3차원 리뷰 + 발견별 반박 검증, 10에이전트): 치명 0 · 확정 3건(중복 1) · 반박 3건. 반영:
- 투기장 주간 주사위 순위 역전(21~30위 X30·31~40위 X15 < 참여 X40 — v5.331 부터 실지급) → 21~30 X50·31~40 X45(참여 X40 유지, U1) + smoke 단조성 검사.
- personalRankBuffPct 삭제(표시 전용이던 티어 버프 — 되살리면 arenaGoldBuffPct 와 이중 가산).
- 투기장 보상표 라벨 음절 단위 줄바꿈(레/전/더/리) → .lrow.rw(라벨 nowrap, 줄바꿈은 금액 쪽). 360·375 전 행 1줄 확인.
**첫 세션 UX(발견 워크플로 반박 검증 통과)**:
- #1 길잡이 7단계 골드 정체(~50분, 안내 0) → guideGoldShort(): 배너 '골드 N 부족' + 버튼 [골드던전](오늘 남은 횟수 있을 때), 대장간 필요 골드 빨강+부족액
  + 길잡이 단계면 [골드던전 (n/3)] 보조 버튼, 토스트 안내. [제작]은 유지(칭호 '빈털터리' 경로 — 실물: poorClick 집계 확인). 배너는 5초 주기로 갱신.
- #7 길잡이 배너가 시계·축제·절전을 덮어 절전 가운데 탭이 대장간을 열던 것 → HUD 열을 배너 높이(--gbh, syncGuideOffset·ResizeObserver)만큼 내림.
  실물 375·360: 시계·축제·전투력·절전 elementFromPoint 전부 OK.
- #8 표기: 정산 경험치 '6.800000000000001%' → floor · 튜토리얼 대기 '28.277초' ceil 괄호 · .mat-chip 30px 고정 → 내용 폭(겹침 0).

### 2026-09-25 v5.333 — 던전 래퍼가 hpMul 을 버려 용광로 시련 ×3 이 라이브 미적용이던 결함

enterDungeonFight 가 Battle.startDungeon 에 고정 필드만 넘겨 cfg.hpMul(용광로 시련 3)이 버려졌다 — v5.301 의 '9.8초 접전' 실측은 startDungeon 직접 호출값이었고
라이브는 ×1(약 1/3 시간에 종료)이었다. hpMul·overtime 을 넘기게 수정(D 시나리오는 startDungeon 직접 → 해시 불변). 실물: 파티 CP 4,800 vs 적 30,000 →
14.8초 전투·승리(설계 의도·확인창 '장기전(45초 안에 처치)'과 정합). 라이브 난이도 상승이라 [수정] 공지. smoke '던전 래퍼 옵션 전달'.
**원칙: 새 전투 옵션을 startDungeon 에 추가하면 enterDungeonFight 에서도 넘겨라.**

### 2026-09-25 v5.332 — 정확성 4건(워크플로 #20·#21·#4·#3, 전부 반박 검증 통과)

- **#4 던전 종료 후 홈이 3인 파티로 남음**(전투력 3배·처치 +71%) → enterDungeonFight onEnd 에서 finally Battle.refreshParty(투기장 복귀와 같은 자리).
  ⚠ endDungeon·loop 에서 부르지 말 것(D 시나리오가 startDungeon 직접 호출 → 기준 해시 변경 / loop 는 렌더에서 시뮬 변경). 해시 불변 확인.
- **#3 고급 조각(레전더리 판정 = 조각 +3)에 미보유 영웅 '영웅 등장'·'획득!' 거짓 표시**(신규 계정 63% 노출 — 몬테카를로) → 폴백 삭제,
  실제 해금만 연출. summonRun 이 legendJobs 반환(난수 순서 불변) → 결과 카드 금테 '🌟' + 미보유 직업 조각 진행(n/25). 배너 '고급 조각!'.
- **#20 옵션 재설정**: 잠금 무시·봉인(레전더리 사선) 행 과금 → rerollRow() 순수 함수(locked/sealed/short/ok), 잠긴 행 사선. '옵션은 전투력 미반영(준비 중)' 안내.
  실효과 부여는 별도 설계 과제(스탯 풀·확률 ⚠비전미확인 — 후보: heroCritRate/heroCritMul 가산). 재화사슬 감사 문서의 '옵션 리롤 = 유효 소비처' 전제는 사실과 다름.
- **#21 일괄 분해**가 보유형 효과(고서·물약)를 지워 전투력 −3.3% → 종류당 1개 보존(HOLD_FX_RE)·목록 동일성 삭제·확인창 안내, 단건 마지막 1개 경고.
  **코스튬 레거시 누수**: 루비 코스튬 구매가 S.costumes 를 올려 비매품 3종(flame·frost·gold) 무료 해금 → 카운터 증가 삭제(기존 해금분은 유지 — U1).
  회색코인 '한정 코스튬'은 없는 레거시 1벌을 실제로 지급(전부 보유면 품절 숨김). 인벤토리 코스튬 수 = 실제 보유 수.
- smoke 2건 추가(구 코드에서 둘 다 실패 확인: 홈 영웅 2명·거짓 등장 1회 검출). 튜토리얼 실클릭 워커 완주(막힘 0).
- 별건(미처리·판단 필요): enterDungeonFight 가 cfg.hpMul 을 Battle 에 넘기지 않는다 → 용광로 시련 hpMul 3 이 라이브에 미적용일 수 있음(검증자 지적). 넣으면 라이브 난이도 3배 — 별도 확인.

### 2026-09-25 v5.331 — 표시만 되고 지급·동작이 없던 약속 정리 (워크플로 #22, 반박 검증 통과)

- **투기장 매일 티어 골드 지급**(rollDaily, ARENA_TIER_ROWS = 표 = 지급 정본, raw). 표 금액 1/10(레전더리 2,500만→250만/일):
  원값은 초반 방치 수입(~2,700만/일)을 거의 두 배로 만들고, 매칭(적 CP≈내 CP×0.85~1.15)상 승률 ~50% 라 신규도 빠르게 상위 티어.
  대상 = 투기장 1회 이상 입장. 주차가 지났는데 투기장을 안 열어 리셋이 안 된 경우 브론즈로 지급(옛 레전더리 영구 지급 구멍 방지).
- **투기장 주간 순위 주사위 지급**(arenaWeekRoll 리셋 직전 순위, ARENA_DICE_ROWS 파싱 — arenaWeeklyDice). 이번 주 1판 이상만.
- 문구 정리(경제 0): '입장 제한 시간 10:00~12:00' 5곳 삭제(판정 코드 0) · 점령 '매주 초기화' → '버프 계속 유지'(확인창 '상시 적용'과 정합)
  · 길드 도움말/공지의 '길드 랭킹 매주 초기화' 삭제 · 7/27 가짜 점검 공지를 실제 투기장 주간 정산 안내로 교체.
- **하지 않은 것(검증자 판단)**: 점령 주간 리셋(기존 이용자 ×1.8 광산 버프 너프·U1 위반) · 10~12시 입장 게이팅(순수 손해) · _tower 월간 리셋(소탕 기준값 너프).
  **보류**: 월드보스·탑 월간 랭킹 보상(WB_REWARD·TOWER_REWARD) 지급 — NPC 랭킹 기반 정산 설계 필요. 표 문구 '매월 N일 초기화'는 아직 그대로.
- 시뮬은 투기장을 모델링하지 않는다(기준선 불변: 100h N0.5/R10/E48/L96). smoke '투기장 매일 티어 골드·주간 주사위' 추가. 공지 [업데이트] 1건.
- 작업 중 사고: 점령 토스트 치환에 '/*' 주석을 닫지 않아 문법 오류 — node --check 가 즉시 잡음(커밋 전). 치환 문자열에 주석을 넣을 땐 닫는 '*/' 확인.

### 2026-09-25 v5.330 — 획득 배율 '표시 = 적용' 정합 (다중 렌즈 발견 워크플로 #5, 반박 검증 통과)

**발견 경로**: 자율 주행 다중 렌즈 워크플로(5렌즈 발견 → 후보별 반박 검증, 35에이전트) — 30후보 중 27건 생존. 이 항목은 정확성 렌즈.
- 프리미엄 방치 골드 **이중 적용**(idleTick ×2 + addGold goldBuffMul ×2 = 약속 ×2 가 ×4, 실측 346→1,385G/s) → idleTick 의 ×2 삭제. 실물 ×2.000.
- 마을회관 계수: 적용 0.05%p/Lv vs 표시 0.2%p/Lv(v4.7 실측) → VILL_BUFF_PP 로 단일화(villHallMul).
- **약속만 있고 미적용**: 훈련소(유료 판매)·길드 +5%·길드장 +35%(창설 확인창)·무쇠 캠프 경험치 +50%(점령 확인창) → 관문에 구현(U1 — 약속을 지우지 않고 지킨다).
  관문: villHallMul · villTrainMul · guildGainMul · addGoldMul · idleGoldMul · idleGoldPerMin · heroExpMul.
- 분해 환급(단건·일괄) raw — 견적=지급(골드 버프가 붙어 제작가 초과 → 제작→분해 순환 이익). 골드던전·탑은 골드 획득 콘텐츠라 버프 유지(시뮬도 non-raw 모델, raw 전환은 U1 위반).
- 버프 화면·정산·절전의 배율 표시를 관문 함수에서 파생(표시 합계에 미적용 항목 포함·적용 항목 누락이던 것). '개인랭크 버프'(미판독·미적용) 행 → 실제 적용되는 '투기장 순위 버프'. 약탈 문구 '약탈 보상 골드 +20%'.
- smoke '획득 배율 관문' 추가(구 코드 실패 확인). 기본 상태 배율 ×1 — 결정론 해시·시뮬 등급 도달 불변(100h: N0.5/R10/E48/L96).
- 이용자 불리 정정(프리미엄 방치 ×4→×2) 포함이라 [수정] 공지로 숨기지 않고 알림.

### 2026-09-25 v5.329 — 코드리뷰(v5.323~328) 반영 (확정 결함 0 · 가능성 3건 정리)

2차 독립 코드리뷰: check·D1~D5 재실행 통과, 확정 결함 0. 재료 상자 dataset.open 선설정은 종전 연타 경합을 오히려 닫았다(확인).
가능성 3건을 한 줄씩 정리: ① 드랍 비행·도착 반짝임이 연출 줄이기에 미연동 → fx-noflash/reduced-motion 규칙 ②결과 창을 닫은 뒤에도
지연 공개 효과음이 울림 → reveal 에서 isConnected 확인 ③ growthBurst/enhBurst 호출 try 보호(연출 실패가 저장·갱신을 막지 않게).
남긴 것: queueWipeAdvice 900ms 와 closeModal 250ms 경로가 서로 다른 전멸로 좁은 창에서 겹치면 이론상 2회 표시 — 재현 조건이 좁고 회귀 아님.

### 2026-09-25 v5.328 — 코드리뷰(v5.314~322) 반영: 전멸 분석 유실 수정

독립 코드리뷰(격리 워크트리에서 D1~D5 포함 검사 통과): 치명 결함 0 · 렌더 보간 복원/결정론/wipeRemedies 무부작용 확인.
- [중·확정] closeModal 훅이 250ms 사이 다른 모달이 열리면 대기 중 전멸 분석을 **버렸다**(queueWipeAdvice 쪽엔 재대기가 있어 비대칭) → 재대기.
  실물: shop 열린 채 전멸 대기 → closeModal→openModal('hero') 즉시 → hero 동안 pending 유지 → 닫으면 패널 표시 확인.
- [하] _visibilitySettle 주석의 '상한 8시간' → OFFLINE_CAP_H · [하] 시뮬 offcap=0 이 || 로 버려지던 것 → !==null.

### 2026-09-25 v5.327 — 레벨 업 글자 연출

레벨업 fx(링·별)에 '레벨 업!' 글자(팝 1.4→1 · 상승 · 페이드)를 더했다 — 무엇이 일어났는지 문자로 확정. 그리기 전용·해시 불변.
참고: 그리기 코드의 rnd() 는 Math.random(비시드)이다 — 전투 시드 난수(bRnd)와 별개라 연출 지터가 결정론을 깨지 않는다.

### 2026-09-25 v5.326 — 던전 결과 연출

showDungeonResult 결과 카드에 움직임만 더했다(3초 자동 퇴장 G-80 결정 유지): 승리 = 제목 팝(1.7→1) + 아이콘 뒤 회전 광선,
획득 칩 = 250ms 뒤 90ms 간격으로 튀어 오름(`.dg-pop`), 패배 = 제목 팝 뒤 짧은 흔들림. 연출 줄이기 연동.

### 2026-09-25 v5.325 — 각성·결정 달성 연출(전투력 전후)

토스트 한 줄이던 각성(기본·심화)·각성의 결정 달성을 `growthBurst(title, lines, kind)`(enhBurst 카드 공용판)로:
'각성 +5 / 전투력 276 → 280 (+1.4%) / 계정 전체 스탯 +1.5%' — 5단계마다 이정표. `cpDeltaLine(cp0,cp1)` 은 totalCP 전후.
(전투력 % 가 명목 +1.5% 보다 작게 나오는 건 heroPower 의 반올림 — 정상)

### 2026-09-25 v5.324 — 소환 연출: 영웅 초상 등장 · 카드 뒤집기 · 고등급 예고광

- 영웅 등장(heroRevealFx)이 **직업 아이콘**(같은 직업이면 같은 그림)이었다 → `heroPortrait` 실제 초상 + 등급색(--hg) 회전 광선·후광·팝.
  ⚠ heroPortrait 크기는 em — `.hr-art`(64px)를 물려받으면 416px 가 된다 → `.hr-pic{font-size:16px}` 로 고정(실측 86px).
- 재료 상자: 열 때 카드 뒤집기(flip-in), **영웅·레전더리 상자는 열리기 0.28초 전 등급색 예고광+떨림**(E 보라·L 금) + 등급별 효과음.
  모두 열기 간격 35→70ms(너무 빨라 읽을 수 없었다). 영웅 조각 결과 카드는 90ms 간격으로 뒤집히며 깔린다.
- 연출 줄이기 연동(번쩍임 끔 → 광선 숨김 · OS 동작 줄이기 → 회전·떨림 정지).

### 2026-09-25 v5.323 — 드랍이 실제 재화 카운터·인벤토리로 날아간다

종전 드랍은 16px 이모지가 캔버스 윗변으로 올라가 사라졌고(재화 카운터는 캔버스 밖 DOM 이라 닿을 수 없음), 몬스터 밑에 그려져 가려졌다.
→ ① 캔버스 `drawLoot`: 처치 지점에서 직접 그린 금화/보석이 통 튄다(t 0~0.35, 몬스터 위에 그림) ② `flyLoot`: DOM 비행체가 곡선(바깥 가로
선형 × 안쪽 세로 가속)으로 `#curGold`(골드) / 인벤토리 내비(재료)까지 날아가 도착 시 `.loot-hit` 반짝임. 동시 14개 상한.
그리기 경로에서만 넘긴다(`d._flown` 표시 — x/y/t 가 아니라 보간 복원 대상 아님). 해시 불변. 홈 사냥 전용(던전 onKill 은 drop 미호출).

### 2026-09-25 v5.322 — 오프라인 상한 8h → 12h (시뮬 실측 근거) + 개선 묶음 공지

- 근거: 설계 종합 P5(강의 규칙집 090강 — 8h 는 수면과 충돌·12h 가 초기 기준 최적) + **실측(캐주얼 2400h·시드 42)**:
  | 지표 | 8h | 12h |
  |---|---|---|
  | 등급 도달 R/E/L | 24.5/97/169h | **동일** |
  | 최종 CP | 766,642 | 780,247 (+1.8%) |
  | 액션 이벤트 | 497 | 584 (+17.5%) |
  | 후반(600h~) 밀도 | 3.3/접속일 | 4.5/접속일 |
  | 최장 공백 | 7.5h | 7.5h |
  | 각성 | +46 | +48 · 결정 가호 131→232회(늘어난 골드 흡수) |
  → 초반 곡선 불변, 가장 성기던 후반 밀도만 오른다. **캐주얼 2400h 새 기준선 = 12h 열.** 풀 플레이 시뮬은 오프라인이 없어 영향 없음.
  상한은 올리기만(U5) — 다시 낮추지 말 것. 이 표는 `node balance-sim.mjs 2400 casual [offcap=N]` 으로 재현한다.
- NOTICES 최상단에 개선 묶음 공지(오프라인 12h·전투력 표시·전멸 분석·강화 결과·전투 연출·연출 설정·진행도 보호).
- smoke '주간 축제' 의 공지 [0] 위치 고정 검사 → 존재 검사(some). 새 공지는 항상 맨 위에 추가하는 관례라 위치 고정은 매번 깨진다.

### 2026-09-25 v5.321 — 연출 줄이기 설정(흔들림·번쩍임·숫자) + 오프라인 상한 단일 출처

- 설정 '연출' 3토글(`S.settings.fxShake/fxFlash/fxDmg`, 기본 켜짐 — 기존 체감 불변) · `fxOn(k)` 관문 · OS `prefers-reduced-motion`
  이면 흔들림 자동 끔 · `body.fx-noflash` 가 CSS 번쩍임(레전더리 광원·강화 광선·보스 비네트)을 약하게. 전부 그리기 전용(해시 불변).
  실물: 숫자 끔 → 캔버스 숫자 그리기 306→150(남은 150 = 영웅 레벨 배지 매 프레임) · 새로고침 후 유지.
- **오프라인 상한 `OFFLINE_CAP_H` 단일 상수**: 8 이 game.js 4곳·시뮬·스모크에 흩어져 있었다 → 전부 상수 참조(값 8 그대로).
  시뮬은 `offcap=N` 인자로 가정 실험 가능(게임 무수정).

### 2026-09-25 v5.320 — 정체 계단 패널 '전멸 분석' (장기 설계 종합 1순위 P2)

홈 사냥 전멸 시 토스트 한 줄 + 최하급 후퇴뿐이라 '무엇을 해야 다시 올라가나' 를 이용자가 찾아야 했다 → `showWipeAdvice(tier)`:
권장 전투력 vs 리더 전투력 막대·부족분 + 수단 최대 3개(리더 레벨 +10 / 안전 구간 장비 강화 +1 중 골드 대비 최고 효율 / 빈 부위
채우기)의 **전투력 +N(+%)·비용·[가기]** + [안전 사냥터로](리더 전투력이 권장을 넘는 최고 사냥터 — 후퇴 규칙은 유지, 복귀 한 번을 줄임).
- 수단 수치는 heroPower 정본의 가정 계산(임시 반영→계산→finally 복원). smoke '전멸 분석 패널' 이 호출 전후 S.equips·전투력·골드 불변을 잡는다.
- 다른 화면을 보는 중 전멸하면 빼앗지 않고 닫는 순간 띄운다(`_wipePending`, closeModal 훅).
- 실물: 실제 전멸(Lv12 로 권장 3,600 사냥터) → 패널 표시 → [안전 사냥터로] 로 0→2단계 이동 확인.

### 2026-09-25 v5.319 — 강화 결과 연출(전후 수치 · 성공/이정표/하락/유지/파괴)

토스트 한 줄이던 강화 결과를 `enhBurst(e, e0, destroyed)` 로: 성공은 방사 광선 + '+N 강화 성공' + 대표 스탯 전후
('마법 공격력 1,400 → 1,540 (+10.0%)' — statLine 과 같은 식 `(base+enh×per)×등급mult`), 5단계마다 이정표(크게·legendary 음),
하락은 붉은 흔들림+전후, 하락방지·극한 실패는 회색 '단계 유지', 파괴는 암적색 균열. #modal-root 위 비차단·1.4초 자동 제거.
실물 QA: 4결과 전부 난수 고정(한 evaluate 안에서 override→click→복원 — 게임 루프의 연출 난수가 끼어들면 순서가 어긋난다) 확인.
근거: 설계 종합 V2(강의 규칙집 '강화 전후 차이를 숫자와 시각효과로 동시에').

**장기 설계 종합(기획팀장, 자료 저장소 BatMakeGame 근거 — 읽기 전용) 도입 우선순위 요약**:
상 = 정체 계단 패널(부족 전투력+해결 수단 3개를 수치로) · Number Feel 묶음(진행 중) · 업데이트 안전 규칙 게이트(U1 비감소·U3 승계 경로
동시 출고·U5 캡 단조·U7 소급 비대칭·U11 천장 보존) · 오프라인 상한 8→12h 검토 / 중 = 날짜 기반 콘텐츠 스위치 표 · 탑 정기 확장+직전
구간 완화 · 진행도 조건형 캐치업+복귀 7일 · 4주 시즌 틀 · 제작 실패 위로·천장 / **비권고 = 계정 전체 환생**(동일 유형 표본 26개 중 0건 —
우리 무한 결정 축이 이미 권장형 '층 얹기'). 연출 규칙은 자료가 가장 빈약한 축(타격감·사운드 규칙 0건) — 연출 사양은 실물 검증으로 확정.

### 2026-09-25 v5.318 — 우두머리 등장 경고 배너

토스트 한 줄('👑 … 출현!')이라 일반 알림과 구분이 안 되던 보스 등장을 `bossBanner(name,col)` 로 교체(던전 보스·홈 군주 2곳):
가장자리 붉은 비네트 + 중앙 띠 펼침(scaleX, 0.18초) + 이름(보스 색) + 낮은 이중 타격음(sfx 'boss' 92Hz 하강) · 1.4초 뒤 자동 제거.
순수 DOM·pointer-events:none — 결정론 기준 해시 불변.

### 2026-09-25 v5.317 — 데미지 숫자 위계(일반 흰 16 · 치명 금 그라디언트 24+'치명' · 스킬 적주황 26)

- 일반 타격 숫자가 영웅 직업색이라 청록 바닥에서 안 보였다 → 흰색. 직업색은 파티클에만.
- 같은 대상 250ms 연타는 16px 씩 위로 쌓기(최대 3단, `stackY` — 벽시계 기반 그리기 전용 필드) · 동시 숫자 24개 상한.
- ⚠ **상한 초과분을 fx.splice 로 지우면 안 된다** — dmgText 는 bolt 착탄(fx.forEach 안)에서도 불려, 순회 중 배열이 줄면 다음
  fx(착탄 대기 bolt)의 진행이 한 스텝 밀려 **전투 결과가 바뀐다**. t=1(수명 끝) 표시로 다음 filter 에 맡긴다.
- 캔버스 가장자리 숫자는 안쪽으로 끌어들인다(그리기 좌표만). 기준 해시 불변.

### 2026-09-25 v5.316 — 전투 렌더 보간(20Hz 뚝뚝 → 화면 주사율로 매끄럽게)

전투 시뮬은 20Hz 고정 스텝이라 몬스터·영웅 위치와 숫자·파티클·드랍이 50ms 마다 한 칸씩 움직였다.
**실측(캔버스 해시를 매 rAF 채취, 90프레임)**: 라이브 v5.315 연속 동일 프레임 54/89(61%) → v5.316 0/89.
- `pumpFrame` 이 스텝 직전 위치를 `_px/_py` 로 남기고, `draw()` 가 그리는 순간에만 x,y 를 α(=acc/FIXED_DT) 보간,
  fx 시간은 `FX_RATE`(종류별 진행 속도)×잔여시간만큼 앞당겨 `drawScene()` 을 그린 뒤 **finally 에서 원값 복원**.
- 한 스텝에 60px 넘게 옮긴 것(스폰·편성 재배치)은 보간 제외. 결정론 기준 해시 3종 불변(검증은 draw 를 부르지 않는다).
- ⚠ drawScene(구 draw) 안에서 x·y·t 를 쓰면 복원으로 사라진다 — 읽기 전용으로 유지할 것(2026-09-25 전수 확인: 쓰기 0건).

### 2026-09-25 v5.315 — 홈 전투력 칩(+N▲/−N▼) + 연출 P0 결함 2건

- **홈에 전투력이 아예 없었다**(영웅→스탯 모달에서만 보임) → 축제 배지 아래 `#cpChip`(⚔ fmt). 0.5초 주기
  `tickCpChip()` 가 증감을 감지해 초록 '+N ▲'(칩 맥동)·빨강 '−N ▼'. 첫 호출은 기준값만(부팅마다 '+전체' 방지).
  클릭 → `openCPBreakdown(true)`([닫기] 가 홈으로). 투기장 모드에서는 숨김.
- **'0' 데미지 숫자**: 홈 원거리 영웅의 보여주기용 발사체(bolt dmg:0) 착탄이 hitMob(…,0) 을 불러 '0'·번쩍임·넉백·타격음 →
  `hitMob` 가 dmg≤0 이면 즉시 반환(HP 불변이라 해시 동일).
- **치명타 흔들림이 궁극기 흔들림(0.4)을 0.14 로 끊던 것** → `shake=Math.max(shake,0.14)`.
- 기준 해시 3종 불변 확인. 근거: 아트 디렉터 연출 대조 보고(참고 스크린샷 약 40장·8종 — 연출 '문법'만, IP 무관).
  **남은 연출 백로그(우선순위)**: ① 데미지 숫자 위계(일반 흰색/치명/스킬 — 직업색은 숫자가 아닌 파티클에)
  ② 드랍이 HUD 재화로 날아가 카운트업 ③ 보스 등장 경고 배너·HP 잔상 바 ④ fx 진행을 렌더 프레임 dt 로(현재 20Hz 고정
  스텝이라 숫자·파티클이 끊겨 보인다 — bolt 는 전투 계산이라 스텝에 남긴다) ⑤ 임팩트 별·흰 실루엣 피격
  ⑥ 강화·각성·레벨업 달성 연출 ⑦ 스테이지 전환·승패 카드 ⑧ 소환 카드 뒤집기 ⑨ 연출 줄이기 옵션(흔들림·번쩍임·숫자).

### 2026-09-25 v5.314 — 전투 타격감 1차(연출 전용 · 전투 결과 불변)

대표 지시("자료 저장소를 보고 장기적 개선 + 연출을 실제로 구현")의 첫 묶음. 인벤토리 감사로 확인된 빈칸만 먼저 채웠다.
- 데미지 숫자: 큰 수 표기(fmt 만·억) + 외곽선 + 치명타 팝(1.6→1배) + 후반 페이드. 종전 원시 정수는 후반에 몬스터 폭보다 길었다.
- **단일 스킬 데미지가 두 번 찍히던 결함** 수정(hitMob 에 글자색 인자 `txtColor` 추가, 중복 dmgText 삭제).
- 몬스터 사망 잔상(fx 'die' — 흰 번쩍임→1.3배 부풀며 떠올라 0.45초 소멸) · 피격 넉백(그리기 오프셋 5px, m.x 불변).
- 타격음: sfx 표의 hit·crit 이 호출 0건이었다 → hitSfx(일반 180ms·치명 140ms 간격 묶음).
- **결정론 확인**: D1~D5 통과 + 기준 해시 3종이 변경 전과 동일(2f661908…/85599e13…/44936408…).
  원칙: 연출은 fx·그리기 오프셋·벽시계만 쓴다 — 시드 난수·전투 좌표·HP 를 건드리면 해시가 깨진다.

### 2026-09-25 v5.313 — 튜토리얼 손: 손끝이 누를 버튼 안쪽을 짚는다 (대표 지적)

종전엔 위를 향한 👆 이모지를 버튼 **위쪽**(top-26)에 두어 손끝은 허공, 손바닥이 버튼 윗변에 걸쳤다.
이모지는 OS 글꼴마다 손끝 위치도 달라 좌표를 맞출 수 없었다. → 손끝 좌표가 고정된 SVG 손(TUT_HAND_SVG,
손끝 = SVG (15,1)) + `#tutFinger` 를 '손끝 한 점'(크기 0)으로. 기본은 아래에서 올라와 버튼 세로 60%를
짚고, 아래 공간이 모자라면(하단 내비) `.tf-down` 으로 뒤집어 위에서 40% 를 짚는다. 톡 애니메이션은 손가락
축으로만 움직여 손끝 점이 흔들리지 않는다. 실클릭 워커에 **손끝 검사**(손끝 점이 목표 사각형 안 · 손 그림이
화면 안) 추가 — 9단계 36개 목표 전부 통과. `SHOTS=<폴더>` 로 목표별 확대 캡처를 남긴다.

### 2026-09-24 v5.312 — 타이틀 문구·START 하단 배치 + 장시간 누수 소크(결함 0)

- **타이틀**: 문구·START 가 화면 가운데(키아트 인물 얼굴·상반신 위)에 겹쳐 있었다 → `justify-content:flex-end`
  + 하단 페이드 그라디언트로 발치 영역에 배치. 375×812·360×640 확인.
- **누수 소크**: `docs/qa/20260924-장시간-누수-소크-실물QA.cjs` — 5분 방치 전투(401킬)+1분마다 47모달 전수
  열고 닫기. DOM 365→462 는 채팅 40줄 상한까지 차는 워밍업이고 이후 ~465 에서 평탄, 힙 2.9→3.5MB 평탄,
  에러 0. **누수 없음.** (판정은 '단조 증가 + 20%' — 채팅 상한 도달 전 증가는 정상)
