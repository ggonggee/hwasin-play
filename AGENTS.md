# AGENTS.md — 화신(Hwasin) 작업 규약

AI 코딩 에이전트가 이 저장소에서 작업할 때 지키는 규칙. 사람이 읽어도 된다.

> 처음 이어받는다면 **`HANDOFF.md` 를 먼저 읽어라.** 아키텍처·함정·남은 작업이 거기 있다.
> 이 문서는 "작업할 때의 규칙"만 담는다.

---

## 이 프로젝트는 독립적이다

외부 조직 규칙·전역 설정·회사 파이프라인에 의존하지 않는다. 이 저장소 안의 문서
(`README.md`, `HANDOFF.md`, `GAME_OVERVIEW.md`)와 소스만으로 완결된다. 다른 경로의 문서를 찾아 읽을 필요 없다.

**이 저장소는 개발 원본이자 GitHub Pages 배포본이다.** `main` 에 푸시하면 그대로
https://ggonggee.github.io/hwasin-play/ 에 반영된다 — 즉 **푸시 = 배포**다. 검사를 통과하지 않은
상태로 푸시하면 이용자가 그걸 그대로 본다.

빌드 도구·패키지 의존성도 없다. Node 18+ 만 있으면 된다(검증 스크립트용). 게임 자체는 바닐라 JS라
브라우저만 있으면 돈다.

---

## 코드를 고칠 때

### 1. 편집 대상은 3개뿐

```
index.html · style.css · game.js   (저장소 루트)
```

`index.html` 이 원본이다. **단일 파일 미러(`화신.html`)와 `npm run build` 는 v5.82 에서
폐기됐다** — 아이콘을 외부 PNG 에셋으로 옮기면서 단일 파일로 묶는 의미가 없어졌고, 파일도
`npm run check` 에서도 제거했다. `npm run build` 는 지금 돌려도 실패한다(그게 정상이다).

### 2. 커밋 전 반드시 통과시킬 것

```bash
npm run check
```

문법 → 정적 무결성 → 런타임 스모크. 셋 다 통과해야 한다.
실패한 채로 커밋하지 마라.

### 2-1. 배포할 때

```bash
npm run deploy
```

`npm run check` 를 먼저 돌리고 통과할 때만 `git push origin main` 한다. 푸시 전에 `git fetch` 로
원격이 앞서 있지 않은지 확인하라 — 다른 경로에서 올린 커밋과 히스토리가 갈라진 적이 있다.

그리고 **`index.html` 의 캐시버스터(`style.css?v=` / `game.js?v=`)를 버전에 맞춰 올려라.**
빼먹으면 기존 이용자가 바뀐 CSS/JS 를 받지 못한다(실제로 겪은 사고다).

### 3. 새 상태 필드를 추가하면

- `freshState()` 에 기본값을 넣는다.
- **단, 세이브 마이그레이션 판정용 플래그는 `freshState()` 에 넣지 마라.** `mergeDefaults()` 가
  먼저 채워버려서 이관이 통째로 스킵된다 (`HANDOFF.md` 3-3 참조).
- 구버전 세이브로 로드해도 죽지 않는지 `npm run smoke` 로 확인한다(구세이브·빈세이브·손상세이브를
  자동으로 검사한다).

### 4. 재료를 다룰 때

```js
matGain('흑염석', n)     // ✅ 실제 재료명
matGainGrade('R', n)    // ✅ 등급만 정해졌을 때
S.mats['R'] += n        // ❌ 등급 키는 존재하지 않는다. NaN 이 된다.
```

이 실수는 과거에 **다섯 번** 반복됐다. 자세한 배경은 `HANDOFF.md` 3-2.

---

## 벤치마크를 대조할 때

### 사실 판정의 우선순위

```
docs/reference/ 스크린샷  >  docs/design/ 기획서  >  추론
```

원작 스크린샷이 최우선 근거다. 기획서와 스크린샷이 어긋나면 스크린샷이 이긴다
(그리고 기획서를 정정한다).

### 지켜야 할 태도

- **화면에 적힌 숫자를 실제로 세라.** "탭이 4개 있다" 같은 구조 기록만 남기고 헤더의 `30마리` 를
  안 세서 놓친 사고가 있었다.
- **근거 없음을 부재의 근거로 삼지 마라.** "그 버튼을 누른 캡처가 없다"와 "그 기능이 없다"는 다르다.
  확인 못 한 것은 `⚠비전미확인` 으로 표시하고 넘어가라. 지어내지 마라.
- **한국어 UI 스크린샷은 오독하기 쉽다.** 작은 글씨는 확대해서 확인하고, 애매하면 판독 문서
  (`docs/design/20260725-화신-원작플로우-전량판독.md`)를 교차 참조하라.

---

## 타사 IP 절대 금지 — 법무 사안

벤치마크에서 가져오는 것은 **구조·개수·수치·배치·정보 위계**뿐이다.

**금지**: 원작의 게임명·캐릭터명·아이템 고유명·NPC명·서버명·세트명·브랜드 문구를 결과물에 쓰는 것.

**주의 — 1:1 기계 치환도 위험하다.** 과거에 원작 캐릭터명을 자동으로 다른 이름에 매핑했더니,
원작 IP는 피했지만 **다른 회사 게임 캐릭터명과 표기가 겹치는 이름**이 12건 생겼다
(Dota·LoL·캐슬바니아 등). 새 명칭을 만들 때는 타사 IP와 겹치지 않는지 확인하라.

작명은 화신 세계관(불·대장간·다크 판타지) 안에서 오리지널 조어로 한다.

---

## 작업 결과를 보고할 때

- **결론 먼저, 근거는 뒤에.**
- 무엇을 확인했고 무엇을 확인하지 못했는지 구분해서 밝혀라. 검증 안 한 것을 검증한 것처럼 쓰지 마라.
- 테스트가 실패했으면 실패했다고 출력과 함께 말해라. 건너뛴 단계가 있으면 그렇다고 말해라.
- 추정치를 확정값처럼 쓰지 마라. 근거가 없으면 `⚠비전미확인` 을 붙이고 어떤 캡처가 필요한지 적어라.

---

## 문서를 쓸 때

- 한국어로 쓴다.
- 왜 그렇게 했는지(근거·판단)를 남긴다. 무엇을 했는지만 적으면 나중에 되돌릴 수 없다.
- 코드 주석도 마찬가지다. 특히 **"이렇게 하면 안 되는 이유"** 는 반드시 남겨라 —
  이 저장소의 함정들은 대부분 주석이 없어서 반복됐다.
