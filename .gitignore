# 작업 원본(공개 배포에 불필요)
*.psd
*.ai
*.clip

# 도구
node_modules/
.DS_Store
Thumbs.db

# 내부 문서·리서치는 이 공개 저장소에 올리지 않는다 (구 개발 저장소 docs/ 참조)
docs/

# 회귀 가드 데이터 — 로컬 정본 관리(공개 저장소 미포함). 백업: 내부 저장소 docs/design/
ip-banlist.json
